# autoresearch — loop memory

> Read **Lessons** before every run; add a **Run Log** entry at the top after every run.
> Full contract: `docs/wiki/concepts/loop-memory-protocol.md`

## Lessons (read FIRST every run; curated — rewrite/prune as they evolve)

_None yet — the first run seeds this. Promote durable takeaways here (e.g. which topic types
or source kinds the acceptance gate keeps vs discards, recurring thin-coverage areas)._

## Run log (newest first — add each new entry at the TOP; never edit/delete past entries)

_No runs recorded yet._

<!-- Template for each run (newest on top):
### [YYYY-MM-DD HH:MM] <gap or topic>
- Output: <wiki page path(s) + log.md line; never a duplicate of the output>
- Happened: <what the run did — gaps researched, kept/discarded/flagged>
- Worked: <what went well>
- Failed: <what failed the gate / was discarded / rejected>
- Remember: <takeaway; note "→ promoted to Lessons" when durable>
-->
