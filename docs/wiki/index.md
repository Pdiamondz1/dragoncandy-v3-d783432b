# Wiki Index

## Sources

- [[200K Load Run & Header Overflow Session]](raw/sessions/2026-07-26-200k-load-run-and-header-overflow.md) — Slice 2 stopped being *built* and got **run**: migration `20260725140000` applied to prod (both embedded fixtures replayed through the **deployed** function — overlap `honest=naive=400`, stagger `honest=200` vs `naive=400`), a fully-green 2-shard validation (302 MB real egress, 0 media errors), then the **20-shard cap-discovery run** — `honest_peak_concurrency` **4,000 == the naive sum** with `max_concurrent_shards=20`, so GitHub genuinely runs 20 shards at once (cap ≥20); 31,000 requests, **0 breakage / 0 throttled**, 369 MB of real Storage egress, and **prod Postgres at 27/90 connections (~70% idle), 11.40 ms** ⇒ **the DB is not the constraint at the 200K band**. The knee moved to the client (p95 1.9 s → 18.4 s) because the runbook's step-1 knee probe was skipped — recorded honestly, not as a capacity signal. Blocked first by four identical seed failures that looked exactly like a GitHub→Supabase outage and were ours: an unbounded `.in()` overflowing undici's 16 KB header limit ([[Supabase .in() Header Overflow]], PR #345); after the fix 500 bots minted from ONE runner IP (4× the prior max). Teardown to zero, live 25 + the 2,000-profile marketplace intact (2026-07-26)
- [[Account Completeness Engine]](concepts/account-completeness-engine.md) — one derived model for "is this account ready to do X", replacing two half-systems that tracked the same facts different ways (live `deriveReadiness`/`ReadinessGate` + the stored `first_run_missions` blob). Four states where **`unknown` never blocks and never renders as a failure**, so a total API outage yields zero outstanding items and zero blocked actions; `required`/`recommended` tiers with recommended dismissible via a NEW `profiles.dismissed_requirements` (deliberately not the existing `dismissed_coachmarks` — shared opaque string keys would silently cross-dismiss); an action registry so `ReadinessGate` takes `action="apply_campaign"` instead of `require={{stripe:true}}`. Deliberate read split: checklist uses the cheap mirrored Stripe column, the gate pays for the authoritative read, sharing one cache key and therefore one shape. Three defects came from the plan text itself — a sentinel UUID that made a failed org read fabricate `count:0` → "Invite your team" during a loading window, a checklist that could read 5/5 while leaving the user stuck in first-run (only 4 page-visit mission keys stamp `completed_at`), and three tests that could not fail. PR #472, merged + deployed (2026-08-23). **Corrected by slice 2
([[Identity & Address Verification]]):** the "gate is inert because `READINESS_GATE_ENABLED` has no
flag row" reasoning did not extend to the engine itself — `AccountChecklistRows`/`MissionChecklist`
consume it directly with no flag, so slice 1 had in fact shipped two permanently-unmet `required`
rows (`address`, `phone_verified`) visible to every user from day one, now given real writers.
- [[AI Creator Matching Fix Session]](raw/sessions/2026-07-16-fix-ai-creator-matching-location.md) — Hoboken restaurant got "Found 0 potential creators" while 6 Hoboken creators existed; root cause was a silently-swallowed `campaign_matches` INSERT (`match_score` `numeric(3,2)` CHECK 0..1 vs 0–100 scores + a stale `notify_donny_nudge` `NEW.brand_id` branch + a dead `business_address` select), NOT a matching-logic bug; fixed + added distance-based geographic scoring (haversine, the `_shared/geo.ts` port of the tested `src/lib` geo helpers) + "· N mi away"; migration + edge fn deployed to prod, frontend on merge (2026-07-16)
- [[Apple App Store Capacitor Phase 1 Session]](sources/apple-app-store-capacitor-phase1-session.md) — Capacitor iOS foundation: one codebase for web + iPhone, payments split by surface (2026-06-01)
- [[Auth + Onboarding Landing-Theme Retheme Session]](raw/sessions/2026-07-19-auth-onboarding-landing-theme.md) — presentational-only retheme of all 7 entry surfaces (login/sign-up, the 5 auth siblings, onboarding) from dark to the shipped light "Human-driven. AI-assisted." landing identity, softened for forms; new shared `AuthShell` reuses landing `landing-*` tokens/fonts + `Eyebrow`/`LandingButton`; both dark triggers removed + the now-dead `useDarkHtml()` hook deleted (`/internal` is now the only dark surface); Codex + whole-branch review caught an `isolate`-vs-`relative z-10` flex-collapse bug in `AuthShell`, fixed with the landing's own `isolate` pattern; PR #299, merged + live (2026-07-19)
- [[Campaign Delivery, Scheduling & Notifications Session]](sources/campaign-delivery-scheduling-notifications-session.md) — Content-delivery stabilization, notification system, auto cross-scheduling, Donny strategist, revision sync (2026-06-01)
- [[Apply Overload PGRST203 Session]](raw/sessions/2026-07-20-apply-to-campaign-overload.md) — creator PalmDom's "Failed to submit application. Please try again later." (no row created) traced to an **overloaded RPC**: `apply_to_campaign` had a 6-arg + a 7-arg-with-`p_portfolio_url` overload, and **PostgREST resolves by argument NAME**, so any 6-key call matched BOTH → `PGRST203 "Could not choose the best candidate function"` (message lacks `row-level security`/`violates row`, so `useCreateApplication.onError` showed its generic default toast). Proven against prod: DB body works (ran the RPC *as PalmDom* via `set_config('request.jwt.claims',…,true)` in a rolled-back `DO`), but a `curl` **6-key** POST → `PGRST203` while **7-key** resolves — `execute_sql`/`apply_migration` bypass PostgREST so can't repro resolution. Fixed by **dropping the obsolete 6-arg overload** (7-arg's `DEFAULT NULL` covers 6-key callers) + surfacing the real error code + `types.ts` sync. Latent landmine, not proven PalmDom's exact trigger (deployed web sends 7 keys; 24h `api` logs gone). PR #321 (2026-07-20)
- [[Campaign Price Anchoring Session]](raw/sessions/2026-07-19-campaign-price-anchoring.md) — founder feedback that the generated price is too high to start and, being pre-filled, reads as the required price. Relabelling it "suggested" would only move the anchor one line down, so the fix went upstream: `donny-campaign-generate` had **zero** pricing guidance (a bare `"price": <number>`), so the model free-associated to ~$400/deliverable — agency pricing for a first-time local restaurant. Added per-deliverable tier bands (standard $75–150, express $110–225, dragondash $150–300) + `suggested_price_min`/`max`; the price field now starts EMPTY with the range and three one-tap `AppChip` amounts beneath it, Cost Breakdown hidden below the $50 floor, launch gated with a real message (an $800 pre-fill → an empty field suggesting $150–300). Creator half needed no new machinery — the counter-offer system already existed but was **unreachable**, gated on `isInvited`, so organic creators saw only "Looks good — Send". Key gotcha: a crew campaign's `fixed_price` is `0` **not null**, so `isFixedPrice` did not exclude it and dropping the invite gate exposed a "$50 minimum" offer form on DB-declared-free campaigns — caught independently by Codex and `data-exposure-reviewer` (2026-07-19)
- [[Creator Match Auto-Run & Invite Clarity Session]](raw/sessions/2026-08-07-creator-match-autorun-and-invite-clarity.md) — founder screenshot of the campaign detail page with three complaints: matching should run without pressing a button, nobody understands what "Invite" does, and the invite click sits dead for 3-4s. Audit confirmed all three structurally — `match-creators` had **no automatic trigger anywhere** (two buttons only; no creation/publish path and no DB trigger), the invite had **zero explanatory copy on the entire platform**, and the match card had **no pending state at all** while `send-campaign-invitation` ran six serial steps. Shipped: a twice-guarded silent auto-run (ref + `sessionStorage`, deliberately session-scoped so a still-empty campaign re-runs later), `MatchingProgress` stepped UI whose last step never claims "done", split loading flags (they were one, so a campaign that already had matches flashed "Analyzing creators…"), "Invite to apply" + a `campaign_invite` expander, and real invitation status. Two live defects found en route: the panel rendered for **non-owners** (would have auto-fired a doomed call), and an **expired invitation was permanently un-resendable, silently**. Reviews: `data-exposure-reviewer` caught that the revive is the first-ever UPDATE on `campaign_invitations` while the crew trigger is `BEFORE INSERT` only; self-caught that the shared `staggerContainer`/`staggerItem` variants were dead code whose failure mode is **invisible match cards**. `send-campaign-invitation` deployed **v62 → v63**; both-viewport visual pass NOT completed (machine at 100% CPU) (2026-08-07)
- [[Campaign Target Audience Session]](raw/sessions/2026-08-07-campaign-target-audience.md) — founder screenshot of "Logistics & Targeting": the TARGET CREATORS chips need to be "more specific — we have to target audiences… make it very obvious that we want the content to attract that customer type instead of a bunch of options," plus Donny-recommended tags. Audit showed the chips fed **nothing** (read by 3 display components; matching uses the disjoint `creator_profiles.skills` craft enum), the prompt asked for `target_creator_persona` with **no vocabulary** while every sibling field was constrained (so Donny emitted `"Gen Z brunch crowd"` → zero chips lit, still persisted), and the list was duplicated 3× in 3 encodings. Replaced with one Donny-written audience line + 2 swap alternates + 4–6 `campaign_tags`; audience promoted to Campaign Overview and onto every idea card. Reviews changed the code 4× — the edit-page write path bypassed the coercion this change introduced (→ duplicate React keys), the save path contradicted the file's own "read tolerant" rule by truncating legacy prose mid-word, a new ordering test **could never fail** (matched the prose, not the schema), and a cited justification file never writes `ai_analysis`. Codex ran clean on the shipping commit once quota returned (2026-08-07)
- [[Handle-Updated-At Restore Session]](raw/sessions/2026-08-07-handle-updated-at-restore.md) — `public.handle_updated_at()` on prod had a body of literally `-- Function logic here / RETURN NEW;` — it never assigned `NEW.updated_at`, so **35 triggers across 31 tables** fired and changed nothing. Prod drift, not repo state (both repo definitions always said `NEW.updated_at = now()`). Found not by a bug report but by a *contradiction*: a `knowledge-sync` step keyed on `max(updated_at)` called the RAG stale while the row's content was demonstrably new. The one-line `CREATE OR REPLACE` was the easy part — the work was the two consumers that had adapted to the column never moving (`donny-analytics-alerts`, where a frozen-column filter silently means "created in 24h" and would have flipped to "modified in 24h" ⇒ "Payment released" on untouched rows; and DRE `occurred_at`, where false recency clears the forward-only `go_live_at` gate ⇒ retroactive "You earned DC Points" — awards safe via `UNIQUE`, *notifications* not). Adds `campaigns.completed_at` after **withdrawing my own recommendation**: the option the founder approved (bare `created_at`) would also have suppressed legitimate *future* milestones. Codex P2 dissent on the alert windows held-and-annotated rather than complied with; `edge-function-reviewer` corrected an overstatement (the repoint is a pure narrowing, not exact preservation). Ordering was load-bearing; `20260807233100` is `language sql` so Postgres self-enforces half of it. Verified live: trigger fires (rollback-wrapped round-trip), 0 pending DRE events, 0 completed campaigns. PR #385 (2026-08-07)
- [[Code Architecture Audit Remediation]](sources/code-architecture-audit.md) — TypeScript strict mode, type safety, and codebase cleanup (2026-05-04)
- [[Karpathy LLM Wiki Schema]](sources/karpathy-llm-wiki-schema.md) — Operating schema for LLM-maintained knowledge bases; inspiration for this wiki's two-layer architecture (2026-05-24)
- [[Code Architecture Audit Session]](sources/code-architecture-audit-session.md) — Strict mode enablement, 158 unused imports, 34 type errors, Supabase type regeneration (2026-05-04)
- [[Context-Tax Split Session]](raw/sessions/2026-07-19-context-tax-split.md) — split `PROJECT_CONTEXT.md` §5 (68 prose bullets, ~29,950 tok = 65% of everything auto-loaded per session) into a one-line-per-entry index + the non-auto-loaded `docs/SHIPPED_LOG.md`, and amended **both** generators (`knowledge-sync` step 4 + the always-loaded `CLAUDE.md` clause) so it cannot regrow — 176,620 → 73,742 B (−58%), ~440 → ~15 tok per shipped branch; paired #295 reconciled the go-live triage 6 → 2 and scheduled 3 report-only routines. Durable gotchas: a gate counting bullet *headings* proves nothing, normalize CRLF **before** any end-anchored regex (else it false-fails as total data loss), and verify status claims against prod not the doc — which retired 3 stale "pending" items and one already-closed security follow-up. PRs #294 + #295, merged + live (2026-07-19)
- [[Counter-Offer Authorization Session]](raw/sessions/2026-07-20-counter-offer-authz.md) — fixed the open finding on [[Service-Role Data Exposure]]: `create_counter_offer` was a `SECURITY DEFINER` RPC with `anon:EXECUTE` and **zero authorization** — any caller could flip a stranger's application, decline its offers, and forge an offer under any `sender_id`/`sender_role`, then self-accept. One `CREATE OR REPLACE` migration (identical signature — sole caller `useCounterOffers.ts` untouched) adds identity (`auth.uid()=p_sender_id`, before the lock) + participant + role-integrity guards, writes the **server-derived** role (closing the forge-then-self-accept escalation, since the offer now carries `sender_id=auth.uid()` and the UPDATE policy is `sender_id != auth.uid()`), revokes anon/public EXECUTE + explicit-grants authenticated/service_role, pins `sender_role` in the sibling INSERT RLS policy, and pins the `RETURNING` list. Verified live red→green (`set_config('request.jwt.claim.sub',…)` rollback-wrapped; `SET LOCAL ROLE authenticated` for RLS). **Codex P1 "authenticated loses EXECUTE" verified empirically false** (direct default-privilege grant survives the `public` revoke — confirmed via `routine_privileges` + an as-role call); explicit GRANT added anyway for replay-safety; migration `20260720000000` (2026-07-20)
- [[Build-Time Confidentiality]](concepts/build-time-confidentiality.md) — Keeping a figure out of a public bundle: why import.meta.env does not fold when unset, why dead-code elimination leaves it in the sourcemap, and why the assertion needs controls in both directions
- [[Cross-Tenant Proxy Authorization]](concepts/cross-tenant-proxy-authorization.md) — why `outstand-proxy` is not a convenience layer but **the tenant boundary itself**: the Outstand API key is **org-wide**, so every check it declines to make is answered "yes, for everyone." The rule — **a grant may rest only on a fact the client cannot assert** — leaves exactly two for a single post (provider-fetched post accounts ∩ the caller's own; the server-minted `outstand_post_ownership` binding); the request body, the post's platform, and any client-writable table are **not evidence**. Fixed three live pre-existing holes (PR #368): body ids used as a grant (`DELETE /posts/{any_id}` + your own account id deleted **any post in the org**), a platform fallback (one Instagram account authorized every Instagram post), and a list filter that stripped `data` while forwarding an unfiltered `posts` sibling — **observed on prod**: 4 of 5 posts belonged to another tenant, `pagination.total` disclosed the org-wide 49, and the vendor SDK reads the leaking key. Traps: two coupled holes are ONE fix; `.maybeSingle()` returns `{data:null,error}` when >1 row matches, so a guard discarding the error fails open **exactly in the compromised case**; a comment can be a loaded gun (`social-proxy` instructed a future editor to reinstate both bypasses); a cancelled CI job reports as `fail`. **Deployed + prod-verified**: the leaking list returns only the caller's own post (`total` 49→1), a foreign post 403s, and the caller's own post + `/analytics` still 200. A follow-up migration `20260806210000` revokes the client INSERT on `business_outstand_accounts` — the substrate under both surviving grants, where `authenticated`+`anon` held INSERT on `outstand_social_account_id` and the unique index is per-user not global (awaiting the founder-gated apply; no evidence of prior exploitation). Still open: `/media` is unscoped — nothing to filter on, needs its own binding; `count:0` today (2026-08-06)
- [[Honest Analytics]](concepts/honest-analytics.md) — the rule for anything presenting a number as insight: **a claim may not outrun its evidence, and when the evidence is absent say so** rather than rendering something that looks like an answer. Written after the analytics tab shipped three confidently-wrong components — `TopPosts` sorted by RECENCY with no metric read anywhere, `PostingHeatmap` titled "Best Posting Times" with a Low→High **engagement** legend while counting post **volume** (circular: post Tuesdays at 9am, it recommends Tuesdays at 9am — the exact decision Donny exists to inform), and `FollowerChart` titled "Growth" while plotting absolute counts so the tallest bar reads as fastest-growing when it is usually the slowest mover. Meanwhile `content_performance` had accumulated since June with **zero readers in `src/`**. The key insight: a real metric is NOT by itself the fix — computed over one post the number is true and the conclusion still worthless — so claims are gated on `MIN_POSTS_FOR_SIGNAL=3`, always state N, and say how many more posts are needed instead of going blank. Load-bearing details each of which was a bug: interactions EXCLUDE views (delivery ≠ response); SUM across platforms (the key is (post,platform,milestone), so collapsing to post id discards a cross-posted hit's other platforms); gate on what is ON SCREEN not account-wide (a platform filter otherwise flips an all-zero grid into "Best Posting Times"); filter `verified_at IS NOT NULL`, which is what keeps **6 fabricated all-zero rows** off the screen — excluded in code, not deleted from prod (2026-08-06)
- [[Content Delivery System Flows]](sources/content-delivery-system-flows.md) — Complete content delivery lifecycle across all three roles (2026-05-23)
- [[Content State-Machine Drift Repair Session]](raw/sessions/2026-07-23-content-state-machine-drift-repair.md) — the collaboration state machine was recorded-applied but MISSING from prod (phantom drift); restored `transition_content_status`/`content_disputes`/triggers/CHECK; auto-approval dead 3 ways (submitted_at→content_submitted_at anchor, no pg_cron job, missing RPC) — had never once fired; closed a SECURITY DEFINER IDOR; allowed reject-from-submitted; backfilled 15 rows; PR #325 (2026-07-23)
- [[Content Engine Phase B Session]](sources/content-engine-phase-b-session.md) — Brief → DragonShare action shipped + verified (3 slices); deep-link race fix; Phase C next (2026-06-11)
- [[Content Engine Phase C Session]](sources/content-engine-phase-c-session.md) — Return-half link: dragonshare_post_id + first-wins triggers bridge published post → brief → performance; PR #73; spec-anchored (2026-06-11)
- [[Content Engine Phase D Session]](sources/content-engine-phase-d-session.md) — Creator "Your content briefs" card shipped; ownership-gated definer RPC bridges the cross-user RLS gap; verified prod (2026-06-11)
- [[Core Docs Recent Updates Sync Session]](sources/core-docs-recent-updates-sync-session.md) — Post–June-2 sync: DragonShare notifications, iOS camera, legal pages, Outstand recovery, QA Plan C, scale → 73 functions (2026-06-07)
- [[Google Workspace Connections Session]](sources/google-workspace-connections-session.md) — AIOS Connections pillar: per-user OAuth, Drive hub, ops-deck restyle, Donny export, Gmail compose deep-link, metrics Sheet, Chat scaffold; 6 PRs (2026-06-13)
- [[Wiki-Commit-PR Session]](raw/sessions/2026-06-18-wiki-commit-pr.md) — admin-gated "Open wiki PR" button: applied strategy-doc correction → GitHub PR back to docs/wiki/ so it survives the next donny-knowledge-sync; PR-only, idempotent, GITHUB_WIKI_TOKEN gate (2026-06-18)
- [[Donny Answer to Wiki Session]](raw/sessions/2026-06-18-donny-answer-to-wiki.md) — admin-gated "Save to knowledge" button: a fresh internal Donny answer → GitHub PR creating a NEW docs/wiki/<concepts|analyses> page → Donny RAG on merge; sibling of wiki-commit-pr with a stricter client-input guard, PR-only (2026-06-18)
- [[AIOS Ingest-Secret Rotation Session]](raw/sessions/2026-06-18-aios-ingest-secret-rotation.md) — new Supabase sb_secret key rotated prod's service-role credential → AIOS 3am routines + content cron 401'd for a week; fixed with a shared ingest-auth gate accepting injected key OR a stable AIOS_INGEST_SECRET; PR #129, deployed + verified (2026-06-18)
- [[Counter-Offer Enum Fix Session]](sources/counter-offer-enum-fix-session.md) — Postgres enum cast bug, PL/pgSQL variable typing, missing campaign_status value (2026-05-21)
- [[Dark-Luxe App Theme Session]](raw/sessions/2026-07-17-dark-luxe-app-theme-slice1.md) — forced the app to a single dark "Dark Luxe" theme matching the landing (Slice 1 of a phased rollout): `forcedTheme="dark"` + `<html class="dark">` + retuned `.dark` tokens + shared dark primitives (`.dc-surface`/`.dc-panel`/`.dc-field`, `dc-teal-pill`/`dc-ghost-pill`, `GlowBackdrop`/`Eyebrow`), then converted the hardcoded light literals across auth/onboarding/chrome/Donny-interior/dashboards/first-run; the two-color-system insight + the dark-fill-as-text contrast trap; teal/pink kept; Opus + Codex clean; PR #269, merged + deployed (2026-07-17). **Superseded same-day by the light-app pivot — see [[App Theme Pivot Session]].**
- [[App Theme Pivot Session]](raw/sessions/2026-07-17-app-light-marketing-dark-pivot.md) — reverted the force-dark app (#269) to **LIGHT** after founder feedback (too dark / unreadable / white patches); dark kept only for landing + login/sign-up + onboarding + `/internal`, via a new **`useDarkHtml()`** hook that adds `dark` to `<html>` per-route; `ThemeProvider` `defaultTheme="light"` (not forced — Codex caught `forcedTheme` breaks `/internal`); fixed a **washed-auth** regression (scoped-div `.dark` leaves `<body>` white → glows wash → `useDarkHtml` darkens `<body>`); PRs #275/#277, deployed (2026-07-17)
- [[Database Schema]](sources/database-schema.md) — Supabase Postgres schema overview (2026-05-23)
- [[Data-Exposure Reviewer Session]](raw/sessions/2026-07-19-data-exposure-reviewer.md) — asked to port Harbormill AIOS's subagents; **the premise did not hold** — Harbormill has **zero** custom subagents (its agent layer is skills, and DC is *ahead* on 2 of 4: Loop Scout beats `loop-audit`, and `validator-forge` lacks DC's `{done,checklist,missing}` contract) — so the work became "is *one* new subagent justified?". Yes: shipped the read-only `data-exposure-reviewer` (`model: opus`, `Read/Grep/Glob` only, **no MCP tools**), resolving the [[Claude Subagents Audit]] Tier-2 deferral by drawing the boundary it asked for. Trigger evidence: `edge-function-reviewer` **PASSED** a service-role IDOR + client-controlled `org_id` (PR #260) because its RLS clause is one line under a bundling-led checklist. Validated by re-staging 3 historical defects as replay worktrees — the Crews one reached `send-campaign-publish-notifications` **unprompted by grep**. Whole-branch review then caught that the entry gate could gate out the flagship check (a scope-column-only migration has no policy/definer) — **a suite can appear to cover a capability while exercising it only via an unrelated precondition**. Its first real runs found 6 unfixed exposures on `origin/main` (5 controller-verified), filed for a separate branch; PR pending (2026-07-19)
- [[Delivery Tier Timing Merge Session]](raw/sessions/2026-07-19-delivery-tier-timing-merge.md) — founder screenshot: the campaign builder asked for delivery speed **twice** (a "When do you need this posted?" picker + a separate Delivery Tier badge). They were not just adjacent but **fully decoupled** — the picker wrote only `deadline` (its tier subtitles were cosmetic strings that set nothing), the badge wrote only `delivery_type` (the field carrying the fee, the SLA timer and the Stripe line item) — so "This Weekend" + Standard tier was reachable. Replaced both with `DeliveryScheduleSelector`, which emits **one patch** containing both fields, making the inconsistent state unrepresentable; custom date kept but re-derives the tier. Codex ran **3 rounds, every finding real and each caused by the previous fix**: SLA-mismatched offsets → an inclusive-vs-exclusive cost split that would quote $500 and charge $575 → DragonDash's (correct) same-day date being rejected by a launch validator that parsed date-only strings as UTC midnight AND demanded strictly-future, **making DragonDash unlaunchable** → a free crew campaign printing "Total $75.00". Two pre-existing money bugs fixed (stale `delivery_fee` on edit-save; duplicated cost math extracted to `computeCampaignCost`). 1051 tests; PR pending (2026-07-19)
- [[Donny Audit Phase 1 Session]](sources/donny-audit-phase1-session.md) — Role-based tool filtering, prompt injection defense, tier-based token clamping (2026-05-06)
- [[Donny Audit Phase 2 Session]](sources/donny-audit-phase2-session.md) — Quota enforcement, SSE streaming, dual auth, upgrade CTAs (2026-05-06)
- [[Donny Campaign Creativity Session]](raw/sessions/2026-07-16-donny-campaign-creativity.md) — freed Donny's over-constrained campaign prompt (the real fix; the cost auto-downgrade never fired — always Sonnet, 0.3% of budget): soft platform preference, `creative_concept` + one wildcard, robust parser in a pure `lib.ts`; chat unified to 3 concepts; premium tier @8192 with a Sonnet floor (never Haiku@512); frontend crash-proofing; shipped on Sonnet@8192 because Opus 4.8 prod-key access was unverifiable (Opus a 1-line toggle); PR #243 (2026-07-16)
- [[Donny Campaign Pre-fill Session]](raw/sessions/2026-06-18-donny-campaign-prefill.md) — Donny chat hands a brief to the Create-a-Campaign builder via ?brief= → opens pre-filled on the Launchpad; new prepare_campaign tool; broken /campaigns/new route fixed; PR #124 (2026-06-18)
- [[Donny Chat Input & Timestamps Session]](sources/donny-chat-input-timestamps-session.md) — expanding `<textarea>` prompt (was single-line, scrolled text off-screen) + per-message timestamps & teal date dividers; shared across consumer + internal Donny; light-vs-dark forced time-inside-bubble; Codex caught a tool-row day-grouping bug; PR #140 (2026-06-20)
- [[Donny Chat Matcher Fix Session]](raw/sessions/2026-07-16-donny-chat-matcher-fix.md) — sibling of the campaign-matcher fix on Donny's conversational `match_creators` tool: same over-narrow-filter bug (required `niche` `ilike` on `bio`, `location` `ilike` on the freeform field, ANDed → "Found 0" over a non-empty pool); rewritten to fetch broad → soft score (pure `creator-discovery.ts` reusing `_shared/geo.ts`) → rank (location 0.4 + niche 0.4 + rating 0.2) → top 10, never excludes; Codex P1 = service-role RLS bypass needed a `profile_visibility='public'` filter; `donny-chat` deployed (2026-07-16)
- [[Donny Data Visibility & Quick-Actions Session]](raw/sessions/2026-07-16-donny-data-visibility-quick-actions.md) — Uncle Rocco's Donny 404'd on "Invite Creators" (LLM-invented route → `navigate()` unvalidated → catch-all 404) and reported "no campaigns/DragonShare — data sync issue"; the real cause was schema drift swallowed to `[]` (`campaigns.platform` doesn't exist — it's `platforms[]`; the whole DragonShare agent queried dead columns/enums); fixed both donny-orchestrator + donny-chat: `isKnownRoute` allow-list (server drop + client guard) + role-aware, schema-correct, error-surfacing queries + an ownership gate closing a service-role IDOR + server-side-only org_id; edge fns deployed v63/v145; PR #260 (2026-07-16)
- [[Donny Desktop Overlay Session]](raw/sessions/2026-07-16-donny-desktop-overlay.md) — desktop Donny panel squished every page (docked `flex-shrink-0` sibling stole 320–420px from `flex-1` main; viewport-keyed grids crushed cards); one-className fix makes it a `fixed inset-y-0 right-0 z-40` overlay so content keeps full width; safe via the PageTransition opacity-only contract; PR #236 (2026-07-16)
- [[Campaign Generate Async Jobs Session]](raw/sessions/2026-07-14-campaign-generate-async-jobs.md) — the durable fix for mobile-dropped generations: donny-campaign-generate returns {job_id} in <1s, finishes via EdgeRuntime.waitUntil into a campaign_generation_jobs row, client polls its own row (blip-tolerant, 2.5s × 3min); streaming rejected on PR #151 evidence; session-JWT-only gate; surfaced the pre-existing donny-chat generate_campaign service-role 401; PR #232 (2026-07-14)
- [[Donny-First Dashboard & Route Blind-Spot Session]](raw/sessions/2026-08-09-donny-first-dashboard-and-route-blind-spot.md) — two efforts on one thread. (1) PR #409: `/settings/billing` + `/settings/social` hardcoded in **12 places across 10 files** with **no top-level `/settings/*` route** — all 404, including the "Upgrade" CTA gating the paid Weekly Content Plan and the primary "Connect Outstand" nudge button. `isKnownRoute` never caught them because it only guards routes the LLM **invents**; hardcoded paths never reach it. Found and correctly diagnosed **2026-06-07** and deferred as "out of scope" — broken for two months. Three role vocabularies (`profiles.role` vs `parties[].role` persisted as `party_role` vs others) meant folding `brand` into the business branch **silently redirects** rather than 404s; the first fix copied an adjacent ternary that was itself broken, caught by Codex + `edge-function-reviewer` independently. (2) PR #410: the business dashboard body becomes Donny (greeting + attention list + prompt box + three taps) behind a default-off flag, with today's body preserved verbatim at `/dashboard/business/overview`. Scope set by a prod audit: only **4** Donny tools verifiably work, `social_*` is **0/7** and falsely blames the user's connection, `donny_tool_executions` has **zero** rows for every consumer sub-agent. Four defects found in review — all four in code written into the *plan* — capped by a Codex P2: `campaigns.deadline` is a Postgres `date`, so `new Date()` parsed it at **UTC midnight** and `'due today'` was unreachable all day in every timezone (2026-08-09)
- [[Donny-First Creator Dashboard Session]](raw/sessions/2026-08-10-donny-first-creator-dashboard.md) — Phase 3: the creator role gets the Donny-first body, with the old body preserved verbatim at a new `/dashboard/creator/overview`. The shared pieces became genuinely role-generic (`useDonnyHomeConversation`, `DonnyHomeShell`, and a `/simplify`-extracted `useDonnyHomeInteractions` both containers had been duplicating byte-for-byte), while the two builders were deliberately kept as **siblings** — the roles rank by different rules. **Two taps, not three:** `billing_agent` reads `organizations` and would hand a creator the **restaurant** subscription catalog (a live defect this phase routes around), and **no** agent can answer "find work" at all. **`donny_tool_executions` can never confirm any sub-agent tap, for any role** — its insert sits inside the `isSocialTool && mcpBridge` branch, so the emptiness that has been read as evidence is an instrument that was never wired up. The defect that mattered: **one number answering two questions** — a lifetime `collaborationCount` gated "nothing in flight", and 11 of 16 prod collaborations are `completed`, so a creator who *finished* their work counted as busy and an onboarded one rendered a **blank** attention region; writing the test then exposed a second half the review had not named (the money-first merge branch **omitted the find-work item entirely**). Codex added two more: `cancelled` collaborations counted as earnings (setup ranked above value), and `DCTour` **spotlighting a zero-height wrapper** — a 16px hole with an arrow pointing at blank page — fixed at the mechanism for all three roles. A third Codex P1 (financial columns unreadable by `authenticated`) was **refuted by prod impersonation**. Payout copy is worded true-in-both-worlds because `stripe_onboarding_complete` now has **two disagreeing readers** (2026-08-10)
- [[Donny Dashboard Inline-Chat Session]](raw/sessions/2026-08-09-donny-dashboard-inline-chat.md) — the founder's acceptance test of the repaired `social_*` tools produced the **first `status='success'` row in `donny_tool_executions`' history** for any `social_*` tool (`social_get_post_analytics`, 23:23:57), with [[Honest Analytics]]' sample-size gate visibly holding under a real user — and exposed two problems of different kinds. The unreadable "drawing or chart" was a markdown table arriving as literal pipes: `DonnyMessage` ran ReactMarkdown with **no `remark-gfm`**, and tables are GFM, not base CommonMark — the tell was the `**bold**` in the same message rendering fine. The panel opening was **not a bug**: Phase A's prompt box was a launcher by design. Phase B's real blocker was not on the design doc's seven-hazard list — `useDonny` gates queries on `stage !== 'closed'`, so an inline thread with the panel shut renders a **permanently empty box**, indistinguishable from "no messages yet". Fixed by separating what `stage` conflated (**panel visible** vs **conversation live**) via a ref-counted `registerInlineConversation()`, leaving `stage` byte-unchanged so four hazards dissolve. Then **four Codex rounds found four more defects, all one shape — the code claimed more than it delivered**: `package-lock.json` still had `remark-gfm` as a dev dep, a failed first ask rendered nothing at all, the fix for that offered a **dead "Try Again"** (`lastUserMessage` is assigned *after* the throw `retry()` guards on), and a follow-up typed mid-answer **vanished** (silent `return` + an input that cleared unconditionally) (2026-08-09)
- [[.com Phase 1 & esm.sh Bundler Outage Session]](raw/sessions/2026-08-09-dotcom-phase1-and-esm-sh-bundler-outage.md) — three efforts on one thread: **Phase 1 of the `.io` → `.com` migration** (PR #414 — `.com` was already publicly serving while CORS-blocked on all 82 edge functions, apex failing TLS on GoDaddy parking IPs, and absent from GoTrue's allow-list); **an outage I caused and had to diagnose** — Supabase's server-side bundler can no longer build a bootable worker from `esm.sh/@supabase/supabase-js`, found only by comparing a broken function against a working one after four wrong hypotheses, then isolated with a 7-version single-variable probe and fixed across 121 files (PR #415); and the **82-function redeploy in 8 canary-verified batches, money last**, closing the Phase 1 gate on both viewports (real 390px via browser-use CDP, after `resize_window` silently lied). Also deletes the orphaned `verify-recaptcha` (live 4 months after its source was removed) and repairs a **silent revert** — a parallel session's `donny-orchestrator` fix, overwritten by a fleet deploy pinned to a pre-merge commit, caught by the knowledge-sync scope check (2026-08-09)
- [[Donny Social Tools Repair Session]](raw/sessions/2026-08-09-donny-social-tools-repair.md) — Donny told the founder he had "no visibility into which Instagram account is connected", sent him to find an **"account ID"** on a page that displays none, and promised to post once he had it — while `social_*` had never once succeeded (7 calls, 7 errors, 0 successes). The prod audit overturned **two standing project claims**: instrumentation was NOT missing (`donny_tool_executions` held 158 rows and had already caught this), and the cause was NOT the fabricated `account_id` (the bridge sent the **service-role key** where `outstand-proxy` runs `auth.getUser()` on the anon client → no user → 401 before any account logic; plus a `{action}` body against a **path**-routed proxy, and a header nothing reads). Repair: 7 tools → **4** (three had no backing operation); `account_id` deleted from every schema and resolved server-side (`status='active'`, disambiguation by **handle + platform, never ids**); `create_post`/`schedule_post` return a **draft card** the owner taps to publish, so the LLM structurally cannot publish; numbers gated at `MIN_POSTS_FOR_SIGNAL=3`. Two plan-authored measurement bugs caught in review: cumulative milestone rows **summed** (~3× inflation, proven against prod post `XDbxe`) and both `content_performance` reads **ungated on `verified_at`**, which would have let 6 fabricated all-zero rows clear the sample-size bar (2026-08-09)
- [[Donny Mobile Quick-Action Navigate Session]](raw/sessions/2026-07-14-donny-mobile-quick-action-navigate.md) — "prompts not clickable": navigate quick-actions changed the route BEHIND the fullscreen mobile chat sheet (now closes first), and the ?brief= generation's ~1-min non-streaming fetch dropped on mobile (human retry copy + one-tap-retry brief seed); donny_cost_ledger proved the server finished while the client fetch died (2026-07-14)
- [[Donny Orchestrator Find-Creators Session]](raw/sessions/2026-07-16-donny-orchestrator-find-creators.md) — the CONSUMER web/mobile Donny chat calls donny-orchestrator (sub-agent router), NOT donny-chat (internal) — so the donny-chat match_creators fix never reached the surface businesses test; found via a network capture. Real fix: a find_creators sub-agent on donny-orchestrator (public+completed query → shared rankCreators → text list + View buttons), tool_choice-forced on discovery intent (excludes campaign mentions). Deployed v61, LIVE-verified returning real Hoboken creators + distances (2026-07-16)
- [[Donny Tray Close UX Session]](raw/sessions/2026-07-16-donny-tray-close-ux.md) — first-open Donny tray had no ✕ (users trapped until they sent a message + Donny replied); the panel is a 3-stage machine (closed→tray→chat) whose tray and chat rendered two *different* headers — unified into one shared teal `DonnyPanelHeader` (tray: ⌃ expand + ✕ close; chat: ⌄ minimize + ✕ close), deleted `DonnyChatHeader`; + desktop click-outside (`useIsMobile`-gated, `[data-donny-launcher]`-excluded), inviting "🎉 You're all caught up" empty state, de-grayed brand chips; rebased onto main's fixed-overlay panel (#236); live-verified on prod; PR #258 (2026-07-16)
- [[Help Center Screenshots & Search Session]](raw/sessions/2026-07-19-help-center-screenshots-and-search.md) — two frontend/content help efforts. (PR #306) 7 new feature screenshots + a landing refresh embedded into help articles via the public `help-screenshots` bucket — Donny strips HTML so images never reach it; CLI upload gotchas (relative src + `--workdir`, PowerShell not Git Bash; cp won't overwrite → additive + repoint). (PR #310) a **Help** item in the desktop sidebar (`navConfig`, all 3 roles) + an improved `/help` search: pure client-side ranked `rankHelpArticles` (title>terms>body, AND tokens, partial match, HTML-stripped body), `?q=` IS the state (deep-link + back/forward-safe), article-page search box — client-side over `search_vector` for the ~32-article corpus; both prod-verified (2026-07-19)
- [[Identity & Verification Session]](raw/sessions/2026-08-23-identity-verification.md) — slice 2 of 4 of the onboarding redesign: gives `phone_verified`/`identity_verified`/`address` real writers, and closes several PII holes found along the way. 16 new verification columns across `creator_profiles`/`business_profiles`/`org_units`, all server-write-only via a per-table `BEFORE INSERT OR UPDATE` trigger (grant-based lockdown rejected for these three — at least one write path per table is a runtime-computed object, so an explicit grant list is a silent-42501 trap). A stamp outlived the fact it attested to **four separate times** in one slice — phone, address, identity-on-Stripe-disconnect, identity-on-Stripe-revocation — closed by three different mechanisms (two DB triggers with a dual `IS DISTINCT FROM` condition so a same-statement composite write survives; an application-level clear on `disconnect-stripe-account`; a derivation reorder so revocation outranks a historical stamp). Found and closed a **4th recorded instance** of a column-level `REVOKE SELECT (email)` being a no-op against `profiles`' table-wide GRANT — closed by revoking table-wide SELECT and granting back 15 columns, minus `email`/`phone`. Found and left OUT of scope: a live unauthenticated IDOR, `get_user_conversations`, verified with controls (an unrelated uuid → 0 rows; anon with no JWT → 13 rows belonging to others). Three defects originated in the plan/spec text itself and were reproduced faithfully by implementers, including `company.verification.status`, a Stripe field that does not exist (fixed by deriving company identity from `payouts_enabled && !disabled_reason` instead). The controller's own Ruling 9 was wrong — reasoned the required `identity_verified` tier was inert because `ReadinessGate`'s flag row is absent, missing that `AccountChecklistRows`/`MissionChecklist` consume the engine directly with no flag at all, which would have shipped a permanent unclearable checklist row to all 36 accounts; caught only by the final whole-branch review. A process incident: a reviewer ran `git stash` on files a different agent was actively editing in the same worktree, destroying in-flight work that had to be redone. `verify-phone`/`verify-address` both 503 by design — three required secrets are unprovisioned, so nothing is verifiable end-to-end yet. **The file carries an ADDENDUM covering the review loop that ran after it was first written** — 8 further commits, 14 findings (2 Codex P1s, 3 from an internal re-review, then 9 across seven Codex rounds, clean at round 7). Corrects the body above: the stamp/fact rule hit **five** times not four; the `verify-phone` throttle is now the atomic SQL RPC `reserve_phone_verification_send` and `exceedsSendLimit`/`withinCooldown` are deleted; `verify-address` takes no address from the caller at all and instead compare-and-sets the stored row against itself; and 11 migrations, not 9 (2026-08-23)
- [[Instagram Connector Deploy Session]](raw/sessions/2026-08-24-instagram-connector-live.md) — PR #489 merged (`db736dc8`), migration `20260825120000` applied and all seven edge functions deployed and boot-verified on prod. Merging `main` exposed a **duplicate migration version** — `feat/verify-address-throttle` had shipped `20260825100000` while this branch was open, and the ledger is keyed on the version alone, so the second file to apply is refused as already recorded and forcing past that manufactures `recorded != actual`; renumbered, and closed durably by `supabase/migrations.test.ts`, which found **seven** pre-existing collisions (frozen, not fixed — none is in prod's ledger). The PR's standing Lighthouse failure was **variance, not the branch** (desktop perf 0.73 vs the 0.90 gate; every other PR that day passed, config byte-identical, and a re-run with no landing file touched came back green). Codex clean; three `edge-function-reviewer` passes returned only deploy-ordering findings, no code defects. **Two shell traps made "it ran fine" false twice** — the commands ran in the main checkout where the branch's files do not exist (and where `config.toml` would have deployed the three anonymous functions with `verify_jwt=true`, so Meta's callbacks would 401 before the signature check), then `! cd X && cmd` in plain zsh parsed as `(! cd X) && cmd`, inverting the `cd`'s success and short-circuiting the rest. Both caught by probing prod rather than believing the report. Deliberately not claimed: the two Meta callbacks answer `503 not_configured` without `INSTAGRAM_APP_SECRET`, so the forgery-rejection path is proven by unit tests and **not** by a live probe (2026-08-24)
- [[Internal Donny Profile-Read Fix Session]](raw/sessions/2026-06-27-internal-donny-profile-read.md) — read-side sequel to PR #180: donny-chat loaded the caller's profiles row with .single()+throw "Profile not found", blocking Internal Donny for internal-only users; pure resolveDonnyProfile() synthesizes a minimal profile for them (consumer unchanged); deployed donny-chat v134 via the Supabase CLI (too large for MCP re-paste); PR #185 (2026-06-27)
- [[Internal-Only User FKs Session]](raw/sessions/2026-06-26-internal-only-user-fks.md) — Adrian (first internal-only AIOS user, no profiles row) blocked at Google connect + Internal Donny by FKs to profiles(id); repointed 3 AIOS FKs to auth.users + added describeError so the opaque "internal error" (PostgrestError≠Error) surfaces the real DB message+code; PR #180 (2026-06-26)
- [[Investor Pitch Deck & Cost Model Session]](sources/investor-pitch-deck-cost-model-session.md) — /pitch deck (15 slides) + sourced ~$3M capital-raise cost model; brands woven in; Donny super-agent Vision slide (2026-06-17)
- [[iOS First Physical-Device Build Session]](raw/sessions/2026-08-14-ios-first-physical-device-build.md) — DragonCandy ran on real Apple hardware for the first time, on the founder's new M5 MacBook Pro with the Organization enrollment approved. **The spec's biggest open risk was a non-issue:** Xcode 26.6 compiled iOS 13.0 on Capacitor 6 with no error — read off `--deployment-target 13.0` in the compiler invocation, not inferred from a green build — so no target bump and no Capacitor 7. **The one real UI defect was structurally invisible to every existing check:** `viewport-fit=cover` puts the layout viewport under the notch, `src/` used `safe-area-inset-top` once against eight `-bottom`, and the landing logo rendered on top of the status-bar clock — but mobile Safari's URL bar occupies that space, so no web test, responsive mode or both-viewport `verify-prod` could ever surface it. Fixed on 5 real chrome components, deliberately NOT on 9 in-page `sticky top-0` headers (which stick inside a scroll container, where an inset makes a mid-page gap). **Falsified a standing project claim:** the `capacitor://localhost` CORS widening did NOT reach the whole fleet — 13 functions, almost exactly the money surface, still answer `https://dragoncandy.io` to a native origin, proven with a `.com` control that distinguishes "not allow-listed" from "dead endpoint". Also: the committed `Podfile` was missing both plugin pods, so `pod install` without `cap sync` builds an app with no camera and no share sheet; Node 26 defines `localStorage` on `globalThis` and shadows jsdom's, breaking 50 tests that CI (Node 24) passes; and `codex`, the mandatory reviewer, was simply absent on the new machine. On-device: boot, login and Donny PASS; safe area found broken, fixed, founder-confirmed. See [[iOS TestFlight First Build]] (2026-08-14)
- [[iOS TestFlight First Build Session]](raw/sessions/2026-08-09-ios-testflight-first-build.md) — a signed build to the founder's physical iPhone via TestFlight, verifying the camera + share-sheet features that had never once run on iOS hardware. Repointed `window.location.origin` (`capacitor://localhost` inside the shell) at 21 sites across 14 files via a new `publicOrigin()` seam, sorted into repoint / leave-alone / gate-the-consumer-instead (the Outstand OAuth callback has no route back into the shell, so `ConnectAccountButtonGroupGated` says so rather than repointing); trusted the native origin in the edge-function CORS allow-list (inert until a canaried redeploy); moved the bundle ID `io.dragoncandy.app` → `com.dragoncandy.app` before any App Store Connect record existed (7 docs corrected, not 5 as first counted); added the export-compliance plist key; re-confirmed the purchase-CTA gate still closed after 233 commits. **The process record is the more durable part**: five defects found in the plan itself, not the code — a wrong grep count, a `never`-typed test fixture that couldn't typecheck under strict mode, a `supabase functions download` step that reverted committed work and zeroed a live file, a hardcoded dead route reintroducing an already-fixed 404 bug, and a gate's expected-hit-count contradicting the plan's own later design decision — plus a `deno install` run that silently corrupted `node_modules` (vitest 4.1.2→4.1.10) for four tasks. See [[iOS TestFlight First Build]] (2026-08-09)
- [[Dragon Feed Mobile & Zip Search Session]](raw/sessions/2026-07-16-dragonfeed-mobile-feed-zip-search.md) — mobile Instagram-style single-column feed (`useIsMobile` JS branch, desktop grid unchanged) + a zip+radius search reusing the geo stack (pure `filterMediaByRadius` + a thin `useFeedLocationFilter`); frontend-only; Codex-clean after 2 P2 lazy-geocoding fixes (don't filter mid-geocode; skip geocoding under "Any") (2026-07-16)
- [[DragonFeed Backdrop Adapter Session]](raw/sessions/2026-07-17-dragonfeed-backdrop-adapter.md) — closes the [[Landing Cinematic Video Redesign]]'s "future DragonFeed adapter" prediction: new anon `landing-clips` edge fn returns public URLs of boosted+verified+unflagged DragonShare video (paid boost = curation gate, since trust-then-flag alone is too risky for anon top-of-funnel exposure); the seam merges dynamic-leads-static-backfills over the static playlist; `HeroSection` remounts `RotatingBackdrop` on a clip-content signature (index-based rotation needs it); `RotatingBackdrop` now advances on `onError` too so an undecodable/404 clip can't freeze the hero; whole-branch review found 5 eligible boosted rows already live in prod (not latent); Opus + edge-function-reviewer PASS + Codex clean; PR #268 merged + live (2026-07-17)
- [[DragonFeed Creator Search Session]](raw/sessions/2026-07-16-dragonfeed-creator-search.md) — second Dragon Feed iteration: the search box drives two modes — empty → browse media feed; name and/or ZIP-or-city → an Instagram-style vertical creator list (avatar + bold-matched name + location·rating·posts + skill chips, tap → profile); name match global, location narrows by radius; new pure `feedCreators.ts` (12 tests) + controlled `useFeedCreatorSearch`; deletes PR #242's `useFeedLocationFilter` + `filterMediaByRadius` (a zip is now a search trigger); 804/804, Codex-clean (2026-07-16)
- [[DragonFeed Uplift & Nav Active-State Session]](raw/sessions/2026-08-07-dragonfeed-uplift-and-nav-active.md) — two founder reports in one PR (#384). The sidebar "double-clicked button" was a **specificity** bug — each role's bare-root Dashboard href prefixed all ~26 child routes — fixed with one shared longest-match-wins `activeNavHref()` replacing the same expression copy-pasted into three navs. The feed's four complaints shared one root cause (*an item is not a row*); the `feed_items` table designed to fix it was **cut** after verification showed uuid ids would silently empty the Inspiration page + dashboard strip (both parse the composite `content_id` back apart), the mirror had no lifecycle contract, and 34/34 items already carried a `storage.objects.created_at`. Shipped instead: real dates + stable order, NEW badges, skill chips, duration badges, desktop attribution, honest gated view counts. Also: the 26 hidden items behind a default-off opt-in nobody could find. **Deliberately not done** — flipping the profile-form default (a whole-form blur write would have retroactively flipped stored consent) and the DragonShare merge (no public SELECT policy, no consent flag anywhere). Codex clean; no migration (2026-08-07)
- [[DragonShare Amplification Engine Session]](sources/dragonshare-amplification-engine-session.md) — Upload-first submit, trust-then-flag, watermark, two-path boost payment, 80/20 split (2026-06-01)
- [[Landing Backdrop HEVC .MOV Fix Session]](raw/sessions/2026-07-17-landing-backdrop-mov-fix.md) — founder report "creator side shows one looped video": the leading boosted DragonShare clip on prod was a real HEVC (H.265, `hvc1`) 1920×1080 `.MOV` Chrome/Firefox can't decode, silently rendering black without firing `error` (PR #268's onError-advance fix didn't cover it); 3 fixes — dynamic clips now TRAIL the curated static clips (not lead), `.mov`/`.MOV` dropped from eligibility (mp4/webm only), and a 15s max-dwell watchdog on `RotatingBackdrop` so a clip that neither ends nor errors force-advances; reverses two PR #268 decisions on concrete evidence; edge-function-reviewer PASS + Codex clean; PR #273, merged + live (2026-07-17)
- [[Landing Redesign & Lead Capture Session]](raw/sessions/2026-06-28-landing-redesign-lead-capture.md) — `/frontend-design` rebuild of the public landing into Dark Luxe Editorial (scoped `.dark` wrapper, Reveal/MediaSlot/VideoSlot, broadened restaurant→business, flag-gated rewards) + a full public lead pipeline (closed-anon-DML `leads` table + `capture-lead` edge fn w/ honeypot + fail-open IP throttle + Resend); Codex-clean after 2 P2s; backend deployed + curl-verified on prod; also installed the Supabase CLI (2026-06-28)
- [[Landing Cinematic Single-CTA Redesign Session]](raw/sessions/2026-08-22-landing-cinematic-single-cta.md) — the public landing collapses from six scrollable sections + a contact form to **one screen**: fixed logo header, full-bleed video hero (ten real ABB + Uncle Rocco reels, no user uploads), single "Get started" CTA that reuses `AuthPage`'s existing role-selection step instead of asking role on the landing; ~20 dead files deleted. Two encodes per reel chosen by viewport orientation, crop windows hand-picked by watching each clip; every reel capped at 12s, and the cap's own justification had a latent bug — the stall watchdog armed on layer-active (~3s margin against slow-connection buffering) rather than on playback actually starting, caught by Codex and fixed by arming on both and resetting on `playing`. The `LANDING_VIDEO_BACKDROP_ENABLED` flag is deleted, not defaulted off, because with video as the whole page an "off" state is an outage, not a kill switch. A whole-branch review (not per-task) caught a scroll bug from three stacked height rules, a white cold-load flash on two hard-coded-white surfaces, and a spec-vs-plan disagreement that was really about two different elements. Was blocked on written ABB/Uncle Rocco footage permission and **merged 2026-08-23** as #459 once the founder confirmed it; the brightest-frame contrast check (spec §7) passed at every clip's brightest frame (worst case white 6.24:1 against a 3.0:1 requirement). See [[Landing Cinematic Single-CTA Redesign]] (2026-08-22)
- [[Adrian Feedback: Body Scroller & How-It-Works Session]](raw/sessions/2026-08-23-adrian-feedback-body-scroller-and-how-it-works.md) — three notes on the shipped landing from Adrian Vella, one of which was a real bug **I had already declared not a bug**. "The screen jumps if I scroll up or down" on mobile: `body` (not `<html>`, not `<main>`) is the document's scroll container because `body{height:100%;overflow-x:hidden}` computes `overflow-y` to `auto`, and `AppShell`'s `h-screen` = `100vh` = the URL-bar-collapsed height on iOS Safari, so the shell overhung body by ~60–90px, body scrolled by exactly that, and scrolling collapsed the bar → grew `100dvh` → resized the page mid-gesture. The earlier refutation failed twice over — it measured `main` (not the scroller) in an emulator (no URL bar, so `100vh === 100dvh` and the gap is structurally zero) — and the durable lesson is **when a probe returns zero, prove the probe could have returned non-zero**. Fixed at `AppShell` with `DashboardLayout` tracking it, pinned by a text assertion since jsdom has no layout engine. Also: an underlined "Log in" under the CTA in `#B8ECDA` (the *pale* mint — small text needs 4.5:1 and the slogan's `#7BE3C0` measures 3.91 at p90 there against 4.62), and a "Learn more" pill pointing at a new `/how-it-works`, built because the redesign had deleted the only page that explained the product. Codex clean; the iOS half is unverifiable in any browser or simulator (2026-08-23)
- [[Landing Cinematic Video Redesign Session]](raw/sessions/2026-07-16-landing-cinematic-video-redesign.md) — evolved the Dark-Luxe landing into a cinematic 6-section page: morphing per-role hero (guarded `?role=`), swappable `landingClips` clip-source seam (empty v1 → ship-before-clips → Cloudflare Stream → future DragonFeed), `VideoSlot variant="backdrop"`, honest Proof band, "Donny" naming, transparent scroll-aware header; subagent-driven (11 tasks), Opus whole-branch + Codex clean, browser-verified; gotchas: Tailwind `relative` beats `absolute` (later-defined wins), size a tall logo by height, transparent header needs scroll-fade (2026-07-16)
- [[Landing "Human-driven. AI-assisted." Redesign Session]](raw/sessions/2026-07-18-landing-joe-redesign.md) — founder-provided mockup ("Joe's design") reframes the landing's positioning to "Human-driven. AI-assisted."; new light landing (no `.dark` wrapper), additive `landing-*` tokens + Bricolage/Instrument/Silkscreen fonts, static two-door hero replaces the role-morphing one, `AudienceLanes`/`ProofSection`/`StartFreeSection` deleted, the cinematic-video system preserved but gated off by default (`LANDING_VIDEO_BACKDROP_ENABLED`, new single-key light-scrim `HeroVideoBackdrop.tsx`), splash/Suspense fallback flipped dark→light; brief generator + lead capture reused untouched; whole-branch Opus review (3 fixes) + Codex clean; PR #293 (2026-07-18)
- [[Launch Events & Drive Delivery Session]](raw/sessions/2026-08-24-launch-events-and-drive-delivery.md) — PRs #513 and #515. A founder mark labelled "Restaurants in Hoboken:" got answered with **our** count (two) instead of the town's, because beside a liquidity model that phrase says both — **a plausible answer to the wrong question looks exactly like progress**. Label and question now say town-wide; the founder's answer went into a new `FOUNDER_FACTS`, deliberately NOT the assumptions register, since a founder saying a thing is neither MEASURED nor BENCHMARKED nor MODELED. Three launch events recorded (Antique Lofts, the Colony Hotel, a Montauk venue not yet chosen) as **events, not a change to the metro sequence** — read as three market launches they would contradict the deck's own liquidity slide — and left **unpriced**, because the ask is derived from a budget scoped to one city and inventing a number moves the raise. Explaining that in `deck/pending.ts` **failed the confidentiality build**: that module is in the public bundle's graph, so its strings ship, and so would a comment via the sourcemap. Also `npm run pitch:upload` (rclone) — see [[Drive Artifact Delivery]]. Six Codex findings over four rounds, all real, all mine; the sharpest was an **inference stated as a sourced fact** in an investor document (a seasonal audience overlap we have never measured), now labelled an argument. Two tests' first drafts were worthless — one asserted a slide's text and passed against the label it was meant to catch (2026-08-24)
- [[Loop Scout First Run]](raw/sessions/2026-06-20-loop-scout-first-run-builds.md) — AIOS Loop Scout's first batch triaged 2 built / 2 wontfix / 1 acknowledged; shipped expire-social-hooks + expire-email-verification-tokens crons; Codex caught a verify_jwt P1; aios_ingest_key Vault landmine fixed (2026-06-20)
- [[Founder Playbooks Session]](raw/sessions/2026-06-19-aios-founder-playbooks.md) — AIOS Founder Playbooks v1: saved repeatable internal tasks Donny runs on demand (report-only + propose); self-contained aios-playbook-run under the caller session JWT; the landing spot for Loop Scout candidates; + the verify-db-schema dev skill; PR #132 (2026-06-19/20)
- [[AIOS Workspace Knowledge-Merge Session]](raw/sessions/2026-06-20-aios-workspace-knowledge-merge.md) — Donny reads AIOS docs (read_file/workspace_read_file), import a Workspace doc → Strategy library, and in-UI approve+merge of wiki PRs (wiki-merge-pr) into RAG + library — no GitHub/Lovable trip; 3 slices, Codex-clean after 4 fix waves (2026-06-20)
- [[Validator Skills → Loops Session]](raw/sessions/2026-06-20-validator-skills-loops.md) — standardized one verdict-block contract (reuses Founder Playbooks done_check), added the verify-knowledge validator + a bounded knowledge-sync loop, taught Loop Scout to score condition-2 by validator presence; the validator caught 2 real pre-existing wiki orphans on its first run (2026-06-20)
- [[make-validator skill]](raw/sessions/2026-07-07-make-validator-skill.md) — the deferred *automate-last* step of the validator-skills work: a project-scoped `make-validator` meta-skill (NEW + RETROFIT modes) that authors validators to the one verdict contract; dogfooded by retrofitting verify-prod + verify-db-schema to actually emit the `{done,checklist,missing}` block (they were counted as validators but emitted only prose); skills+docs only, no prod risk; gap 1 of a 3-gap AIOS agent-loop audit (2026-07-07)
- [[AIOS Kill-switch Playbook + Loop-callable Playbooks Session]](sources/aios-killswitch-playbook-loop-session.md) — A1: report-only kill-switch-watch Founder Playbook (PROJECT_CONTEXT §3 guardrails as an executable check, armed-watch scaffold); A4: made playbooks loop-callable via a playbook-runner cloud-routine (execute_sql + capability map, deduped finding on breach only) — no schema/edge-fn/secret/auth change; Codex-clean (2026-06-20)
- [[Body Height Unit Mismatch Session]](raw/sessions/2026-08-24-body-height-unit-mismatch.md) — the follow-up that actually fixed the landing scrolling, after the previous PR's fix did not. Three diagnoses of one report: content overflow (refuted by measurement), the rubber-band (plausible, shipped, did not fix it), and finally `html, body { height: 100% }` resolving against the initial containing block — on iOS Safari the SMALL viewport — while the shell's `100dvh` GROWS as Safari collapses its toolbars, so body scrolled by the difference and the strip below it painted WHITE. **§9 had already measured the disagreement** (753 vs 833), fixed the shell's unit and left body's: *a height comparison has two sides*. Two lessons beyond the CSS — **position rules out cause faster than any probe** (the white sitting BELOW the app shell killed every in-page hypothesis at once), and **a true fact can confirm a false diagnosis** (the simulator proving WebKit *applies* `overscroll-behavior` answered a question nobody had asked). Confirmed on a real phone, the only instrument that could (2026-08-24)
- [[Page Drag & Logo Size Session]](raw/sessions/2026-08-24-page-drag-and-logo-size.md) — two reports against the landing that had already been "fixed" once. **The page could still be dragged** — the `h-[100dvh]` fix removed the scrollable gap that made the screen *jump*; rubber-band overscroll is a different mechanism and exists on a macOS trackpad too, so this one hit both viewports. Closed with `overscroll-behavior-y: none` (Y only — X carries iOS edge-swipe-back) **plus** the landing painting `<html>` grape, because the elastic gutter is outside the body box and a WKWebView's bounce is a native setting no CSS reaches. Run on the **iOS simulator** with a throwaway computed-style readout, which proved WebKit applies the property natively and — recorded as explicitly unproven — that *applied* is not *suppressed*. **And the logo had five sizes**, because a width class on a taller-than-wide asset multiplies the height; the durable half is that the previous fix pinned two files **to each other by hand** and reported green for a day while three unenumerated headers stayed wrong. See [[Mobile Viewport & Fixed Positioning]] §10 and [[Brand Logo Sizing]] (2026-08-24)
- [[Patch-Based Corrections Session]](sources/patch-based-corrections-session.md) — internal Donny corrects a strategy doc via small find/replace edits (server reconstructs the full doc, downstream unchanged), cutting the ~130s correction turn to seconds + ending the mobile streamed-fetch "Load failed"; backtick-in-template-literal deploy gotcha; PRs #151/#152 (2026-06-21)
- [[Light-Theme Polish Phase 1 Session]](raw/sessions/2026-07-18-light-theme-polish-phase1.md) — gave the light app one consistent on-brand look via a new shared kit ([[Light-App Kit]]) adopted across dashboards + campaigns + browse; clean-white + brand accents; de-gray surfaces/badges (not text); the nested-button + `AppCard`-`p-0`-over-shadcn-`Card` gotchas; fixed a same-file card mismatch + invisible `text-white` leftovers; Codex-clean, residual-grep zero; PR #280 (2026-07-18)
- [[Light-Theme Polish Phase 2 Session]](raw/sessions/2026-07-18-light-theme-polish-phase2.md) — rolled the shared [[Light-App Kit]] onto the surfaces Phase 1 deferred: **messaging** (retired the `bg-teal-50` "teal island" bg → white + `PageBody`; `teal-50` wash → `bg-dc-teal/[0.04]` inset tint; chat bubbles untouched), the **DragonShare + Dragon Feed** pair, and **public profiles** (`AppCard`/`AppStatusBadge`; pink hero + white text untouched; Busy badge → `tone="neutral"`, green "Available" kept). Surfaced the third kit gotcha — `AppCard` is not a `forwardRef` component, so a card needing a `ref` (a `reviewsRef` scroll target) keeps the ref on a plain `<div>`. Presentational only; Codex-clean, residual-grep zero; PR #282 (2026-07-18)
- [[Light-Theme Polish Phase 3 Session]](raw/sessions/2026-07-18-light-theme-polish-phase3.md) — rolled the shared [[Light-App Kit]] onto the surfaces Phase 1/2 deferred: **Settings** (the shared `SettingsSection` wrapper de-grayed → cascades across every settings section + promotions' `CGCPostingPreferences`; `StripeConnectSetup` chrome-only), **Promotions** (cards → `AppCard`, tabs → `AppChip`, pills → `AppStatusBadge`; error-boundary keeps its `bg-red-50` wash), and **Org/Billing/Payments** (money-flow **styling-only** — amounts/fee-math/enums/handlers/redirect-URLs byte-unchanged; failure red + semantic payment colors kept; starter/growth tier badges kept distinct). Founder deferred **Outstand to a Phase 4** (~51 files, per-instance social-platform-color judgment). Scope guards held: `AvatarCropModal` (dark-onboarding-shared), `PricingPage`, and the public customer funnel all absent from the diff. Codex-clean; 983/983 tests; residual-grep zero; PR #285 (2026-07-18)
- [[Light-Theme Polish Phase 4 Session]](raw/sessions/2026-07-18-light-theme-polish-phase4.md) — the FINAL surface-group slice: the ~47-file **Outstand** social-integration surface, in 6 reviewed sub-batches (shell/accounts, compose/drafts, calendar, analytics, prompts/sponsorship/money, engagement/Donny). Its own phase because Outstand's blue/purple/red MIX **social-platform BRAND colors (KEEP)** with off-brand accents → needs per-instance judgment, not find-replace. Kept: the `socialNetworks.ts` map + IG gradient + `x bg-gray-800` + all platform tints, chart data-viz colors (`TEAL_SHADES`/series/`<Cell>`), money-flow (DragonDash rush styling-only), `VerifiedBadge`/`CrossPostPrompt` (NO-CHANGE); only neutral unknown-platform fallbacks de-grayed. Batches 1–5 reviewed APPROVED, Batch 6 folded into the whole-branch review after a stream-timeout-post-commit; Codex-clean; 983/983 tests; residual-sweep zero; PR #288. **All four surface groups now on the kit.** (2026-07-18)
- [[Read the Traces Session]](raw/sessions/2026-07-18-read-the-traces.md) — a founder-supplied video on how Anthropic engineers automate ("4 rules") audited against the repo: **3 of 4 already implemented past what it describes** (4-Condition Test = "match the bottleneck"; the 7 scheduled routines = "Claude comes to you"; Founder Playbooks `done_criteria` + the verdict contract = "objectives not tasks"), so only rule 3 — **read the traces** — was built. New global `read-the-traces` skill over 598 local session files (~40MB) that nothing had ever read; plus repair of the two broken trace writes it depends on: `donny-orchestrator`'s `donny_tool_executions` insert (nonexistent columns + omitted NOT NULL `message_id` → had NEVER written a row, while `bug-sweep-agent` read the empty table as "clean"; `message_id` made nullable, deployed v69) and `playbook-runner-agent`'s invisibility on `/internal/loops`. edge-function-reviewer PASS; Codex clean after 4 rounds. Central `aios_loop_runs` deliberately deferred — build the free read before the storage; PR #292 (2026-07-18)
- [[De-gray Backgrounds & Off-Brand Accents]](raw/sessions/2026-07-18-degray-backgrounds-accents.md) — a cross-app cleanup after the four phases, prioritizing the two highest-visual-impact categories the surfaces/badges rule left: **panel `bg-muted`/`bg-gray-*` backgrounds** → `bg-dc-teal/[0.04]` inset, and **off-brand blue/purple/indigo accents** → teal/pink (keystone: the blue/indigo sponsorship cards). Audit finding: **no full-page washes remain** — every page is white, so it's all panel-level. 4 reviewed sub-batches, 55 files. Kept: pink/teal chat bubbles, dark-circle send buttons, chart data-viz, semantic amber/red/emerald/green, media backings, gray TEXT, shadcn `ui/*`. A translucent inset tint vanishes over a colored bubble → the inbound-attachment box got `bg-white/40` (whole-branch-review catch). Codex-clean; 983/983 tests; residual-sweep zero; PR #289. **With this, every panel + accent in the light app is on-brand.** (2026-07-18)
- [[Loop Memory & Security Triage Session]](raw/sessions/2026-06-24-loop-memory-and-security-triage.md) — shipped the [[Loop Memory Protocol]] (Phase 1, PR #161); the #161 conflation/merge/edge-deploy + verify-prod lazy-chunk blind spot; and a full read-only [[SECURITY DEFINER Advisor Triage]] (149 advisors) that was DELIBERATELY DEFERRED pre-launch (2026-06-24)
- [[Test-Mode Stripe UX Session]](raw/sessions/2026-06-24-test-mode-stripe-ux.md) — one-tap test-mode payout bypass (auto-enabled Custom account) + card-only checkout, live unchanged; pure vitest-tested mode helpers; MCP deploy verify_jwt-per-fn + full-path file-naming gotchas; live-verified payouts flip; PR #168 (2026-06-24)
- [[Social Provider Decision]](concepts/social-provider-decision.md) — **Decision 2026-08-04: STAY on Outstand; do NOT finish the Zernio cutover** (which had already reached Phase 2, merged + live, PR #360). Both justifications unwound. (1) The "hard ~7-connection cap" was a **real number but a SOFT quota** — plans carry an included-accounts figure (10) that support raises **free on request**; it appeared in exactly ONE file, the spec that used it as justification, traceable to no dashboard/doc/reply. (2) "Effectively no analytics" was **refuted by our own DB**: [[Outstand]] concluded 2026-06-11 from ONE empty-metrics YouTube post that our posts were "fundamentally unmeasurable" — two bullets *after* warning that an empty result is ambiguous — while `XDbxe`, captured two days later, returned **1,388 views / 5 likes growing between snapshots**. Nobody re-checked. **A post with no views returns 0; that is a measurement, not a failure.** Cost then *inverted*: Outstand bills **activity** and with Unlimited Posting is **flat ~$268/mo at any account count**; Zernio bills **supply** (per connected account, uncapped) — $1,218 at 1,000 accounts, $20,218 at 20,000. That taxes the curve that grows FIRST (creator sign-ups) against revenue that arrives LATER; at Y1 ARR, 5,000 accounts ≈ **10–21% of revenue**, rivalling the 15% AI cap. Creator-package fees offset only *transacting* creators (one $250 starter package covers a 2-account creator's year); Outstand's flat model deletes the idle-creator problem entirely, so **no connection gate was built** — requirement deleted, not engineered around. **Kept:** the `social-proxy` op-dispatch gateway (tenant scoping enforced server-side from our own DB, provider-independent), a fully live-verified dormant **Zernio adapter** (switching = a `provider` row update, not a rewrite), and 3 real bugs found en route (last-row-wins account pick in `confirm-posting-schedule`; `useDraftPosts` `.maybeSingle()` on a multi-row query telling working users to reconnect; `MediaRef` missing the `contentType`/`size` that media validation gates on). **Deliberately NOT done:** the 37-file `@outstand-so/ui` removal + 7 component rebuilds — that work existed only to escape Outstand. **Open risk:** quota raises are **manual, no self-serve path** — another company's support queue inside our onboarding; pre-raise ahead of demand (`docs/runbooks/outstand-quota-request.md`). Both failures share a shape: thin evidence recorded without provenance, then treated as settled ([[Verify Before Reporting]]).
- [[Stripe Webhook Revival Session]](raw/sessions/2026-06-24-stripe-webhook-revival-dual-secret.md) — the prod Stripe webhook was silently dead (empty stripe_webhook_events, STRIPE_WEBHOOK_SECRET unset); fixed with trust-true/verify-false payout-ready (PR #173) + dual-secret platform/Connect verification (PR #174); MCP byte-diff deploy, warm-isolate-stale-env redeploy, Vault≠Edge-Secrets, Snapshot-not-Thin (2026-06-24)
- [[Notification & Invitation Authorization Session]](raw/sessions/2026-08-08-notification-and-invitation-authorization.md) — three pre-existing authorization holes surfaced while shipping #382's invite-clarity UX, each **proven on prod inside a rolled-back transaction** before and after the fix. (1) `campaign_invitations` UPDATE had no `WITH CHECK` — my first diagnosis ("`creator_id` is rewritable") was **wrong**: Postgres defaults an omitted `WITH CHECK` to the `USING` expression, so the real holes were a forged `status='accepted'` and a **repointed `campaign_id`**, which manufactures apply-after-published rights. A policy cannot pin a column (no `OLD` in `WITH CHECK`) → column-level GRANTs. (2) `apply_to_campaign` checked eligibility only on its crew branch and, being `SECURITY DEFINER`, **bypassed the INSERT policy carrying exactly that rule** — an uninvited creator applied to an `active` campaign. Fixed by calling the policy's own predicate, not a second copy. (3) `create-notification` authenticated its caller then **discarded the user object** — any authenticated user could put arbitrary text in anyone's feed, as any actor, and email them. Closed with JWT-derived actor + `can_notify_user` (backtested 89/91 AND call-site-enumerated, which is the only way sponsorship was found) + server-composed copy for `content_liked`. **Five Codex rounds, five real findings, all mine** — including a tightening that silently killed 7 working email flows. Migrations `20260808010000`/`020000`/`030000` applied; `create-notification` v46. PR #387 open (2026-08-08)
- [[Notification Email Audit Session]](sources/notification-email-audit-session.md) — a dead invitation-email button cascaded into auditing every notification email; found the self-only auth gate was 403-dropping 9 transactional emails sent frontend→counterparty; rerouted via create-notification + added 3 missing templates; PR #161 (2026-06-23)
- [[Origin Story & Knowledge-Sync Automation Session]](sources/origin-story-sync-automation-session.md) — Authored the canonical DragonCandy origin story into the strategy library (one story, three-sided vision woven in; Joe Castelo=CEO corrections) + built the [[Knowledge-Sync Automation]] (npm aliases + auto post-merge hook + installer); 9 PRs #154–#162 (2026-06-21/22)
- [[Durable Flush Ledger Session]](raw/sessions/2026-07-24-durable-flush-ledger.md) — stage 1 of the wallet-first payout fix: a durable `pending_balance_flushes` ledger (table + claim/confirm/fail/bump RPCs + a `flush_${id}`-keyed shared `executeFlushTransfer` + a `reconcile-pending-flushes` `*/15` cron) makes the shared wallet→Stripe flush exactly-once — closes the identical-cents idempotency-key under-pay without re-introducing ambiguous-failure over-pay; bump-on-confirm-fail bounds the past-TTL double-pay loop; proven by a real test-mode Stripe replay E2E; branch `feat/wallet-first-payout` (2026-07-24)
- [[VerifiedRoute Missing-Profile Lockout Session]](raw/sessions/2026-08-02-verified-route-missing-profile.md) — a user reported as "unable to sign up or log in" was **logging in successfully every time** (`last_sign_in_at` that morning; `email_confirmed_at` five weeks earlier) and being deflected by `VerifiedRoute`, which gated on `profile?.email_verified !== true` and so collapsed "email unverified" with "no `profiles` row". It guards exactly ONE route — `/profile/setup` — so a profile-less user was bounced off **the only page that could provision them**, looping via `AuthPage`'s `isAuthenticated` re-check, under advice they could never act on. Two ways in: `handle_new_user` early-returns for `account_scope='internal'` (AIOS stakeholder invite → no consumer profile by design), and — the common one — **any** signup with a missing row but a `role` in metadata, because `AuthContext.createProfileFromMetadata()` fabricates a stand-in profile carrying **no `email_verified` key**. That second cause defeated the first fix and survived a Codex round: `profile ? … : …` takes the first branch for the fabricated object and `undefined !== true` rebuilds the loop ⇒ **"profile is null" is not a reliable test for "the row is missing"**; resolve on whether the flag is KNOWN (`profile?.email_verified ?? !!user?.email_confirmed_at`). Also: `OnboardingWizard` never created the `profiles` row it assumed existed (so onboarding couldn't self-heal) and `AuthContext` had no `refreshProfile()`. 3 Codex rounds, 2 real defects found; the 4 tests covering genuine unverified-email blocking pass against BOTH old and new code. Prod remediation was one confirmed write (dual internal+consumer account; metadata `role` required or the wizard defaults to creator → a second loop); users-without-a-profile now 0. PR #357 (2026-08-02)
- [[Wallet-First Reroute Session]](raw/sessions/2026-07-24-wallet-first-reroute-stage2.md) — stage 2 of the wallet-first payout fix: removed the transfer-vs-pending fork in `release-creator-payout` (one path: atomic wallet credit+marker → best-effort exactly-once flush → finalize), closing the two cross-path residuals by construction; reconciled the 3 frontend money readers to one `metadata.type`-keyed Total-Earned rule + In-Wallet-from-`pending_balance`; DI-testable `wallet-first.ts` module (not an `import.meta.main` guard); 4 Codex fix rounds (In-Wallet staleness, REVOKE-contested column → route via edge fn, phantom timeline, flush-on-verified-readiness + ready-aware copy); branch `feat/wallet-first-stage2` (2026-07-24)
- [[Payout Finalize Retry Session]](raw/sessions/2026-07-23-payout-finalize-retry.md) — `release-creator-payout` ran its post-money finalize once, fire-and-forget (logged CRITICAL, returned 200 → money moved, DB inconsistent, no retry); added a retried `finalizePayoutState` (self-heals transient DB blips). Descoped by 4 review passes — safely surfacing/retrying a finalize failure needs a durable payout marker (the Stripe key is only ~24h; the wallet credit is non-idempotent) → Complete follow-up; PR #328 (2026-07-23)
- [[Posting-Schedule Failed-Status Session]](raw/sessions/2026-07-23-posting-schedule-failed-status.md) — sibling of the state-machine drift: `campaigns.posting_schedule_status='failed'` is written by `confirm-posting-schedule` and rendered by `CampaignScheduleSection`, but the CHECK forbade it → silent stuck at `pending_review` + dead "Schedule Failed" UI; migration `20260723130000` adds `'failed'` (DB-only); PR #326 (2026-07-23)
- [[Project Context]](sources/project-context.md) — Project identity, strategy, and operating instructions (2026-05-23)
- [[QA Staging Supabase (Plan B) Session]](sources/qa-staging-supabase-planb-session.md) — Isolated staging Supabase for the CI/CD gate; 213-migration replay, secrets, Stripe sandbox alignment (2026-06-02)
- [[Staging Headless Login Session]](raw/sessions/2026-07-19-staging-headless-login.md) — founder: *"my credentials don't work in the testing environment… I don't want to be a bottleneck to always log in."* Two-layer diagnosis: the founder's `@harbormill.net` account is **prod-only** (the three seeded `.staging@dragoncandy.test` accounts are the *entire* staging user table), and staging's schema is **drift-corrupted** (~6 wk / ~90 migrations behind, recorded `schema_migrations` disagrees with actual objects) so the green **`smoke` gate is false assurance**. Shipped `npm run staging:login -- <role> --base <preview>`: mints a session with **no password typed** (admin `generate_link` → JSON `/auth/v1/verify` → session in URL hash), guarded to refuse prod (JWT-`ref` decode) and pin the target to this project's own previews (no token leak); Codex-clean round 9, proven end-to-end. Also replayed the 28-migration Crews cluster to unblock dashboard/list (gotcha: MCP `apply_migration` doesn't record `schema_migrations`). Rule: **verify auth-gated features on prod after merge, not on drifted staging.** PR #318 (2026-07-19)
- [[Realtime Edge Cases Session]](sources/realtime-edge-cases-session.md) — Race conditions, presence ghost state, keepalive, single-slot triggers (2026-05-06)
- [[SEO Audit Session]](sources/seo-audit-session.md) — react-helmet-async, JSON-LD, sitemap, h1 hierarchy, a11y fixes (2026-05-05)
- [[Stripe Prices]](sources/stripe-prices.md) — Definitive pricing reference, all test mode (2026-05-23)
- [[AIOS UI Polish Session]](raw/sessions/2026-06-26-aios-ui-polish.md) — AIOS shell reworked from an 11-pill wrapping row to a grouped left sidebar (Monitor/Operate) + mobile hamburger drawer with a pinned "Ask Donny"; shared PageContainer/PageHeader primitives across all 12 internal pages; Briefings/Strategy mobile doc-list cap + Findings pre wrap; presentational only, Codex-clean; PR #179 (2026-06-26)
- [[AIOS Stakeholder Invite Session]](raw/sessions/2026-06-26-aios-stakeholder-invite.md) — backfill ingest of PR #178: admin-only invite-by-email for internal-only AIOS accounts; handle_new_user guard hard-blocks consumer profiles for account_scope='internal'; manage-internal-users edge fn (invite/list/revoke); 2 Codex P2 catches (stale trigger body, never-accepted re-invite); shipped + deployed (2026-06-26)
- [[Dezzy Content Playbooks Session]](raw/sessions/2026-06-27-dezzy-content-playbooks.md) — two report-only Dezzy Founder Playbooks (content calendar + website/changelog drafts) seeded on the existing playbook rails; Dezzy = playbook suite, not a new runtime; zero code/table/UI, no aios-playbook-run edit (non-overlap w/ sibling dezzy-outreach); placeholder discipline for non-fabrication (2026-06-27)
- [[Dezzy Weekly Brief Session]](raw/sessions/2026-06-27-dezzy-weekly-brief.md) — Domain 5 capstone: a report-only admin-only dezzy-weekly-brief Founder Playbook (Monday action console — summary / platform numbers / worked-didn't / top 3 actions / Dezzy-queue checklist / system health); separate from the stakeholder weekly brief (reconciles via get_latest_briefing); orchestrate-not-embed (points to the detail playbooks, no aios_playbook_runs tool needed) → pure seed; Codex-clean (2026-06-27)
- [[Dezzy Press & Events Session]](raw/sessions/2026-06-27-dezzy-press-events.md) — Domain 4, the first Dezzy domain shipped as a scheduled CLOUD routine (not a playbook — the runner has no web access): monthly dezzy-press-events-agent web-scans press/podcast/publication/conference opportunities → top ~10 deduped [press]/[event] aios_findings via aios-report-ingest → /internal/findings; zero-infra (reuses findings rail); URL-required + $0-aware + never-critical + resolved-skip; Codex P2 (self-contradictory severity rule) fixed (2026-06-27)
- [[Strategy Library Management Session]](raw/sessions/2026-06-29-strategy-library-management.md) — DC AIOS strategy-library audit + safe reversible archive + core-file protection: `is_core` flag (seeded + `BEFORE INSERT` trigger, server-enforced) + archive triple; archive-aware `donny-knowledge-sync` (keystone: re-sync never resurrects an archived doc) + one-time `source_hash` backfill; service-role `dedup_candidate_pairs`/`internal_doc_exact_dupes` + admin archive RPCs; monthly `strategy-library-audit-agent` → `/internal/findings`; full prod rollout + keystone smoke passed (84/84 source_hash, archive/un-archive round-trip); founder go-live = create the `/schedule` routine (2026-06-29)
- [[Synthetic Weight Phase 0 Session]](raw/sessions/2026-07-23-synthetic-weight-engine-phase-0.md) — the safety spine before any bot exists: a `synthetic_users` registry auto-filled by extending `handle_new_user` (the `@synthetic.dragoncandy.test` email is the source of truth), a two-sided **actor-OR-parent** exclusion across `aios_*` metrics + the moat tables, a fail-closed `SYNTHETIC_BOTS_ENABLED` kill switch, a live-mode money guard checking BOTH parties, and leaf-first `purge_synthetic_data()` teardown. Proven on a rollback-wrapped 5-bot round-trip with founder metrics byte-identical; Codex caught 4 real segregation gaps over 3 rounds (2026-07-23)
- [[Synthetic Weight Phase 1 Session]](raw/sessions/2026-07-23-synthetic-weight-engine-phase-1.md) — the private-crew free-rails behavior engine on top of the spine: bots transact only on free crew rails (no money, no public surface), driven by a daily cron. **Live on prod** at N=25 (2026-07-23)
- [[Synthetic Weight Phase A Session]](raw/sessions/2026-07-24-synthetic-weight-load-economics-phase-a.md) — load proof & economics: a cross-tick session pool, a two-lane bulk-seed (interactive + depth), a ramped knee-not-outage load driver, two service-role RPCs, and the `/internal/simulation` load-curve + MODELED-revenue slice. **The finding: a single GH runner caps ~312 concurrency on EGRESS while prod's DB stays ~91% idle** — so a credible 50K proof needs multi-IP fan-out. Codex caught 8 more real issues over 7 rounds (2026-07-24)
- [[Synthetic Load Runner Matrix Session]](raw/sessions/2026-07-24-synthetic-load-runner-matrix.md) — Slice 1, the multi-IP fan-out that breaks the single-runner egress wall: `bulk-seed --with-content`, a ~90:10 DAU behavior mix (media as a HEAD proxy), 3 **matrix-only** RLS-real writes so single-runner never touches the live `bot0##` cohort, `get_sim_load_matrix_summary`, the `synthetic-load-matrix.yml` workflow, and a summed dashboard card. The 50K band ran at 83% DB idle; Codex 3 rounds, incl. an R3 **verified false** by a rollback-wrapped RLS probe; PR #337 (2026-07-24)
- [[Tech Department Scope & Repo Joinability Session]](raw/sessions/2026-08-19-tech-department-scope-and-repo-joinability.md) — board thread (Dame/Joe/Adrian) on adding tech staff; Dame committed to a tech scope of work. Produced `docs/DragonCandy_Tech_Department_Scope.md` (goals, 4 roles, stack, ways of working, comp by region) — and, in auditing readiness, found the repo **could not safely be handed to anyone**. The measured case for hiring: 1,174 src files / 92 pages / 269 hooks / 98 edge fns / 389 migrations / 2,443 tests, ~2,500 of 3,299 commits by one person, and commits/month Mar 190 → **May 1,023** → Aug 131, an **87% fall from peak** — the complexity tax of one maintainer, not a motivation story. `PROJECT_CONTEXT` §4 had claimed 73/206/80 for ~a year. Shipped the guard + `supabaseEnvGuard.ts` (+19 tests), `CONTRIBUTING.md`, `ARCHITECTURE.md`, `onboarding/first-week.md`, PR/issue templates, `.nvmrc`+`engines`, a truthful README, and the Lovable→Vercel drift fix in both runbooks **and** [[QA CI/CD Gate]] (which had recorded the cutover in one section while two others still said Lovable). Gotchas: `git ls-files` not `.gitignore` tells you what ships; `package.json` is CRLF so a JSON round-trip made a 137/134-line diff for 3 lines; `find -name a -o -name b` needs parens (154 vs vitest's true 243); and the first draft **invented US salary bands while instructing the reader that reconciliation with the cost model was mandatory** — replaced with the model's figures, which surfaced a real decision (this plan hires a PM at month 0, the cost model at month 6). PR #451, Codex clean at round 2, CI green (2026-08-19)
- [[Credible 200K Load Matrix Session]](raw/sessions/2026-07-25-credible-200k-load-matrix-slice2.md) — Slice 2 closes the two gaps between Slice 1's number and a credible "200K DAU": `media_fetch` becomes **real** Range-capped GET egress against public storage (requiring exactly `206`, so worst-case bytes are computable up front), and the summary gains a bin-width-independent event-sweep `honest_peak_concurrency` + `max_concurrent_shards` so staggered/queued shards can't inflate the naive per-shard sum. Plus media error/latency signals, `/internal` surfacing, `MAX_SHARDS` 10→20; 4 real Codex fixes over 5 passes; PR #343 (2026-07-25)
- [[Living Marketplace Phase A1 Session]](raw/sessions/2026-07-25-living-marketplace-phase-a1.md) — Sub-project A: a persistent, browsable `botmk_` synthetic cohort built through real RLS-enforced flows, scaled to **2,000 profiles** (500 business / 1,500 creator across 24 US cities) with segregation byte-identical afterwards. The active/depth split (a ~24-bot interactive core + bulk-inserted browse-only depth) is what gets past the ~25-per-IP mint 429 wall; PRs #339–#342 (2026-07-25)
- [[Dezzy SEO Articles Session]](raw/sessions/2026-06-28-dezzy-seo-articles.md) — Domain 6 SEO/organic-discovery slice: a pure-seed report-only dezzy-seo-articles Founder Playbook that drafts one publish-ready SEO article/run for $0 organic acquisition (grounded keyword pick via get_platform_stats + GTM "creators-first" override); E-E-A-T no-fabrication + founder-confirmed link placeholders; the rest of Domain 6 (milestone celebration/case studies/referrals/boost-perf) GATED on the empty DRE ledger + missing referral system; Codex-clean (2026-06-28)
- [[DRE Go-Live Runbook Session]](raw/sessions/2026-06-28-dre-go-live-runbook.md) — read-only readiness check + go-live runbook for the Dragon Rewards Engine (no prod change): DRE is fully deployed + cron-live (jobid 7) and the silent backfill already ran (98 events / 24 balances); go_live_at=2099 gates ONLY the in-app bell, not awarding or UI visibility, so ~24 users likely already see points/tiers (DragonPointsCard/DragonTierBadge have no go_live gate); flipping go_live_at = turn on forward award notifications (≈irreversible) = a founder launch decision, not an engineering deploy (2026-06-28)
- [[DRE UI Launch Gate Session]](raw/sessions/2026-06-28-dre-ui-launch-gate.md) — fixes the un-gated rewards UI exposure: gate DragonPointsCard + DragonTierBadge behind the DRAGON_REWARDS_ENABLED feature flag (seeded OFF) so points/tiers stay hidden (anon-safe) until launch; chose the flag over go_live_at because dre_config is authenticated-read but public profiles are anon (go_live_at would break anon badges post-launch); launch is now two switches (flag→UI, go_live_at→bell); runbook updated; Codex P2 (stale runbook) fixed (2026-06-28)
- [[DRE Rename (Creator Standing) Session]](raw/sessions/2026-06-28-dre-rename-creator-standing.md) — founder feedback: fantasy tier names + "Dragon Points" read corny; renamed the user-facing labels only to a mature "Creator standing" system (currency Dragon Points→Reputation/Rep; tiers Egg→Rising/Scout→Established/Knight→Pro/Master→Elite/Legend→Icon; emojis dropped). Display-only — keys/tables/flag/DP/DRE internal names untouched (no migration); dre-award-engine notification copy redeployed v2; Codex-clean (2026-06-28)
- [[Anonymous Brief Generator Fix Session]](raw/sessions/2026-06-28-anonymous-brief-generator-fix.md) — the landing's free brief generator 500'd on every call (delegated to the user-gated donny-campaign-generate with a service-role key → 401); rewrote generate-anonymous-brief self-contained (own fetch + hardcoded Haiku) + Layered-v1 abuse hardening (global cap, best-effort per-IP, honeypot, hardened SSRF) + a thin-page source_quality signal; spec-reviewed (6 fixes) then Codex-clean after 2 P1s (trailing-dot SSRF + malformed-IPv6 cap bypass); deployed + live-verified (2026-06-28)
- [[Dezzy Milestone Celebrations Session]](raw/sessions/2026-06-28-dezzy-milestone-celebrations.md) — Domain 6 amplification core, un-gated by the live DRE: a get_recent_milestones read tool (7th on aios-playbook-run; firsts + campaign milestones, 30d, PUBLIC handles only, resolved by event_type role prefix, capped 15) + a report-only dezzy-milestone-celebrations playbook drafting #DragonDashed posts; spec-reviewed (2 rounds) + Codex-clean after 1 P1 (migration timestamp) + 2 P2s (business-completion label, role-prefix resolution); deployed + data-layer-verified (2026-06-28)
- [[Dezzy Outreach v1 Session]](raw/sessions/2026-06-27-dezzy-outreach-v1.md) — Dezzy AI (company-facing growth agent) domain #3 the Outreach Machine, v1: report-only/draft-only dezzy-outreach Founder Playbook + get_reactivation_targets read tool (3 segments: stalled campaigns / dormant creators / lapsed restaurants; public-handles-only, org-aware, captured-boosts); 2 Codex P2 fixes (business-handle privacy parity, active-org-members); deployed + ran twice on prod (done=true, no PII leak) (2026-06-27)
- [[DRE Engine + Tiers + Badges Session]](raw/sessions/2026-06-27-dre-engine-tiers-badges.md) — first sub-project of the [[Dragon Rewards Engine (DRE)]] (parent Phases 1–2): config-driven Dragon Points ledger + idempotent anti-join award engine (cron edge fn, consumes existing events) + 5-tier system + tier badges on dashboards/public profiles; zero cash exposure, ledger-first; spec+plan+code reviewed (Codex-clean), founder go-live pending (2026-06-27)
- [[AIOS Agent-Loop Audit]](raw/sessions/2026-07-07-aios-agent-loop-audit.md) — audited the AIOS against a YouTube agent-loop framework → 3 gaps built + two-model-reviewed: make-validator meta-skill (#217), /internal/loops mission control (#218), and the runtime spend source-of-truth (#220, deployed + proven live). Standout reframe: the AI kill-switch was measuring dev spend, not runtime (2026-07-07)

- [[Creator Groups Session]](raw/sessions/2026-07-09-creator-groups.md) â€” Creator Groups ("crews") + private group campaigns: standing per-business creator roster (owner=business user, inviteâ†’accept lifecycle) + `campaigns.group_id` private-visibility RLS + one-tap free apply; escrow uncoupled for free collabs, paid/public path byte-unchanged; 10 Codex rounds (auth-ownership trigger, prod-only `creator_count`-JSONB, escrow-path guards, status gates, draft overrides, email copy) + 2 pushed-back false positives; schema live on prod, frontend deploys on merge (2026-07-09)
- [[Find Creators Location Search Session]](raw/sessions/2026-07-07-find-creators-location-search.md) â€” restaurant Find Creators "near me" + radius control (client-side, reuses the geo stack, no schema): default near-me off the saved location, city/ZIP override, Nearest sort, distance on cards, widen-if-empty nudge; consolidated the buried Zip/City/Country filters + dropped county; brainstormâ†’specâ†’planâ†’subagent execution â†’ Codex-clean after 6 rounds (effect-sync staleness, ZIP precision, brand-page coupling, legacy-`location` fallback) (2026-07-07)

- [[Schedule Agenda Simplification Session]](raw/sessions/2026-07-10-schedule-agenda-simplification.md) â€” made scheduling agenda-first (mobile + desktop default to one scrolling day-list + one ï¼‹Schedule + tap-month jump); shared pure AgendaItem model/adapters â†’ AgendaView; kept desktop grids/drag as an optional toggle + readable Month chips; fixed the /calendar ï¼‹Schedule silent no-op + the review-panel dead-end (screenshot); 8 TDD tasks, per-task + whole-branch Opus reviews (44px, responsive variant, legend) + Codex-clean after a P2 sponsorship-parity fix; frontend-only (2026-07-10)
- [[Social Measurement Spine Session]](raw/sessions/2026-08-06-social-measurement-spine-and-post-ownership.md) â€” two efforts recorded together because **PR #365's knowledge-sync never ran** (verified against `origin/main`, not a worktree). #365 built the spine and found three live defects nobody had reported: **video posts silently discarded at publish** (a CHECK rejected `video_reel`, the insert's error was thrown away, the user was shown success), **every unmeasured post stored as a real zero** (Outstand confirmed *three* distinct states all return `success:true` with all-zero metrics; only one sets `metrics_error`), and the measurement record never written for most posts â†’ moved to the webhook choke point. #366 then made amplification measurable (it was the one path writing no schedule row), stopped it being relabelled `campaign` (the webhook's `post_type` recompute had no `amplification` mapping â€” Task 1 alone would have shipped a correct row into a consumer that corrupted it), added the reconciliation sweep, and **server-established post ownership**. **Codex found the defect four prior reviews missed**: amplification's id parse never checked the `.post.` level, resolved `null` every call, and skipped both writes â€” the branch's entire purpose, inert. All four earlier reviews asked *is the new code correct?*; Codex asked *does it run?* Traced to a brief that said to extend an existing guard â€” reuse-don't-duplicate is right, but nobody checked the reused thing worked. (2026-08-06)
- [[Outstand Cross-Tenant Metric Read Session]](raw/sessions/2026-08-05-outstand-cross-tenant-metric-read.md) â€” the live cross-tenant metric read found during the spine work: `social_post_log`'s INSERT policy constrains only `user_id`, so an authenticated user can name any `outstand_post_id`. The `verified_at` gate closed blind enumeration and the API-quota-burn angle but **not** the targeted case; the obvious fix proved circular because the trust anchor was itself client-writable. Closed in PR #366 by [[Social Measurement Spine]]'s server-established ownership binding. (2026-08-05)
- [[Service-Role Remediation Session]](raw/sessions/2026-07-19-service-role-remediation.md) â€” PR #308: 12 authorization guards across 4 edge functions + the pure `_shared/campaign-access.ts` (`evaluateCampaignAccess`/`evaluateApplyAccess`, 19 tests, fails closed), remediating what the new `data-exposure-reviewer` found. **Running the reviewer on its own fix found more than the original six** â€” incl. a cross-tenant *write* (`handleRegenerate` overwrote another tenant's preview row while returning their brief) that was not in the filed findings at all. **Then Codex caught one all three of its rounds missed**: applications â‰  collaborations under the `campaigns` SELECT policy (`has_collaboration_on_campaign` is status-independent; there is **no application arm**), so a rejected applicant kept access to a *closed* campaign's brief and budget â†’ split `isCollaborator` / `hasApplication`. Two **functional regressions** were introduced and caught in review (gating a pre-application endpoint on participation; filtering the caller's OWN row on visibility) â€” a security fix that breaks the feature is its own failure mode. `verify-db-schema` read the live policy from prod and independently confirmed the Codex finding; 0 stale `profiles.org_id` pointers. Deployed donny-chat v147 / donny-campaign-preview v98 / donny-creator-match v73 / donny-apply-pitch v57 with `verify_jwt` preserved (apply-pitch is `true`, the rest `false` â€” a blanket `--no-verify-jwt` loop would flip it). Latent not leaking at ship time: 0 private creators / businesses / crews / drafts (2026-07-19)
- [[Wiki RAG Dedupe Session]](raw/sessions/2026-08-10-wiki-rag-dedupe.md) — PR #437, the follow-up that found #434 right about direction and wrong about volume. `sync-internal-docs.mjs` already syncs the whole wiki tree at internal scope and `wiki-merge-pr` writes the same namespace, so marking every page internal produced **113 pages embedded twice, 109 byte-identical** — two of internal Donny's five RAG slots available to one page, double embedding cost every sync, and the duplicate was the **only** copy subject to the hard `FAIL_CHARS` skip that had queued four pages for splitting. Now the script publishes only the (empty) `CONSUMER` allowlist and the duplicates are pruned, 249 → 136. The cost: publishing-only is not self-healing, so a page removed from the allowlist would strand at `scope null` forever — paid for by a **read-only** orphan check that names orphans with their real scope and fails the run, because a rule in a comment does not hold. Two defects found by running it rather than reasoning about it: an orphan message that overclaimed "still consumer-retrievable" when the rows were internal, and `process.exit()` after a fetch being replaced by a Windows libuv abort that **ate the exit code** (1 → 127) — the assertion the wiki had called harmless for months, also fixed on `sync-internal-docs.mjs`. Ordering rule: merge the script change before pruning, or the post-merge hook re-inserts everything from `main`'s older script (2026-08-10)
- [[Wiki RAG Consumer Scope Session]](raw/sessions/2026-08-10-wiki-rag-consumer-scope.md) — PR #434: `EXCLUDE` in `sync-wiki-to-donny.mjs` was inert (gated on a `SYNC_CURATE=1` the unattended post-merge sync never sets), so 19 pages synced to the **consumer** RAG at `scope null` on every merge — 107 of 112 wiki rows consumer-reachable on prod. Two findings reshaped the fix: `sync-internal-docs.mjs` **already mirrors every wiki page** at internal scope (1:1, 112 and 112), so the `wiki:` rows serve only the consumer scope; and the worst page was on neither list, reachable through the `general` catch-all agent. Inverted to a `CONSUMER` allowlist, left **empty** — after which the consumer predicate returns **0 of 247** rows, i.e. consumer Donny's entire RAG had been the leak. Codex found 2, both mine: a regression test I broke by deleting the list it parses (grepped `SYNC_CURATE`, never `FORCE_INTERNAL`), and a false claim in my own guard comment that a stale allowlist entry is "more protected, never less" (2026-08-10)
- [[Legal Entity on the Public Site Session]](raw/sessions/2026-08-10-legal-entity-public-site.md) — PR #439: Apple Organization enrollment `5HA89RBHQH` is verified partly by **visiting the website**, and dragoncandy.com named no legal entity anywhere (grep-confirmed zero hits). Started as a footer line; reading the legal pages found **neither** named the entity — Terms §1 said "operated by DragonCandy", a **brand, not a company** — while `LegalPageLayout`'s own docstring says those pages exist to "expose stable, indexable URLs for App Store Connect". Shipped one `src/lib/legalEntity.ts` constant across three surfaces (footer city-only; Terms + Privacy entity + full postal address), footer as a **sibling below** the existing row so the desktop row stayed byte-identical (10 insertions, 0 modifications; text at x=104px = the logo's exact left edge). **The session shipped a wrong claim and then removed it**: "a New Jersey limited liability company" went into the operative sentence of the Terms, and the founder's IRS CP 575 B letter showed it was an inference — that document attests the name, LLC status, Form 1065 and a Hoboken *mailing* address but **never where the LLC was formed**, and §15's choice-of-law clause is not a formation claim. Also found: the IRS and D&B records **disagree on the street line**, and the D&B form is the one to publish. Mobile verified by 316/386px `srcdoc` iframe because `resize_window` leaves `innerWidth` pinned; landing footer closed at bundle level (read the deployed `legalEntity-*.js`) because the signed-in session redirects `/landing` (2026-08-10)
- [[Mobile Screenfit Session]](raw/sessions/2026-07-14-mobile-screenfit-fixed-position.md) â€” two founder iPhone screenshots root-caused: PageTransition's transform (framer stalls at `initial` on first load, leaving translateY(6px) inline forever) trapped every fixed element â€” bottom nav/Donny anchored to page content, not the viewport â†’ transition is now opacity-only by contract; useScrollDirection gained an 80px bottom-reveal floor; crew Invite sheet fits iOS via 82dvh + safe-area footer padding; verified with a live fixed-probe before/after; Codex-clean; frontend-only (2026-07-14)
- [[Google Workspace Signatures Wave 1 Session]](sources/google-workspace-signatures-wave-1-session.md) — 2026-08-20, branch `worktree-dc-google-workspace` (11 commits, Codex clean). The founder set up the Workspace and asked for it to be organised "like an official cool corporate workspace"; this is Wave 1's **code half only** — the admin console half (two shared drives, nine Google Groups, `adrian@`, the service account) cannot be done by any agent and is founder-owned. Shipped: PNG brand marks (Outlook's Word engine cannot render WebP), a pure signature renderer with 19 tests, and an Apps Script that installs signatures domain-wide nightly via domain-wide delegation, reading titles from the **directory** so there is one place a title can be wrong. Found: three founders' titles stale in **nine** files including the live investor deck and all four `docs/hiring/` docs Adrian forwards to candidates. Deliberately not swept: `Org_Staffing_Plan.html`, whose labels encode a working arrangement across a three-phase org chart rather than a title (2026-08-20)
- [[Site Access Lockdown Session]](raw/sessions/2026-08-23-site-access-lockdown.md) — the session that built the private-preview gate: two layers (Supabase signup off + an edge Basic password as Vercel Routing Middleware), the 401-not-redirect decision, the deleted client-side gate that shipped its password as a bundled string constant, and the nine defects review found — including a Lighthouse exemption written somewhere it could not work, and a fail-open branch sitting in front of all the fail-closed logic (2026-08-23)

## Entities

- [[Capacitor Native Shell]](entities/capacitor-native-shell.md) â€” iOS delivery surface; wraps the web build in a native shell (Phase 1 shipped)
- [[Donny AI]](entities/donny-ai.md) â€” Intelligence layer: campaign generation, creator matching, analytics, scheduling
- [[DragonCandy Platform]](entities/dragoncandy-platform.md) â€” AI-powered creator-restaurant marketplace
- [[DragonDash]](entities/dragondash.md) â€” Premium rush content delivery, the profit engine
- [[DragonShare]](entities/dragonshare.md) â€” Organic-content amplification engine, 80/20 boost payments
- [[File Management]](entities/file-management.md) â€” Content-deliverable system: file_uploads + versions/permissions/comments/tags, private buckets + signed URLs
- [[Google Workspace]](entities/google-workspace.md) â€” AIOS Connections: per-user OAuth, Drive hub, Donny export, Gmail deep-link, metrics Sheet, Chat scaffold (dark)
- [[Organizations]](entities/organizations.md) â€” Team accounts: parent org â†’ org units (locations/products) â†’ members; multi-unit, seat billing, RLS
- [[Outstand]](entities/outstand.md) â€” Social-posting bridge (Instagram, TikTok, YouTube); delegated posting, account recovery
- [[Stripe Connect]](entities/stripe-connect.md) â€” Payment infrastructure, escrow, subscriptions, boosts (test mode)
- [[Supabase]](entities/supabase.md) â€” Backend: Postgres, Auth, Edge Functions, Realtime, RLS

## Concepts

- [[Toast Partner Integration]](concepts/toast-partner-integration.md) — six deployed `toast-*` edge functions, four migrations, a settings card and a runbook, built against an authentication flow **Toast does not have**. Toast has no OAuth: no authorize URL, no user redirect, no authorization code, **no refresh token** — a partner POSTs `clientId`/`clientSecret`/`userAccessType: TOAST_MACHINE_CLIENT` to `/authentication/v1/authentication/login` for a ~1-hour bearer token and re-logs-in on expiry, while restaurants grant access **restaurant-side** (Toast Web → Integrations → Browse & purchase → Add Now) and each request names one via `Toast-Restaurant-External-ID`. So `toast-oauth-start` redirects nowhere, `toast-oauth-callback` exchanges a code Toast never issues, `toast-token-refresh`'s 30-min cron performs a grant that does not exist, and the runbook troubleshoots `invalid_grant` errors Toast cannot emit. **The tell was in the repo all along: every Toast URL is an env var** — nobody hardcoded a host because nobody had the docs. Blast radius today is zero (no `TOAST_*` secret ⇒ all six answer `toast_not_configured` 503; zero `%toast%` tables on prod ⇒ the migrations were never applied), but `PROJECT_CONTEXT.md` claimed Toast POS as an **active integration** until 2026-08-23 — a claim `SHIPPED_LOG.md` had already contradicted in writing two weeks earlier, and nothing read. Partnership Step 1 of 3 submitted 2026-08-23 as Dragon Candy LLC; **the license agreement self-terminates 2027-02-23** (§3(d)) if Toast has neither accepted nor rejected by then, and Toast warns of a backlog — so the deadline can pass with nobody at fault. Eight stages to GA; sandbox credentials need legal/compliance/privacy/security sign-off **plus** a signed partner agreement, production needs a one-hour certification demo (2026-08-23)

- [[AI Creator Matching]](concepts/ai-creator-matching.md) — The business "Find Perfect Creators" matcher: scoring pipeline + `campaign_matches` write path + the "Found 0 potential creators" root cause (a silently-swallowed INSERT — `match_score` `numeric(3,2)` vs 0–100 scores, a stale trigger's `NEW.brand_id`, and a dead `business_address` select — NOT a matching-logic bug) + distance-based geographic scoring ported into `_shared/geo.ts` (edge functions can't import from `src/`); soft scoring never excludes; weight-normalization invariant (non-AI sum = 100 − ai_quality); now also covers the **Donny chat `match_creators` sibling** (internal Donny) AND the keystone wiring fact — the **consumer web/mobile Donny uses `donny-orchestrator`** (sub-agent router), where the real `find_creators` sub-agent (shared `creator-discovery.ts`, `tool_choice`-forced) makes Donny return a ranked creator list (2026-07-16)
- [[AIOS Internal Shell]](concepts/aios-internal-shell.md) — How `/internal/*` is navigated + laid out: desktop left sidebar grouped Monitor/Operate, mobile hamburger drawer, pinned (non-floating) Ask Donny, shared PageContainer/PageHeader primitives, the dark ops-deck theme
- [[AIOS Runtime Spend Source-of-Truth]](concepts/aios-runtime-spend-source-of-truth.md) — the ≤15%-of-revenue AI kill-switch should govern RUNTIME serving cost (donny_cost_ledger), NOT the invoice (mostly founder Claude Code dev/opex); the two-constraint ledger gotcha (auth.users FK + tier CHECK both blocked user-less/embedding inserts); alert loop (ai-cost-vs-cap → playbook-runner finding) + a live /internal "Runtime vs cap" card; PR #220 (2026-07-07)
- [[AIOS Stakeholder Invite]](concepts/aios-stakeholder-invite.md) — Admin-only invite-by-email for internal-ONLY AIOS accounts (no consumer app); hard-block keystone = handle_new_user guard skips consumer profiles when account_scope='internal'; manage-internal-users edge fn (invite/list/revoke, verify_jwt=false, last-admin guard); access purely user_roles; PR #178
- [[Anonymous Brief Generator]](concepts/anonymous-brief-generator.md) — The landing's free "paste a URL → brief" teaser (`generate-anonymous-brief`): self-contained generation (never delegate to the user-gated donny-campaign-generate with a service-role key → 401), hardcoded Haiku (NOT getModelConfig → Sonnet), HTTP-200 error-discriminator contract, and Layered-v1 abuse hardening (global daily cap as the real cost ceiling + best-effort per-IP + honeypot + hardened SSRF guard) + a thin-page source_quality signal
- [[Auth + Onboarding Landing-Theme Retheme]](concepts/auth-onboarding-landing-theme.md) — presentational-only retheme of login/sign-up + the 5 auth siblings + onboarding from dark to the light "Human-driven. AI-assisted." landing identity, softened for forms; new `AuthShell` reuses the landing's additive `landing-*` tokens/fonts; both dark triggers + the now-dead `useDarkHtml()` hook removed (`/internal` is now the only dark surface); the `isolate`-vs-`relative z-10` flex-collapse gotcha in `AuthShell`; PR #299 (2026-07-19)
- [[Brand Logo Sizing]](concepts/brand-logo-sizing.md) — one logo size across every header, in `src/lib/brandLogo.ts`. Both marks are **taller than wide** (280x326 and 400x465, aspects 0.859/0.860), so a width class does not cap the height, it multiplies it: auth rendered 116/140/**163px** tall, the post-login mobile bar **74px**, the desktop sidebar **116px**, against the landing's 56. The durable half is the guard, not the CSS — the 2026-08-23 pass fixed two files and pinned them **to each other by hand**, and that test reported green for a full day while three headers nobody had enumerated stayed wrong. *A guard that watches the pair you already repaired cannot see the four you did not*; `brandLogo.test.ts` now derives the header list instead of listing it, the same shape as `profilesWriteGrants.test.ts`. Because the two aspects agree to within 0.001, one height class renders identically whichever file a surface imports. Intrinsic `width`/`height` must be the asset's REAL size — `PublicPageHeader` carried 140x47 (aspect 2.98), so the attributes meant to prevent layout shift caused one. The two post-login headers are pinned at class level only: reaching them needs a login and **no test-account credentials exist** (2026-08-24)
- [[Campaign Generation Creativity]](concepts/campaign-generation-creativity.md) — How Donny turns a business (URL/photo/text) into campaign ideas + the PR #243 rework: the diagnosis (weak output was the over-constrained PROMPT, not the cost auto-downgrade — verified innocent via the prod ledger, always Sonnet at 0.3% of budget); the freed prompt in a pure testable `donny-campaign-generate/lib.ts` (soft platform preference, `creative_concept` + one wildcard, relaxed caps, robust outermost-`{}` parser); chat unified to the 3-concept path; the model `floor` so the profit flow never drops to Haiku@512; frontend crash-proofing; shipped on Sonnet@8192 (Opus a 1-line toggle)
- [[Campaign Invitations]](concepts/campaign-invitations.md) — what a campaign invitation **actually is**: a direct nudge to apply, not an assignment and not a queue-jump. Verified, not assumed — `usePublicCampaigns` filters only `status='published'` + `group_id IS NULL` (**no invitation filter exists**), `send-campaign-publish-notifications` already mails every onboarded creator at publish, there is **no "Accept" button** (accepting *is* applying; `apply_to_campaign` flips the row as a side effect), and the one real privilege is applying after the campaign leaves `published`. The product said none of this — no tooltip, no help text, no `help_articles` row — so users read "Invite" as "make this creator do the work" (PR #382 fix: header copy, **"Invite to apply"**, a `campaign_invite` WhyExpander, and status shown as Invited·waiting / Applied—review them / Declined, since `status` had always been fetched and never read, rendering a **declined** creator identically to an accepted one). Gotchas: an expired `pending` invite was permanently un-resendable *silently* (`ignoreDuplicates` never refreshed the row → owner clicks, nothing happens, forever) — now revived; `trg_reject_group_campaign_invitation` is **`BEFORE INSERT` only** despite its comment claiming every write path, so the crew guard is re-asserted in code; `bulk-invite` **skipped the in-app bell — fixed 2026-08-08**, both paths now share one `notifyInvitedCreator()`. **§ Integrity (PR #387, 2026-08-08):** two live holes proven on prod inside a rolled-back transaction — the UPDATE policy's omitted `WITH CHECK` (which does NOT mean unconstrained: Postgres defaults it to `USING`, so `creator_id` was pinned; the real holes were a forged `status='accepted'` and a **repointed `campaign_id`**, manufacturing the apply-after-published privilege) → now decline-only + column GRANTs, since a policy cannot pin a column against change (no `OLD` in `WITH CHECK`); and `apply_to_campaign`, a `SECURITY DEFINER` RPC that **bypassed the very INSERT policy carrying its eligibility rule** on the non-crew branch → an uninvited creator applied to an `active` campaign
- [[Campaign Lifecycle]](concepts/campaign-lifecycle.md) — Draft through completion, applications, sponsorship
- [[Campaign Price Anchoring & Negotiation Reach]](concepts/campaign-price-anchoring.md) — what a business pays a **creator** (vs [[Pricing Architecture]], which is platform monetization): the two-sided anchor problem, why the fix belonged in the generator rather than the label (a pre-filled bad number and a suggested bad number are the same number), the founder-approved per-deliverable tier bands with `campaignPricing.ts` as the single source, an empty price field with tap-to-fill chips, honest "estimate not market rate" copy for a pre-revenue platform, and the band fallback that makes the anchor drop on frontend merge alone. Plus negotiation **reach** — the counter-offer system was never missing, just gated on `isInvited`. Gotchas: `fixed_price = 0` (crews) is not `null` so `!= null` is not an "is this paid?" check; `isInvited` carries no authorization weight (it never reaches the DB and derives partly from a URL param)
- [[Campaign Target Audience]](concepts/campaign-target-audience.md) — what a campaign is aimed at: the 2026-08-07 replacement of the decorative "Target Creators" chip grid (`Foodie/Lifestyle/…`, hardcoded in 3 places, written to `ai_analysis.target_creator_persona` and read by **nothing** — matching scores the `creator_profiles.skills` *craft* enum, a disjoint taxonomy) with a Donny-written **audience line** — the customer the content should attract — plus 2 one-tap alternates and 4–6 creative-direction `campaign_tags` for the creator's brief. Audience is the campaign's thesis so it sits in Campaign Overview and on every idea card (you pick between *customers*, not titles); tags sit with style direction. Key mechanism: `target_audience` is emitted **before** `style_direction`/`key_messages`/`hashtags` in the schema because the model is autoregressive — order does what an instruction alone does not. Exactly two coercion boundaries (Zod + `normalizeDraft`), "write short, read tolerant" (`MAX_AUDIENCE_CHARS` 160 > the prompt's 120, because legacy-wizard campaigns store prose under the same key). No migration — `ai_analysis` only; legacy personas keep rendering and sunset on their own. Gotchas: the localStorage draft path **never sees Zod** (bare `JSON.parse` → `.map` throws, invisible in dev); `lib.test.ts` forbids `MUST`/`ONLY`/backticks in the prompt; `codex review --base main` reviews **nothing** when work is staged-but-uncommitted. **SHIPPED 2026-08-07 — both halves live** (frontend #372, `donny-campaign-generate` **v114**); a real prod generation returned 3 distinct audiences (age band + proximity, none a creator job title), 2 alternates and 6 tags each, with style/tags visibly derived from the audience. The frontend-BEFORE-function deploy order is now history, not an instruction; a transitional `"target_creator_persona": []` remains in the prompt to protect stale tabs and is dropped ~a week out
- [[Cloud Platform Strategy]](concepts/cloud-platform-strategy.md) — should we move to AWS / GCP / Azure / Rackspace as we scale? **We are already on AWS and we do not manage it** — Supabase deploys *only* to AWS regions and Vercel's functions run on AWS (Cloudflare fronts the API endpoint, so edge ≠ compute). Infra is **0.4–1.3% of projected revenue** at every scale point to 1,000 users and *shrinks* as a share; the 200K-band run used **27 of 90 DB connections**, so the DB was never the constraint. Migrating costs ~a quarter of the senior developer's year to optimise a sub-1% line. **The premise was checked and did not survive:** no credible ranking puts any cloud ahead on security, and the one large measured gap (76% / 64% / 8% of accounts with publicly exposed services) measures **how customers configured their accounts**, not the platform — an argument *against* us taking on that configuration. Rackspace isn't a cloud (MSP; ransomware 2022 + breach 2024). **The finding that settles it: none of our real security defects were cloud defects** — [[Cross-Tenant Proxy Authorization]], [[verify_jwt Is Not Authorization]], [[Payout Finalization & Re-entrancy]], [[Notification Delivery]]'s caller-chosen `href`s, [[Local/Production Boundary & Repo Joinability]] — **every one would have existed identically on Azure**. Staying is safe because Supabase is **open source** and the exit is staged: Team $599 (SOC 2/ISO 27001) → **Enterprise BYO-cloud, no app rewrite** → self-host → decompose; **Stage 2 is the one people miss**, and most who "migrate off Supabase" needed it and did Stage 4. Six numeric triggers written down; the likeliest (a customer demanding SOC 2) is answered by a **$599 subscription, not a migration**. Hiring consequence: **not a DevOps hire** — "Cloud — AWS" is technically true and would have produced exactly the wrong candidate (2026-08-20)
- [[Content Delivery State Machine]](concepts/content-delivery-state-machine.md) — 9-status flow from pending through resolved. **2026-07-23 (PR #325):** the whole machine was recorded-applied but MISSING from prod (phantom drift) — restored `transition_content_status`/`content_disputes`/triggers/the 9-value CHECK; revived auto-approval (dead 3 ways: wrong `submitted_at` anchor → `content_submitted_at`, no pg_cron job, missing RPC); closed a service-role IDOR (REVOKE); added `submitted → rejected` past max revisions
- [[Content Engine]](concepts/content-engine.md) — Live-signal loop: brief → DragonShare action → performance; Phases A+B+C+D built (loop closed + surfaced to creators, verified prod)
- [[Context Tax]](concepts/context-tax.md) — What every Claude Code session pays before reading code: CLAUDE.md + its 4 @-imports, measured at ~45,700 tok and cut ~58%% by splitting PROJECT_CONTEXT §5 (68 prose bullets, 65%% of the tax) into a one-line index + the non-loaded docs/SHIPPED_LOG.md. The mislabel ("Active Workstreams" holding shipped work) mattered more than the length because TWO instructions told every session to append there — so the fix amends BOTH generators (knowledge-sync step 4 + the always-loaded CLAUDE.md clause, the load-bearing half). Rules: move verbatim (loss is then mechanically checkable), log entries are historical snapshots while §5 is the status authority, gates must compare CONTENT not structure, and normalize CRLF BEFORE any end-anchored regex
- [[Cost Model + DAU Forecast]](concepts/cost-dau-forecast.md) — admin-only `/internal/forecast`: a pure `forecastModel` projecting infra footprint → Supabase tier → total cost → revenue → gross margin at Today/500K/750K/1M DAU, from measured load-run coefficients + 9 founder-editable `aios_dashboard_settings` assumptions; a what-if capacity/cost model (no DAU tracking exists), admin-gated (reads admin-only cost sources), AI self-caps at 15%/$250-floor; sub-project 3 of 4
- [[Creator Groups (Crews)]](concepts/creator-groups.md) — A business's standing private roster of creators; posts a campaign scoped to a crew that only active members see and one-tap apply to with no payment (free `fixed_price=0`). Private visibility via the `campaigns` SELECT chokepoint (`published AND (group_id IS NULL OR is_active_group_member)`); both apply gates (`apply_to_campaign` RPC + `can_create_application`) tightened to member+published; cross-owner targeting blocked by the `enforce_campaign_group_ownership` trigger; escrow uncoupled for free crews (paid path byte-unchanged). Gotchas: verify columns vs prod not migrations (`creator_count` is JSONB-only), `group_id` in every campaign `.select()`, saveDraft overrides, grant asymmetry on the definer helpers
- [[Creator Location Search]](concepts/creator-location-search.md) — The "near me" + radius filter on Find Creators: one `LocationFilter` model (near_me/custom/any), client-side haversine over the existing geo stack, ZIP-precise geocoding (geocoded wins over the static city centroid) with a freeform-`location` fallback; shared-header coupling with the brand page (role-neutral copy, role-aware center, brands default to no active radius); effect-sync staleness fixes (restore the center in reset/mode paths, not via the effect); county dropped
- [[Dark-Luxe App Theme]](concepts/dark-luxe-app-theme.md) — **Light app, dark marketing/entry** (final, PRs #275/#277). A force-the-whole-app-dark experiment (#269) was reverted after founder feedback (too dark / unreadable text / white patches); the working app is **LIGHT**, and dark is scoped to **landing** (self-scopes `.dark`) + **login/sign-up + onboarding** + **`/internal`** via a **`useDarkHtml()`** hook that adds `dark` to `<html>` per-route. `ThemeProvider` = `defaultTheme="light"` (NOT forced — a forced light breaks `/internal`, a Codex catch). **Keystone gotcha:** a scoped-div `.dark` leaves `<body>` white → the auth glow layers composite over white and **wash out** → `useDarkHtml` makes `<body>` dark. Durable: the two-color-system model (which made the per-file revert clean), `.dc-field`-loses-to-shadcn-`<Input>`, the dark-fill-as-text contrast trap
- [[Data Flywheel]](concepts/data-flywheel.md) — Primary competitive moat via accumulated match data
- [[DC Points Visibility Session]](raw/sessions/2026-08-07-dc-points-visibility.md) — DC Points had no destination: a bell said "+200 DC Points" and went nowhere on click, points showed on two dashboards with no explanation, and even the founder needed a SQL query to answer "what was that for." `feat/dc-points-visibility` (10 tasks, 22 commits) ships a `/rewards` page, an always-visible `DcPointsChip`, a caller-scoped `dre_my_standing()` RPC that both the page and a new Donny `rewards_agent` read from (the agent deliberately does NOT call the RPC itself — it derives identity from `auth.uid()`, null under the orchestrator's service-role client), and a notification that names its reason instead of just a number. **The product decision:** earn-only, no perk economy — a tier confers a public badge and nothing else, mirroring [[Honest Analytics]]'s stance two days earlier. Found and fixed a `donny_knowledge` scope leak (2 never-built DRE engineering specs reachable by consumer Donny via `scope IS NULL`) with a migration + a fail-loud `FORCE_INTERNAL` guard in `sync-wiki-to-donny.mjs` — necessary because the sync rebuilds `scope` on every call and would otherwise have silently reverted the migration on this very PR's own merge. Also documented, not fixed: a pre-existing stale-cached-tier possibility (a sub-5-star review lowers `avg_rating` without firing any ledger event) and that `campaign_launched` pays on every launch, not just the first (now publicly visible in the earn catalog). **State as of writing:** 3 migrations applied+verified on prod; 2 edge functions NOT deployed; PR NOT open; Codex second review NOT run (OpenAI quota until 2026-08-08 08:55) (2026-08-07)
- [[Deep-Link Param Query Race]](concepts/deep-link-param-query-race.md) — Capture deep-link URL params at mount; URL-cleanup tears down queries mid-flight
- [[Delivery Tier Selection]](concepts/delivery-tier-selection.md) — DragonDash/Express/Standard as ONE selection: the tier↔deadline derivation (forward = the date each tier's advertised turnaround can hit; reverse = the cheapest tier that can still make a chosen date), the UI-vs-DB vocabulary split (`dragondash`/`dragonrush`), local-midnight parsing, why same-day deadlines must be launchable, and the cost invariant `budgetTotal === fixed_price + delivery_fee` (what create-campaign-escrow actually charges); Known Issues = five conflicting turnaround tables
- [[Dezzy Agent (Playbook Suite)]](concepts/dezzy-agent-playbook-suite.md) — DragonCandy's company-facing growth agent (counterpart to user-facing Donny); the keystone decision that Dezzy is NOT a new runtime but a branded suite of Founder Playbooks on existing AIOS rails; v1 = the Outreach Machine (dezzy-outreach playbook + get_reactivation_targets read tool, report-only/draft-only)
- [[Dezzy Content Playbooks]](concepts/dezzy-content-playbooks.md) — Dezzy = a branded suite of AIOS Founder Playbooks (not a new runtime); the content half: dezzy-content-calendar (5 company social posts/wk) + dezzy-website-updates (changelog/announcement drafts); report-only, pure seed migration, no edit to aios-playbook-run; placeholder discipline for non-fabrication (Domains 1+2)
- [[Domain Migration (.io → .com)]](concepts/domain-migration-io-to-com.md) — Moving prod from `dragoncandy.io` to `dragoncandy.com` on an **expand → switch → redirect → contract** principle (every allow-list takes BOTH domains before traffic moves; `.io` removed last or never). **Phase 1 was not a migration — it was stopping a live breakage**: `www.dragoncandy.com` was already attached to Vercel and publicly serving while NO `.com` origin was in any allow-list, so all 82 edge functions (login, signup, payments, Donny) were CORS-blocked, the apex failed TLS on leftover GoDaddy parking IPs, and GoTrue held no `.com` redirect entry. Four copy-pasted origin lists collapsed to one module per runtime, exported as **narrow groups not a flat set** (the consumers don't trust the same hosts). Includes the `staging-login.mjs` prod-guard that would have **silently stopped protecting** the moment `.com` became prod, and why every CORS/GoTrue probe needs an **unlisted control** (both fail *open* into something that looks like success). Phase 1 gate green 2026-08-09, both viewports. **Phase 2 code shipped 2026-08-10** — apex is canonical (`www` → apex 308, path/query preserving), `SEO.tsx`'s one `SITE_URL` constant drives every canonical + `og:url`, and `DEFAULT_ORIGIN` moved after Codex caught it missing — an omission the wiki had *already flagged in writing* and nobody read. Phase 2 **config** (three secrets + GoTrue Site URL, which logs out all ~42 users) is founder-owned and outstanding; Phases 3–6 not started (2026-08-10)
- [[Donny Chat UX]](concepts/donny-chat-ux.md) — Shared chat components on two opposite-theme surfaces (light consumer / dark internal); the light-vs-dark rule (time inside bubbles, teal divider chip), expanding-textarea input, hidden-tool-row day-grouping, tray-vs-chat input split
- [[Donny Data Visibility & Quick-Action Routing]](concepts/donny-data-and-quick-actions.md) — How the consumer Donny (`donny-orchestrator`) sees a user's own campaigns/applications/collaborations/DragonShare and navigates via quick-action pills — and the two bug classes: (1) schema drift swallowed to `[]` (`campaigns.platform`→`platforms[]`, the whole DragonShare agent on dead columns/enums) → "no data/data sync issue" — verify columns vs prod + surface errors (`data_partial`), never `?? []`; (2) LLM-invented `route`s passed to `navigate()` → 404 — validate against an `isKnownRoute` allow-list (server drop + client guard) + role-aware route builders. Plus service-role tenant scoping (resolve `org_id` server-side only). **Corrected 2026-08-09 (PR #409): that guard only catches routes the LLM INVENTS.** Hardcoded paths never reach it — `/settings/billing` + `/settings/social` shipped in **12 places across 10 files** with no `/settings/*` route at all, 404ing the "Upgrade" CTA on the revenue path and the primary "Connect Outstand" nudge button, while a working guard sat beside them for two months (diagnosed 2026-06-07 and deferred as "out of scope"). Three role vocabularies coexist, so folding `brand` into the business branch **silently redirects** instead of 404ing. New `donnyRoutes.parity.test.ts` asserts the mirrors agree — directionally (server ⊆ client). Sibling of [[AI Creator Matching]]
- [[Donny RAG Scope Boundary]](concepts/donny-rag-scope-boundary.md) — Who can retrieve what from `donny_knowledge`. Consumer Donny and internal Donny read the **same table**, separated by one nullable `scope` column — and **`scope NULL` is consumer-reachable**, so the permissive value is also the default. That single fact produced a 107-page leak: `sync-wiki-to-donny.mjs` had two denylists, `EXCLUDE` (19 pages) gated behind a `SYNC_CURATE=1` flag the unattended post-merge sync **never sets**, and `FORCE_INTERNAL` (5 pages). Verified on prod 2026-08-10: **107 of 112 wiki rows consumer-reachable**, retrievable through `donny-orchestrator`'s default-scope `retrieveContext` → `agents/general.ts`, the catch-all that answers *"what is DragonCandy?"*. The dead flag was the smaller half — **a denylist fails OPEN**, holding only what someone enumerated, so the worst page was on neither list: `entities/dragoncandy-platform` states the live user count, the vendor-by-vendor burn, and that Stripe is in test mode. PR #434 **inverted the default** (every page synced `internal` unless allowlisted) and **PR #437 went further — the script now publishes only what `CONSUMER` lists, which is nothing.** The wiki is an engineering and founder notebook (*"all four streams stack on one customer"*, *"the profit engine — premium margins"*, *"MVPs over-gate"*); no page in it is written for a customer. **Nothing is lost internally:** `sync-internal-docs.mjs` mirrors every page at internal scope and `wiki-merge-pr` writes that same namespace, so the internal copy has **two** writers and the `wiki:` rows existed *only* for the consumer scope. #434's marking left **113 pages embedded twice, 109 byte-identical** — internal Donny could burn two of five RAG slots on one page — so #437 stopped sending them and pruned the duplicates (**249 → 136**; consumer-reachable **0** throughout). The sharpest number: consumer Donny's entire RAG *was* the leak, and `agents/general.ts` already degrades gracefully. **What #437 cost and how it is paid:** sending every page was self-healing (drop one from the allowlist and its row got overwritten back to internal); publishing only the allowlist strands a removed page's row at `scope null` forever — the same silent rot `EXCLUDE` had — so a **read-only** orphan check diffs the DB against the allowlist, names orphans with their real scope, prints the prune SQL and fails the run, because a rule in a comment does not hold. **Ordering rule learned the hard way:** merge the script change *before* pruning — 113 rows pruned early were re-inserted by the post-merge hook running `main`'s older script (`inserted=113` in `.git/knowledge-sync.log`). Also retired a long-standing "harmless" claim: the Windows libuv `UV_HANDLE_CLOSING` assertion **replaces the exit code** (an intended 1 seen as 127) whenever `process.exit()` follows a fetch — fixed here and on `sync-internal-docs.mjs`. Same leak class as [[Dragon Rewards Engine (DRE)]], fixed page-by-page, which is why it recurred at 107× (2026-08-10). **Corrected 2026-08-23:** its oversize-behaviour table called the internal path's 24k truncation a graceful degradation — `internal_docs` is not what Donny retrieves from, so the truncated half was the only half he could see; both paths now chunk (see [[RAG Document Chunking]])
- [[Donny Social Tools]](concepts/donny-social-tools.md) — Donny's `social_*` surface, repaired in place after **7 calls / 0 successes**. Four rules, each enforced structurally rather than by prompt: Donny never supplies or guesses a tenant id (the account is resolved server-side from the authenticated user — `status='active'`, disambiguated by **handle + platform, never ids**); never offers a capability with no implementation (7 tools → **4**); never states a cause the code didn't report; and **cannot publish** — `create_post` returns a **draft card** the owner taps, so publishing lives in client code, not in the model. The audit overturned two standing claims: instrumentation was never missing (158 rows in `donny_tool_executions`, already carrying the answer), and the cause was auth, not the fabricated `account_id` (service-role key where a **user JWT** is required → 401 before any account logic; `??` can't catch a fabricated non-empty string). Numbers gated at `MIN_POSTS_FOR_SIGNAL=3` ([[Honest Analytics]]). Three measurement traps, all caught in review, all the same shape — *a gate must be about the same thing as the claim it licenses*: `content_performance` rows are **cumulative per milestone** so summing them ~tripled totals (prod post `XDbxe`: true 1388/5, naive 4145/15); both reads were **ungated on `verified_at`**, which would have let 6 fabricated all-zero rows clear the sample-size bar; and the metrics gate counted **user-wide** posts while returning **one account's** rate, so Instagram posts could certify a YouTube rate. Also carries the **once-only guard** (CT-4b): the card is persisted and re-rendered, so a published draft used to re-arm its own button on reload and a second tap posted a duplicate. Closed with the append-only `donny_draft_publications` marker rather than an UPDATE policy on `donny_messages` — that would hand every user write access to the stored text of what Donny said, and "only `rich_cards` may change" is not expressible as an RLS policy anyway (`WITH CHECK` sees only the NEW row). Marker written AFTER the publish, so "row exists ⇒ it went out"; the card fails **closed** on anything short of a definite "not published". **§ Three more the review loop caught after the work looked finished (rounds 5–6):** the card published and never wrote `donny_scheduled_posts`, so a Donny-scheduled post was live upstream and invisible in the calendar and three widgets (both other cross-post callers write that row; this was the only path that didn't) — now built by a pure `buildDonnyScheduleRow` checked against the live CHECKs, where `threads` **fails loudly** rather than being mapped onto an allowed platform; returning null at zero accounts made the honest refusal **unreachable** (no tool offered ⇒ no call ⇒ the branch counting that population could never fire), so the bridge now reports `hasConnectedAccount` and the *caller* decides; and `fetchActiveAccounts` returned `[]` on a **failed read**, which becomes `none` and then the claim `no_social_account` — the founder's original complaint via a DB blip instead of a 401 — with the sharpest detail being that an earlier commit added the missing `if (error)` check, **still returned `[]`**, and the comment claimed the whole fix (see [[Notification Delivery]] for the same "judge a fix against its claim" bar). The distinction now lives in the return type. **Round 7** added a fourth: two branches feeding one wrapper with two different shapes — the MCP branch of `get_account_metrics` assigned the *envelope*, so the follower count `has_signal` describes sat one JSON-decode away inside a string; latent (that branch has never connected on prod) and fixed anyway, because *a config flip is not a code review*, with `unwrapMcpPayload` handling only the unambiguous single-object case since picking "the" payload among several **drops** the response where the envelope merely **nests** it. Also corrected: the CI edge-function typecheck gate covers **none** of this work — both importers are on `.typecheck-ignore` — so "66 clean" says nothing about it; a hand-run `deno check` with a `main` baseline (2 identical errors, the known supabase-js skew) stands in its place (2026-08-09)
- [[Donny Web Access]](concepts/donny-web-access.md) — "Step 2 for Donny": `web_search` + `read_url` as CLIENT tools backed by Tavily (chosen over Anthropic's server-side web_search to leave the streaming engine untouched + keep cost in the ledger); Tavily fetches server-side → NO SSRF surface; both surfaces (internal unmetered, consumer 10/day + 500/day global) metered via `donny_cost_ledger` (the ledger IS the counter — a migration widens its tier CHECK first, else inserts fail silently and caps never fire); untrusted-web-content prompt guard on both surfaces; `feat/donny-web-access` (2026-07-16)
- [[Donny-First Dashboard]](concepts/donny-first-dashboard.md) — The business dashboard body becomes Donny: greeting + what needs your attention + a prompt box + three taps, behind `DONNY_FIRST_DASHBOARD_ENABLED` (**default off**), with today's body preserved verbatim at `/dashboard/business/overview`. Bar: *"my 75 year old mom should be able to comfortably use the app as soon as they log in."* **The audit set the scope, not the mockup** — only 4 Donny tools verifiably work on prod, so the body ships **three** taps and nothing routes to `social_*` (0/7, and it falsely blames the user's connection); no stats tap, and `useDonnyQuickChips` is deliberately not rendered because it returns a "📊 Campaign stats" chip ([[Honest Analytics]]). **A tap that produces a shrug is worse than no tap.** The hard part was already built — `usePendingActions` produced dismissible capped proposals, just buried. `buildDonnyProposals()` is **pure** (injected `now`, no hooks/network) so its tests need no mocks; the location-setup blocker is returned **separately from the capped list** so three applications can't crowd out the thing blocking campaign creation; dismissal is keyed **per proposal, not per campaign** (the old key silenced "submitted content" when you dismissed "applied"). `donny_nudges`' 0-of-33 action rate is an **instrumentation failure, not a behavioural fact** — `executeAction` navigates before its UPDATE lands. **Phase B (2026-08-09) made the dashboard BE Donny** — the founder used Phase A on prod and reported the panel-opening as the defect. The blocker was not on the seven-hazard list: queries gated on `stage !== 'closed'` would render a **permanently empty** inline thread, indistinguishable from "no messages yet". A ref-counted `registerInlineConversation()` separates **panel visible** from **conversation live**, leaving `stage` byte-unchanged so four hazards dissolve; `DonnyThread` (no header, no scroll container) is shared with the panel. Four Codex rounds then found four defects of one shape — **the code claimed more than it delivered**: a half-done `remark-gfm` dependency move (`package.json` yes, `package-lock.json` no), an invisible failed first ask, a **dead "Try Again"** on the fix for it, and a follow-up typed mid-answer that vanished into a silent `return`. Four defects caught in review, all in plan-authored code, capped by a Codex P2: `campaigns.deadline` is a Postgres `date` → `new Date()` parses **UTC midnight** → `'due today'` unreachable all day, and in America/New_York tomorrow's deadline read "due today" until it vanished at 8pm (2026-08-09). **Phase 3 (2026-08-10) extends the same body to the CREATOR role** and makes the shared pieces role-generic — `DonnyHomeShell`, `useDonnyHomeConversation`, and `useDonnyHomeInteractions` (extracted when two reviewers independently found both containers carrying ~65 byte-identical lines); the two builders stay siblings because the roles rank by different rules. Creator ships **two** taps: `billing_agent` would serve a creator the **restaurant** catalog and **no** agent answers "find work". **`donny_tool_executions` cannot confirm a sub-agent tap for ANY role** — its insert is inside the `isSocialTool && mcpBridge` branch, correcting the standing reading of its emptiness. Central defect: **a gate must be about the same thing as the claim it licenses** — a lifetime `collaborationCount` gated "nothing in flight" (11 of 16 prod collabs are `completed`), so a creator who finished their work could see a **blank** attention region; the fix's own test then found the money-first merge branch **omitted the find-work item entirely**. `stripe_onboarding_complete` now has **two disagreeing readers**, resolved by copy that is true in both worlds rather than by plumbing. `DCTour` no longer spotlights a **zero-size** target (a self-hiding section leaves a full-width, zero-height anchor) — fixed at the mechanism, first `DCTour` tests in the codebase. **No per-role kill switch: merging Phase 3 IS the creator launch**
- [[Drive Artifact Delivery]](concepts/drive-artifact-delivery.md) — how a built BINARY gets from this repo to the company Drive, and what must be true before it goes. The Drive MCP cannot: `create_file` takes content **inline as base64**, so a 4 MB PDF becomes ~5.4 MB — larger than a context window (it stays right for small text docs, which it converts to Google Docs; verify by reading back, since one such upload reported `fileSize: 1` and was not empty). So **rclone** (`dcdrive` remote, 2026-08-24), chosen because everything here lives on a **shared drive** and most Drive CLIs assume My Drive — `--drive-team-drive` **and** `--drive-root-folder-id`, both required. `npm run pitch:upload` runs three guards: **the wrong build is refused by page count, never by filename** (a rename defeats a name check, and the `PITCH_NOTES` build carrying Joe's coaching is the one file that must never reach an investor); **which build it is**, from a manifest the exporter writes — the guard the first version lacked, which put the REDACTED deck on the Confidential drive under a name promising the complete one, undetectable because both builds have the same page count and every page is a JPEG; and **staleness** against an enumerated input set. Build identity is asked of the **rendered page**, not the env var (`vite build` and the exporter are separate commands), and the manifest is bound to the PDF by **md5** — a file in the same directory is not evidence about the file beside it. Uploads verified by md5 against Drive, never by rclone's exit code. **Dated hazard: rclone's shared Google client ID retires during 2026**, announced only as a one-line NOTICE — note **rclone itself is not being retired**, only the credential it lends, and every alternative needs its own credential anyway. A second transport now ships in the same command: the Drive API as a **service account**, headless (so it works in CI and on a new machine), reaching the shared drive by **membership rather than domain-wide delegation** — membership grants one drive, DWD can impersonate the domain. Dormant unless a key is configured, and **a key that is present but broken fails rather than falling back**, so a misconfigured secret cannot become a green run by a route nobody chose. **Proven end to end 2026-08-24** — account `deck-uploader@dragoncandy-workspace`, key installed, deck uploaded and REPLACED in place (one PDF in the folder, not two), md5 matching on both sides and re-read through rclone as an independent check. The order mattered: the upload was run BEFORE granting Drive membership, so the 404 branch was exercised against real Google — it reached the folder, proving the JWT signed and Google accepted the credential, and returned the instruction naming the account to add (2026-08-24)
- [[Dragon Feed]](concepts/dragon-feed.md) — The creator-content discovery surface (shared by the business + creator feed pages via `DragonFeedGrid`): mobile (<768px) renders a single-column Instagram-style `FeedPost` feed while desktop keeps the `FeedTile` grid, branched on `useIsMobile()` (a JS branch, not a CSS toggle, so only one media tree mounts). The search box drives two modes: empty → the browse media feed; a creator **name and/or a location (ZIP or city, ≥3 chars)** → an Instagram-style vertical **creator list** (`FeedCreatorList`/`FeedCreatorRow`, tap → profile) — name match global, location narrows by radius via the pure `feedCreators.ts` (`feedCreatorsFromMedia`/`highlightMatch`/`filterCreatorsByRadius`) + the controlled `useFeedCreatorSearch`, reusing the [[Creator Location Search]] geo stack. A zip is a search *trigger*, so PR #242's `useFeedLocationFilter` + `filterMediaByRadius` are deleted as superseded; two lazy-geocoding invariants carry over to the creator level (don't filter mid-geocode — no transient false-empty; skip creator geocoding under "Any" — no wasted Google quota). **2026-08-07 uplift (PR #384):** every founder complaint (no "new" signal, no tags, no filters, no views) had ONE root cause — *an item is not a row*, just a string in `portfolio_urls`, and order was a `Math.random()` reshuffle on every mount. A `feed_items` table was specced then **cut**: the item id `${creator.id}-${url}` is persisted as `analytics_events.content_id` and **parsed back apart** by `useBusinessActivity` + `useInspirationStrip`, so uuid ids would have silently emptied both surfaces; the mirror had no lifecycle contract (whole-array rewrites, so removals would never delete); and 34/34 items already carry a real `storage.objects.created_at`. So: **derive, don't duplicate** — real dates + stable newest-first order, NEW badges, skill-chip filters from the already-fetched `creator_profiles.skills`, a `loadedmetadata` duration badge, desktop attribution, and view counting instrumented-now/displayed-when-significant ([[Honest Analytics]]). Ranking uses the **server-assigned** storage timestamp, never the client-authored millis in the filename. Supply was the real bottleneck: 26 items sat behind an opt-in defaulting off in a collapsed Privacy accordion. DragonShare merge deferred — it is a cross-tenant change with no consent flag anywhere
- [[Dragon Rewards Engine (DRE)]](concepts/dragon-rewards-engine.md) — Platform-wide gamification/growth: an idempotent cron edge function (`dre-award-engine`) consumes existing events → Dragon Points ledger → 5-tier system (DP AND milestone) + tier badges; config-driven (`dre_config`, no deploy to retune), in-app-only/forward-only coalesced bell, silent backfill via a `go_live_at` sentinel, `public_dragon_tiers` view (tier-only); v1 = parent Phases 1–2, founder go-live pending. **2026-08-07: DC Points got a destination.** A `/rewards` page (balance, a full-sentence tier gap, human-labeled history, a live `dre_config`-driven earn catalog), an always-visible `DcPointsChip` in both top bars, a caller-scoped `dre_my_standing()` RPC, a notification that names its reason and links to `/rewards`, and a Donny `rewards_agent` answering strictly from the caller's own standing — all **earn-only, no perk economy**, the same honesty stance as [[Honest Analytics]]. Also closed a `donny_knowledge` scope leak that let consumer Donny retrieve two never-built DRE engineering specs. State as of writing: 3 migrations applied+verified on prod; Codex second review **clean** (3 rounds — two caught the same brand-role fallback, in the page then the Donny sub-agent); PR #378 open; the 2 edge functions are **not yet deployed** (they follow the merge, with different `--no-verify-jwt` flags)
- [[Edge-Function Deploy & Bundling]](concepts/edge-function-deploy-bundling.md) — How an edge function actually reaches prod, and the ways a deploy *looks* successful while shipping something broken, stale, or nothing. The bundle is built **at deploy time, per function**, so a `_shared` change lands only in functions redeployed after it. **Incident 2026-08-09:** Supabase's server-side bundler could no longer build a bootable worker from `esm.sh/@supabase/supabase-js` — `WORKER_ERROR` at boot on every request, unrelated to the code; isolated by a 7-version single-variable experiment (a no-import **baseline first**, or the probe proves nothing), fixed by a version-preserving swap to `npm:` across 121 files. **esm.sh was NOT broken generally** — `stripe`/`jose` boot fine, which is why the fix stayed narrow. Carries the discipline this earned: **canary one function before any fleet deploy**, verify with the anon key (an unauthenticated OPTIONS on a `verify_jwt=true` fn is rejected at the gateway *before* the worker boots), money functions last, `verify_jwt` audited against `config.toml` first, and **re-check `origin/main` immediately before a multi-function deploy** — a concurrent merge plus a stale tree is a silent revert that every health check passes (2026-08-09)
- [[Edge Function Streaming]](concepts/edge-function-streaming.md) — Keepalive NDJSON streaming to beat Supabase's 150s request idle timeout (Pro = 400s wall-clock); early first byte, pure SSE accumulator, unified callModel/runTurn, client-cancel handling, graceful version skew
- [[Error Handling Patterns]](concepts/error-handling-patterns.md) — ErrorBoundary levels, QueryClient throwOnError, async patterns
- [[Facebook Page Insights Connector]](concepts/facebook-page-insights-connector.md) — a per-user, **read-only** link to a Facebook Page; third direct connector under the *Outstand publishes, direct APIs measure* split, on `pages_show_list` + `pages_read_engagement` + `read_insights` and nothing that can post. **Deployed, not launched — there is no Facebook Page to connect**, so the flow never reaches a token exchange and the app secret is present but UNPROVEN (a mistyped secret and a forged signature both fail as 401). Shipped sending **`scope`** and was corrected to **`config_id`** hours later: this app uses Facebook Login for Business, where the two are mutually exclusive and `scope` opens a dialog requesting nothing, surfacing as *"the user declined"* — blaming the user for our bug. Needs `override_default_response_type` too, or the saved configuration's own default response type wins. The value is the three places copying [[Instagram Insights Connector]] would be WRONG: the Page token **never expires** while the *revoke* token lasts 60 days (so no refresh machinery, and disconnect can expire while reading works forever); many rows per user; and Facebook HAS a revoke endpoint, so the YouTube ordering returns — revoke before deleting the only copy of the token. One grant covers every Page, so the last-Page count is made in SQL under an advisory lock, because counting in TypeScript and acting on it strands the grant. **Repeated Instagram's ordering defect at 3.5x the blast radius** — frontend merged 70 minutes before the migration, red error card on three surfaces; the rule had been written down and nothing enforced it. (2026-08-24)
- [[Founder Playbooks]](concepts/founder-playbooks.md) — Saved repeatable internal tasks Donny runs on demand (report-only + propose); the landing spot for Loop Scout candidates; self-contained runner under the caller session JWT
- [[Help Center & Donny Guidance]](concepts/help-center-and-guidance.md) — `/help` is DB-driven (`help_articles`), and the same table grounds consumer Donny's how-to answers via `donny-orchestrator`'s `guidance_agent`. Two consumers, one column contract: `guidance.ts` had queried nonexistent columns (`content`/`search_vector`/`related_paths`) and silently returned zero articles for months — fixed 2026-07-17 to query the real schema + add a `search_vector` (trigger-maintained, since a `GENERATED to_tsvector` column is rejected as non-immutable). Categories live in TWO places (DB CHECK + `HelpCenter.tsx` CATEGORIES); "DC Points" vs "Rep" naming drift flagged. Screenshots embedded from the public `help-screenshots` bucket (PR #306, Donny strips HTML so they never reach it); the human `/help` search is **client-side** ranked (`rankHelpArticles`, `?q=` IS the state) + a sidebar Help item (PR #310)
- [[Honest Analytics and Edge Typecheck Session]](raw/sessions/2026-08-06-honest-analytics-and-edge-typecheck.md) — three items after the proxy security work, all on PR #368 because one GitHub Actions outage (~5h) blocked everything. (1) Drafts "Edit" called `onSwitchTab('compose')` and nothing else — dropped the draft AND left the row, so finishing would publish a duplicate; loading into Compose would not have fixed it either since Compose calls the provider's `createPost` and cannot be a draft editor. Codex then found the bigger pre-existing bug: **hashtags were never published at all** by either path. (2) CI type-checked **none of the 99** edge functions (`tsconfig.app.json` includes only `src`); `deno check` finds **128** errors, so the gate checks the 66 that pass and prints the 33 it skips. Fixed 3 real errors to get there — one would have **persisted `undefined` into `organizations.stripe_customer_id`**. Trap: `--node-modules-dir=auto` from the repo root makes Deno **take over the project's node_modules** and delete `@types/node`. (3) The [[Honest Analytics]] work. Codex ran 3 rounds, all findings treated as blocking, including a **literal NUL byte** that made a source file binary to git (2026-08-06)
- [[In-UI Knowledge Merge]](concepts/in-ui-knowledge-merge.md) — Review + merge wiki knowledge PRs inside /internal (wiki-merge-pr), syncing into Donny's RAG + Strategy library — no GitHub visit, no Lovable deploy; preserves the human-merge gate; unifies Save-to-knowledge, corrections, and Workspace-doc import
- [[Identity & Address Verification]](concepts/identity-verification.md) — slice 2 of the onboarding redesign: `phone_verified`/`identity_verified`/`address` get real writers. The durable lesson is that a stamp must never outlive the fact it attests to, hit **five** separate times in one slice (phone, address, identity-on-Stripe-disconnect, identity-on-Stripe-revocation, identity-on-AUTOMATIC-Stripe-detach) and closed by four mechanisms depending on whether the invalidating event was a single column change, a discrete application action, or a mis-ordered derivation — **this entry said four until the Codex second review found the fifth**, which exists precisely because the manual disconnect was fixed in isolation, so the reset became one shared `_shared/stripe-identity-reset.ts` constant: fixing instances one at a time does not close a class. Also: fail open toward the user but fail closed toward an attacker (readiness display vs. an SMS-pumping throttle, same "unknown"/error path, opposite correct default) — the throttle has since moved out of TypeScript into the atomic `reserve_phone_verification_send` RPC after a read-then-act race, and now has **no automated coverage at all** because the decision is SQL; the 4th recorded instance of a column-level `REVOKE` being a no-op against a table-wide `GRANT`; grants are the right tool only where the write surface is grep-enumerable, a `BEFORE INSERT OR UPDATE` trigger where it isn't — **and that enumeration then failed three times, all silently**, closed by a CI test that re-derives the surface quote-agnostically every run rather than by a fourth careful grep; spec compliance is not correctness (three defects, including a Stripe field `company.verification.status` that does not exist, originated in the plan/spec text and were faithfully reproduced by implementers); and from the seven-round Codex loop — a finding can be right in its conclusion and wrong in its mechanism, a green gate that does not cover your file is not evidence about your change, a destructive write needs an ANSWER rather than merely a non-error, and "unmet for effectively every account" is a finding rather than a reassurance (it appeared twice)
- [[Instagram Insights Connector]](concepts/instagram-insights-connector.md) — a per-user, **read-only** link to an Instagram account, built on the YouTube connector and interesting almost entirely for the three places where copying it would have been **wrong**. **(1)** There is no refresh token — the 60-day access token IS the credential. **(2)** So a connection nobody reads **dies**: Meta only extends a token that is still valid, making refresh-on-expiry (correct for Google) guaranteed to fail, and an expired token is recoverable only by the user re-consenting — hence proactive refresh at 15 days plus a daily sweep for dormant accounts. **(3)** There is no revoke endpoint, so `youtube-disconnect`'s revoke-first ordering would make disconnect **permanently impossible**; disconnect attempts it, reports the outcome, and deletes the row either way — safe here because that destroys our only copy of the token, where YouTube's failure mode is *keeping* a live credential. **The console gave back what the docs took away:** a **deauthorize callback** means Meta will not let us revoke a grant but will tell us when the user does — *ask each platform what it tells you, not only what it lets you do*. That callback and the data-deletion one are the only functions without a JWT, so the `signed_request` HMAC is their entire authorization (8 tests, every negative a real forgery; the HMAC covers the RAW base64url payload, because re-serialising reorders keys and fails looking like a wrong secret; a missing app secret fails **closed**). **The tests caught a fabricated zero in this build's own first draft** — `Number.isFinite(Number(x))` admits `null`, since `Number(null)` is 0 and 0 is finite, so a day Instagram said nothing about became a day with zero reach; totals still added up and only the day count betrayed it. **A defensive-looking default is the most likely place to fabricate data.** Codex found two P1s, both deployment not logic: three anonymous functions relying on a comment asking for `--no-verify-jwt` (now declared in `config.toml`), and a refresh sweep with **no cron** — a guard protecting exactly the population it was built for and nobody else. **LIVE 2026-08-24** — merged, applied, deployed, all three secrets set, and a real account connected (`@areyouaman`), with `last_synced_at` stamped 11 seconds after `connected_at`, which is what proves the analytics read reached Meta rather than a row merely being written. The forgery-rejection path is **now proven live** too: a forged `signed_request` returns **401** on both callbacks where an invented function name returns 404 — it previously 503'd before ever reaching the signature check. Three defects sat between *deployed* and *usable*, none in the connector's own code: the only Instagram button on the location settings page belonged to **Outstand**, so the founder granted the account to the other integration (fixed #502, guarded by a test that **derives** the surfaces rather than naming them); an **Unpublished** Meta app can only be authorized by accounts holding a role on it (Instagram Tester); and Meta's App settings → Basic returns its own `{"success":true}` and then **discards a multi-field write** — save one field per click and read it back, because a vendor's success flag is not evidence the value stuck.
- [[Internal Real-vs-Total Metrics]](concepts/internal-real-vs-total-metrics.md) — how the AIOS metric surfaces separate real from synthetic: Overview is real-only (`WHERE NOT is_synthetic`) with a "synthetic active" banner + "of N incl. synthetic" subs; Simulation mirrors the Overview card set 1:1 for the bot cohort; Weight is synthetic-inclusive with a `real` subcount. Synthetic = `total_all − total` (same method) via a shared pure `deriveCardModel(stats, mode)` + `PlatformMetricSections` (keyed Fragments, not divs → `first:mt-0`); `*_all` fields optional so the frontend degrades gracefully before the founder-gated `20260725150000` migration applies (`feat/internal-metrics-real-vs-total`)
- [[Internal-Only AIOS Users]](concepts/internal-only-users.md) — internal-only AIOS users (`account_scope='internal'`, PR #178) have no profiles row; AIOS tables keyed to the caller must FK `auth.users(id)` not `profiles(id)`; plus the PostgrestError-is-not-an-Error opaque-"internal error" lesson; PR #180. **The "a null profile is tolerated everywhere" invariant is aspirational** — three traps found where it did not hold: FK writes (#180), caller-profile reads (#185), and the **frontend route guards** (#357), which locked such a user out of the consumer app under a false "verify your email" notice. Includes the rule that `profile` being non-null does NOT mean the row exists (`createProfileFromMetadata` fabricates one, minus `email_verified`), and that onboarding is a *provisioning* path
- [[Investor Pitch Deck & Capital Raise]](concepts/investor-pitch-deck.md) — REBUILT 2026-08-24 on a provenance-tagged model; 15 slides in the advisor's order, ask at slide 7, pre-seed SAFE not a $3M priced seed; glossary/provenance/pending-input primitives enforced by tests
- [[iOS TestFlight First Build]](concepts/ios-testflight-first-build.md) — Getting a signed build onto iOS hardware for the first time: the origin-classification rule for `window.location.origin` inside the Capacitor shell (`capacitor://localhost` — repoint / leave-as-nav-base / gate-the-consumer-instead), the `publicOrigin()` seam + `CANONICAL_APP_ORIGIN`, trusting the native origin in the edge-function CORS allow-list (inert until a canaried redeploy — [[Edge-Function Deploy & Bundling]]), and the bundle-ID ordering rule (free before an App Store Connect record exists, frozen after). Process lessons: `supabase functions download` overwrites local source with the deployed bundle and can silently revert same-session edits; `deno install` and npm-managed `node_modules` do not coexist; a plan's hardcoded route or expected-hit-count can drift out of sync with the plan's own later decisions. **2026-08-14 — it ran on hardware:** Xcode 26.6 accepted iOS 13.0 on Capacitor 6 (the spec's biggest open risk, closed by reading the compiler invocation), Donny answering on-device proved the CORS path end to end, and the committed `Podfile` turned out to be missing both plugin pods. **Two claims on this page were falsified and struck:** the CORS widening did NOT reach the whole fleet (13 money functions still answer `.io` to a native origin), and the un-swept remainder is "cosmetic and harmless" only on the web — in `WKWebView` it is a generic fetch error indistinguishable from a broken feature
- [[Knowledge-Sync Automation]](concepts/knowledge-sync-automation.md) — npm run sync:internal/sync:wiki (no key pasted, via with-env.mjs + gitignored .env.sync.local) + auto post-merge git hook + committed installer; keeps Donny's RAG/strategy library current on docs/ merges; Windows pathToFileURL + verify-by-content gotchas
- [[Landing "Human-driven. AI-assisted." Redesign]](concepts/landing-human-driven-redesign.md) — 2026-07-18 redesign to a founder-provided mockup (PR #293), reframing DragonCandy's positioning from "AI generates your content" to **"Human-driven. AI-assisted."**: a real human creator becomes a business's social-media team, Donny assists in the background. New light (no `.dark` wrapper), additive `landing-*` Tailwind tokens + self-hosted Bricolage Grotesque/Instrument Sans/Silkscreen fonts (app's `dc-*`/Outfit untouched); static two-door hero (Business/Creator) replaces the prior role-morphing hero; the entire cinematic-video system is preserved but demoted to opt-in behind `LANDING_VIDEO_BACKDROP_ENABLED` (default off) via a new single-key `HeroVideoBackdrop.tsx`; conversion tools (brief generator, lead capture) reused byte-identical; splash/Suspense-fallback flipped dark→light to avoid a load flash. Subagent-driven (10 tasks) + whole-branch Opus review + Codex clean; 1017 tests pass. **Superseded 2026-08-22 by [[Landing Cinematic Single-CTA Redesign]], which MERGED 2026-08-23** (#459) — this line said the branch was unmerged and that this page "still describes the live site"; it no longer does.
- [[Landing Cinematic Single-CTA Redesign]](concepts/landing-cinematic-single-cta.md) — **MERGED 2026-08-23** as #459 (this entry read "**UNMERGED**, blocked on written ABB/Uncle Rocco footage permission" until the founder confirmed permission) — rebuilds the landing as one screen: fixed logo header, full-bleed video hero (**eight** curated real reels after the caption re-cut — this said ten, no user uploads), single CTA that reuses `AuthPage`'s existing role-selection step rather than asking role on the landing itself. Supersedes [[Landing "Human-driven. AI-assisted." Redesign]] outright (~20 files deleted, not hidden) and revives [[Landing Cinematic Video Redesign]]'s video system as the default page (simplified: curated-only, no per-role keys) after PR #293 had made it opt-in. Two encodes per reel by viewport orientation, crop windows hand-picked by watching each clip; 12s cap whose justification had a latent margin bug in the stall watchdog's arming logic, caught by Codex and fixed (arm on layer-active, reset on `playing`); the video-backdrop flag deleted rather than defaulted off (video *is* the page, so "off" would be a blank homepage); a whole-branch review caught a scroll bug, a white cold-load flash, and a spec-vs-plan scoping disagreement no per-task review could see. **Amended 2026-08-23:** the footer is now transparent with the footage running behind it (the white band was overruled on sight), which exposed an iOS-only white band beneath it that had been live for every page in the app — see [[Mobile Viewport & Fixed Positioning]] §8. Five of the ten reels carried burned-in captions from their original posts; three were trimmed to a clean window and two dropped, leaving eight (5 ABB / 3 Uncle Rocco). That re-cut re-brightened the library, dropping the pink and mint accents to 1.88:1 / 1.90:1 at p90 and forcing the scrim's middle stop 40% → **60%** — *trimming a clip is a contrast change, not just a length change*. Library 36 MB → 16.0 MB, iOS binary 54 MB → 39 MB; CRF moved only −31% while cutting runtime did more. **Amended again 2026-08-23 from Adrian Vella's feedback:** the single-CTA premise holds but gains two secondary ways out, neither of them a fill — an underlined "Already have an account? Log in" under the pill, and a bordered "Learn more" in the footer pointing at a **new `/how-it-works`** (the rebuild had deleted the marketing page, leaving nobody a way to read before signing up; `/pricing` answers cost and `/help` is post-signup support). The link's mint is `#B8ECDA`, **not** the slogan's `#7BE3C0`: small text needs 4.5:1 where large needs 3.0:1, and in that band (0.603–0.635 of viewport, scrim 0.672) the bright mint measures 3.91 at p90 against the pale one's 4.62 — *the "too pale for video" judgement is about headlines and inverts for small text*. A third note in the same message was a real app-wide bug, see [[Mobile Viewport & Fixed Positioning]] §9.
- [[Landing Cinematic Video Redesign]](concepts/landing-cinematic-video-redesign.md) — 2026-07-16 evolution of the Dark-Luxe landing: morphing per-role hero (R2 switcher, guarded `?role=`), a swappable `landingClips` clip-source seam (empty v1 → gradient fallback → Cloudflare Stream), `VideoSlot variant="backdrop"`, Lean-6 structure, honest Proof band; durable gotchas — Tailwind position ordering (`.relative` beats `.absolute`, later-defined wins → backdrop not full-bleed), size a tall logo by height not width, a fixed transparent header needs a scroll-fade background for legibility. **2026-07-17: the seam's predicted DragonFeed adapter shipped** (PR #268, hardened in PR #273) — a new anon `landing-clips` edge fn surfaces real boosted DragonShare video **after** the curated static clips (paid-boost curation gate; `.mov`/HEVC dropped), a `playlistSignature` remount key (index-based rotation needs content-aware keying), and a layered no-stall fix (`onError`-advance + a 15s max-dwell watchdog) so an undecodable/404/HEVC clip can't freeze the hero. **Superseded 2026-07-18 as the shipped landing design by [[Landing "Human-driven. AI-assisted." Redesign]]** — the video system lives on, now opt-in. **2026-08-22: revived (unmerged) as the default page by [[Landing Cinematic Single-CTA Redesign]]**, simplified to curated-only with no per-role keys.
- [[Landing Prerendered Shell & Performance]](concepts/landing-shell-and-performance.md) — The instant-LCP shell in index.html must be content-free (dark splash + logo) or it goes stale and flashes the OLD design on every load (not a cache/SW bug); + landing perf patterns to stay off the mobile-WebKit crash threshold (one shared IntersectionObserver Reveal not per-element Framer Motion, no infinite animation on blur-3xl layers, code-split with a DARK Suspense fallback, in-view-gated autoplay) and the matchMedia legacy addListener-fallback gotcha
- [[Landing Redesign & Public Lead Capture]](concepts/landing-lead-capture.md) — Dark-Luxe landing rebuild: scoped `.dark` wrapper (redefines dark CSS vars for the subtree only, never leaks into the app) + Radix-portal/literal caveats; Reveal/MediaSlot/VideoSlot primitives (Nano Banana Pro-ready placeholder slots); and a public lead-capture pipeline (closed-anon-DML `leads` table + service-role `capture-lead` edge fn, honeypot + fail-open IP throttle, Resend notify)
- [[Legal Entity Identity]](concepts/legal-entity-identity.md) — Dragon Candy LLC (Hoboken NJ) and where it is published: one `src/lib/legalEntity.ts` constant feeding the landing footer (city-only, discreet) and the Terms + Privacy pages (entity + **full** postal address, which is conventional there). Two rules that outlive the Apple enrollment that prompted it: (1) **the IRS and D&B records disagree** — the EIN letter has no floor line, D&B has "5th Floor" — and the site must publish the **D&B** form because that is what Apple's D-U-N-S verification matches, so a well-meaning "correction" to the IRS form breaks the only match that matters; (2) **a governing-law clause is not a state-of-formation claim** — an unproven "a New Jersey limited liability company" shipped into the Terms and was removed after the IRS CP 575 B turned out to attest the name, the LLC status and a *mailing* address but never where the LLC was formed. D-U-N-S, phone and EIN deliberately unpublished
- [[Light-App Kit]](concepts/light-app-kit.md) — The shared light-app primitive kit (`src/components/app/`): `PageBody` (page wrapper — kills double-padding + spacing drift), `AppCard` (one canonical teal-bordered card; `emphasis`/`inset` variants), `AppChip` + `AppStatusBadge` (de-grayed filters/badges), `dc-secondary` button variant. **Adopting the kit fixes consistency AND de-grays at the source.** Gotchas: `AppChip` is a `<button>` → use `AppStatusBadge` (span) inside clickable cards (invalid nested buttons); wrap a shadcn `Card` with `<AppCard className="p-0">` (don't double-pad); watch for invisible `text-white` leftovers on white. De-gray = surfaces/badges only (gray secondary *text* is fine). Third gotcha (Phase 2): `AppCard` is not `forwardRef` — a card needing a `ref` keeps it on a plain `<div>`. Phase 1 = dashboards + campaigns + browse (PR #280); Phase 2 = messaging + DragonShare + public profiles (PR #282); Phase 3 = settings + promotions + org/billing/payments (PR #285); Phase 4 = Outstand, the final surface group (PR #288); then a cross-app backgrounds + off-brand-accents cleanup (PR #289, [[De-gray Backgrounds & Off-Brand Accents]]) — palette also covers `bg-muted` panels (not shadcn `ui/*`). **The entire light app is now on the kit.**
- [[Live DB Health]](concepts/live-db-health.md) — live "Database health" section on `/internal/weight` (retitled "Weight & health"): a `SECURITY DEFINER` `aios_db_health()` live `pg_stat` read (connections vs pool ceiling, call-weighted mean query time — not p95, cache-hit + cumulative xact counters, DB size) polled every 20s + a connection-headroom scale-up alert (warn ≥70% / critical ≥85%, with the pooler caveat); internal-gated + aggregate-only, no new table/secret, CPU/RAM a labeled seam; sub-project 2 of 4
- [[Living Synthetic Marketplace]](concepts/living-synthetic-marketplace.md) — Sub-project A: a persistent, browsable `botmk_` synthetic cohort on prod built through real RLS-enforced flows (US-diverse full profiles excl. social, free campaigns, content, messaging, discounts, reviews, multi-location, CGC); excluded from founder metrics via `is_synthetic`; public free campaigns explicitly status-flipped (accept RPC only auto-activates crew); synthetic org campaign-limit raised; ONE-SHOT cohort-cap + freshness guard (recovery = `marketplace-purge`, mirrors bulk-seed); FK-graph-grounded `purge_synthetic_marketplace_cohort()` teardown (live + verified); `marketplace-seed.yml` workflow. **PURGED from prod 2026-07-30 — prod is real-only** (`SYNTHETIC_BOTS_ENABLED` false; verified 2026-08-02: `synthetic_users`=0, 42 users all real); the engine is retained — restore = flip `SYNTHETIC_BOTS_ENABLED` back true (the harness is fail-closed without it), THEN dispatch `marketplace-seed` (`seed_synthetic_marketplace_depth` is the inert browse-only depth pool, for scaling after, not restoring). It HAD run at 2,000 profiles (500 business / 1,500 creator, 24 US cities; PRs #339–#342) — the **active/depth split** is what made that reachable: ~24 interactive bots ran real flows, ~1,976 depth profiles are bulk-inserted and never authenticate, because a session costs an auth and prod rate-limits those ~25/IP. Segregation held byte-identically across the scale-up; scaling further = direct `seed_synthetic_marketplace_depth` calls
- [[Local/Production Boundary & Repo Joinability]](concepts/local-prod-boundary.md) — for the whole life of the project `npm run dev` connected to the **production** database, and it surfaced not from a bug but from writing the hiring plan: auditing whether the repo was ready for staff answered *no*. Two independent causes both pointed at prod — **`.env` is tracked** (listed in `.gitignore` since, but `.gitignore` does not untrack an already-tracked file, so it ships in every clone) and `client.ts` **fell back to prod** when the var was unset. Closed with a dev-only guard **proven absent from `dist/`** (gated on `import.meta.env.DEV`, so Vite dead-code-eliminates it; `MODE!=='test'` or it breaks all 243 test files; escapable per-session via `VITE_ALLOW_PROD_FROM_LOCAL`). **Its first version failed OPEN** — Codex P1: a string comparison let `…supabase.co/` with a trailing slash through, as did casing, `:443`, a path, a query and a fragment. **A safety control that fails open is worse than none, because it also manufactures the belief the hazard is handled.** Now compares hostnames in a separately tested module (`src/lib/supabaseEnvGuard.ts`, 19 tests) that **fails closed on unparseable input** and matches host equality not substring (`…dybdhguqvx.supabase.co` and `….supabase.co.evil.test` are different projects). Extracted out of `client.ts` because that file still carries a Lovable "auto-generated" header — a regeneration would silently drop the guard. Same audit: no `CONTRIBUTING.md`/architecture map/first-week guide, a README describing a product that does not exist, a dead `product-roadmap.md` (0/72), and no Node pin (Node 26 ⇒ 50 red tests CI passes). **"Joinable" is invisible to whoever already knows the answers.** Still open: `.env` remains tracked (untracking needs Vercel-scope confirmation first) and the committed staging password is unrotated (2026-08-19)
- [[Loop Memory Protocol]](concepts/loop-memory-protocol.md) — Two-zone MEMORY.md (curated Lessons read-first + append-only Run Log) co-located per loop skill; the loop self-improves across runs; Output is a pointer to existing artifacts, not a duplicate; reuses the validator verdict block as the failure feed
- [[Media List From Our Own Table Session]](raw/sessions/2026-08-07-media-list-from-our-own-table.md) — closing the `/media` pagination limit recorded (not fixed) when media was first scoped. `GET /media` paginates the ORG pool, so ownership can only be applied AFTER the page is chosen — filtering one page gave callers an EMPTY gallery, and scanning until the window filled left media beyond the cap PERMANENTLY unreachable. **Five successive Codex findings across those two attempts were the signal the design was wrong, not the code.** Fixed by removing the org read entirely: `POST /media/{id}/confirm` returns the provider's full record, so the confirm step caches it and the list is served from Postgres — window correct by construction, exact total, and the cross-tenant leak class becomes UNREACHABLE rather than handled. SDK contract traps that each would have shipped a broken gallery: `pagination.count` is consumed as the TOTAL (opposite of the provider's own convention), components read `contentType` while the wire is `content_type`, and omitting `pagination` reports a populated gallery as 0 files. The migration STOPS if pre-existing bindings would be stranded rather than trusting that they were zero when I looked. Also fixed a pre-existing bug found by chasing a finding that did NOT hold: `social-proxy`'s adapter had been uploading with **no MIME type at all** (2026-08-07)
- [[Media Scoping and Schedule Completion Session]](raw/sessions/2026-08-07-media-scoping-and-schedule-completion.md) — four queued items after PR #368. **`/media` was unscoped for every method and every authenticated caller** — list every tenant's uploads, DELETE any of them — closed with an `outstand_media_ownership` binding mirroring the post one, STRICT because `GET /media` was `count:0` so there was no legacy population to strand. Delegated posting hidden behind a flag (offered, could never publish, zero rows ever granted). The `posting_schedule_status = 'completed'` transition finally written, at the webhook choke point. **The reviews mostly caught MY mistakes, all treated as blocking:** I reintroduced a cross-tenant WRITE (campaign ids from the client-writable `metadata->>outstand_post_id`; `confirm-posting-schedule` had already been hardened against that exact write); I broke every media upload (kept `contentType` from the SDK's .d.ts when the WIRE field is `content_type`); I wrote a comment asserting the OPPOSITE of the truth about offset paging; and my unbounded safety net could have blown the run budget it was protecting. The edge-typecheck gate from the previous branch caught one before review — first time it paid for itself (2026-08-07)
- [[Migration Replay Drift]](concepts/migration-replay-drift.md) — Prod schema diverged from migration files; 7 defect classes when replaying onto a clean DB
- [[Mobile Viewport & Fixed Positioning]](concepts/mobile-viewport-fixed-positioning.md) — Keeping bottom-anchored mobile UI on-screen: the containing-block trap (a transform on PageTransition — incl. framer's first-load stall at `initial` — pins every `position:fixed` descendant to page content, not the viewport; wrapper is now opacity-only BY CONTRACT; portal-to-body stays the overlay defense), iOS `dvh`-not-`vh` + `env(safe-area-inset-bottom)` for bottom sheets/footers (**this used to say "this app's document never scrolls so Safari toolbars never collapse" — false, and it is what hid §9**), and the hide-on-scroll nav's 80px bottom-reveal floor; + the fixed-probe diagnostic; §6: app chrome sits at `z-40` BELOW the Radix modal layer (`z-50`) so bottom-sheet buttons aren't hidden behind the nav, and non-modal in-page bottom bars clear the nav with `6rem+env(safe-area-inset-bottom)` (PR #297); §7: the mirror rule for the TOP — `viewport-fit=cover` puts the viewport under the notch, so `fixed top-0` and page-chrome `sticky top-0` need `env(safe-area-inset-top)` while in-page section headers must NOT get it (`sticky top-0` means "top of my scroll container", not "top of the viewport"), and a viewport that moves by breakpoint scopes its inset to the breakpoint where it is actually on top. Found only on the first physical-device build — mobile Safari's URL bar hides this class of defect completely (2026-08-14); **§8: in the iOS shell `contentInset` must be `'never'`** — with `'always'` WebKit shrinks `documentElement.clientHeight` by the top safe-area inset (840 vs **778**, inset 62, measured in a real WKWebView) while `innerHeight`/`100vh`/`100dvh` keep reporting the full height, so `AppShell`'s `h-screen` overhangs the document box and the webview's white background shows through beneath it. Live for **every page in the app** and invisible only because every other surface is also white — *a defect hidden by a coincidence of palette is still a defect*. **§9 (2026-08-23): `body` — not `<html>`, not `<main>` — is the document's scroll container, and `AppShell` must be `h-[100dvh]`.** `body{height:100%;overflow-x:hidden}` computes `overflow-y` to `auto`, so body is a fixed-height scroll box; at `h-screen` the shell stood ~60–90px over it on iOS Safari, body scrolled by that, and scrolling collapsed the URL bar → grew `100dvh` → resized the page mid-gesture. Reported from a real phone as "the screen jumps if I scroll up or down". **This entry previously recorded the opposite** — a "paired refutation" that the mismatch produces no scroll on mobile Safari, and that the one-word shell fix would regress every short dashboard page. Both wrong: the refutation measured `main` (not the scroller) in an emulator (where `100vh === 100dvh`, so the gap is structurally zero), and the regression is real but is fixed by moving `DashboardLayout` too rather than by not fixing the shell. Forced-overflow control: body 833/753 → `scrollTop` 80, while html/#root/shell/main all read overflow 0 and `window.scrollY` stays 0. The surviving lesson is sharper than the old one: **when a probe returns zero, prove the probe could have returned non-zero** (2026-08-23). **§11 (2026-08-24) is the one to read first: `body` must be sized in the SAME UNIT as the shell.** `html, body { height: 100% }` resolves against the initial containing block — on iOS Safari the SMALL viewport — while the shell's `100dvh` is the CURRENT one and GROWS as Safari collapses its toolbars, so the shell outgrows body's box, body scrolls by the difference, and the strip below paints body's WHITE. **§9 had already measured the disagreement** (body `clientHeight` 753 vs `100vh` 833), fixed the *shell's* unit and left the other side on `%` — *a height comparison has two sides, and fixing one is not fixing it*. Closed by dvh on html/body + a landing-scoped document lock (`main` left scrollable so the CTA can never be clipped) + a **height** breakpoint `short:` for landscape, which genuinely did not fit (355px needed vs ~310). **Confirmed on a real phone**, the only instrument that could — Chrome, emulation and the WKWebView all lack a collapsing toolbar, so ICB === dvh and the gap is structurally zero (third such defect: §8, §9, §11). Two durable lessons: **position rules out cause faster than any probe** (the white sitting BELOW the app shell eliminated every in-page mechanism at once), and **a true fact can confirm a false diagnosis** — the simulator's proof that WebKit *applies* `overscroll-behavior` answered a question nobody had asked, since *applied* was never *suppressed*. **§10 (2026-08-24), now marked in place as a WRONG diagnosis that shipped: a page with nothing to scroll can still be DRAGGED, and the gutter it opens is white.** §9's own "still open" clause named the mechanism — rubber-band overscroll — and it was right about the mechanism and wrong about the surface: a macOS trackpad bounces too, so the follow-up report covered **both** viewports where the previous bug was iOS-only. The elastic strip sits **outside the body box**, so nothing under `#root` can paint it — the canvas reads its background off `<html>`, falling back to `<body>` (white) — which is how the one dark cinematic page opened a white band above the header and below the footer. Two guards, because they fail differently: `overscroll-behavior-y: none` on html+body (**Y only** — the shorthand takes X, where iOS edge-swipe-back lives; cost: Android Chrome pull-to-refresh), plus the landing painting the canvas grape for its lifetime, which covers what the property cannot reach (Safari <16, and the WKWebView, whose bounce is a **native** scroll-view setting). Proven in the iOS simulator with a throwaway computed-style readout: WebKit **does** apply the property in a WKWebView, `innerHeight === clientHeight` (874, so §8 still holds), body overflow 0 — and, recorded as explicitly unproven, that *applied* is not *suppressed*, which needs a real drag no agent could synthesise
- [[Musk's Algorithm]](concepts/musks-algorithm.md) — Question → Delete → Simplify → Accelerate → Automate
- [[Nav Active State]](concepts/nav-active-state.md) — Which nav item renders as current, via the one pure `activeNavHref(pathname, hrefs)` shared by the desktop sidebar + mobile bottom nav + drawer. **Longest match wins**: the old per-item `pathname === href || startsWith(href + '/')` lit each role's bare-root "Dashboard" item on ALL ~26 of that role's child routes (reported as a "double-clicked button" on Dragon Feed). Exact matching would be the opposite regression — "My Campaigns" must stay lit on `/campaigns/:id` — so the requirement is *specificity*, not equality; it is also self-maintaining (no `exact` flags to rot). Two reusable lessons: a parent-also-highlighted bug is a specificity bug, and the same expression in three files is one missing helper, not three bugs. Known: the Analytics item points at `/dashboard/analytics` for all roles while `/dashboard/brand/analytics` exists (flagged, not fixed)
- [[Notification Delivery]](concepts/notification-delivery.md) — create-notification is the choke point for cross-user notices (bell + service-key email); send-notification-email's self-only gate 403s frontend cross-user sends; category drives default email; template/button conventions. **§ Who may notify whom (2026-08-08):** the choke point itself had NO gate — it authenticated the caller and discarded the user object, so any authenticated user could notify anyone as anyone, and email them; the page had documented `emailType ?? map[type]` ("target **any** template") as a feature. Now JWT-derived actor + `can_notify_user` (six live relationships; derived by backtest AND call-site enumeration, since sponsorship had never fired on prod) + server-composed copy for `content_liked` + template choice bound to `emailType === type`, with `file_uploaded`'s role variant derived from the collaboration
- [[Outstand Proxy Cross-Tenant Authz Session]](raw/sessions/2026-08-06-outstand-proxy-cross-tenant-authz.md) — three pre-existing cross-tenant authorization holes in `outstand-proxy` closed (PR #368, open, blocked by a GitHub Actions **major outage** not by review). Surfaced while reviewing [[Social Measurement Spine]]; worse than the read hole #366 closed, because the provider key is org-wide. The list leak was **settled by one captured request**, not argued: `GET /posts` as a single-account user returned `{posts:[5], data:[1], pagination:{total:49}}` — `data` filtered correctly while `posts` carried 4 other-tenant posts with live Instagram permalinks. Our own `reconcile-social-posts` had already learned to read `body.posts` first. Two pure tested modules (`outstand-post-authz.ts`, `outstand-list-filter.ts`) because `index.ts` calls `serve()` at load; the platform fallback deleted **with its helpers** so it can't return via an argument change. Third commit closed the review's three real findings (incl. a claim guard that failed open exactly when 2+ tenants already claimed an id — a fix `social-proxy` already had) and **dismissed two with evidence** (grant 1 is not structurally empty — `GET /posts/{id}` returns `post.socialAccounts[].id`; the SDK types `accounts` as `string[]`). Codex clean twice; 1240 tests. **NOT deployed** (2026-08-06)
- [[Patch-Based Corrections]](concepts/patch-based-corrections.md) — Donny corrects a strategy doc via small find/replace edits; the server reconstructs the full doc (downstream unchanged), cutting the ~130s correction turn to seconds; PRs #151/#152
- [[Payments Split by Surface]](concepts/payments-split-by-surface.md) — Stripe for marketplace on all surfaces; subscriptions web-only to avoid Apple's 30%
- [[Payout Finalization & Re-entrancy]](concepts/payout-finalization-consistency.md) — making `release-creator-payout` durably re-entrant: a per-collaboration marker (`payout_executed_at`/`stripe_transfer_id`) set AFTER money moves (never a pre-claim → no marked-not-paid) is the re-entry guard; the pending path credits + marks atomically via `credit_pending_balance_for_payout`; finalize failures safely surface for retry + a 15-min reconciliation sweep. **The wallet-first fix is fully shipped:** stage 1 (durable `pending_balance_flushes` ledger → exactly-once flush, closing the identical-cents under-pay) and **stage 2** (the reroute — one money path: atomic credit+marker → best-effort exactly-once flush → finalize) which **closed both cross-path residuals** (concurrent double-pay; Stripe-up/DB-down split-brain) by construction and reconciled the 3 frontend money readers to one `metadata.type`-keyed rule. PRs #328, #329 + `feat/wallet-first-payout` + `feat/wallet-first-stage2`
- [[Pricing Architecture]](concepts/pricing-architecture.md) — Four stacked revenue streams per customer
- [[QA CI/CD Gate]](concepts/qa-cicd-gate.md) — Automated quality gate + staging env between code and prod; human ship gate; Plans A/B/C
- [[RAG Document Chunking]](concepts/rag-document-chunking.md) — How a repo document becomes rows in `donny_knowledge`. `sync-internal-docs.mjs` sliced every document at **24,000 chars**; the comment beside it said "embed input is truncated; full_content is not" — true, and about the **wrong consumer**, since `full_content` goes to `internal_docs` and `donny-orchestrator/rag.ts` returns `donny_knowledge.content` on both its paths and never reads `internal_docs`. Measured on prod 2026-08-23: **142 documents, 2,168,995 chars, 723,128 (33%) reaching Donny in no form at all**, 14 rows pinned at exactly 24,000, in place since 2026-06-11. `DESIGN_SYSTEM.md` stopped mid-sentence inside the safe-area rule, so every design rule after it — including three written that morning — was absent. **Silent in every signal the run produced** (`updated=142 errors=0`, `updated_at` moved); found only by following [[Knowledge-Sync Automation]]'s own rule to verify by CONTENT. Now chunked at ~6k on heading boundaries, **chunk 0 keeping the unsuffixed `source_id`** so no existing row is orphaned, with `chunk_base` for exact sibling lookup and deletion of siblings past the new total when a document shrinks. Chunking is **cheaper to read** too — one retrieval could previously push 120,000 chars into the prompt — so `search_internal_knowledge` went 5 → 10 rows and still sends less text. `SHIPPED_LOG.md` (505k, a quarter of the corpus) is **excluded and printed**, not truncated in silence. Chunking runs **server-side** because there are TWO producers: `wiki-merge-pr`'s `_shared/wiki-sync-payload.ts` carries the invariant *in its own header* and still broke it — script-side chunking would have served a truncated head spliced onto a stale tail (2026-08-23)
- [[RAG Retrieval Evaluation]](concepts/rag-retrieval-evaluation.md) — whether internal Donny actually **finds** the right thing, as distinct from whether the text is in the database. `npm run eval:rag`, against **53 real queries** lifted from `donny_tool_executions` — every internal search Donny has ever run — not questions invented for the occasion. Three measurements in decreasing order of trustworthiness: **controls first** (8 out-of-corpus queries — sourdough, Byzantine iconoclasm, oboe reeds — scoring 0.164–0.280 against real queries' 0.437–0.632, **0 of 8 above even the weakest real query**, reported as an overlap COUNT because two distributions can differ in mean and still overlap completely); **tail reachability**, which needs no judge at all (**12.3%** of k=10 hits are text past the old 24,000-char cut, **32/53** queries surface at least one); and **recall/precision** over hand-labelled relevance, where an unjudged document is reported as unjudged rather than silently counted a miss — *treating unlabelled as irrelevant is how a retrieval change is made to look better than it is, since the newly-reachable documents are exactly the unjudged ones*. Findings: chunking did **not** break what worked (old top document is still top for **43/53**, none fell out of top-10), and **k=10 stays** (recall **65% at k=5 vs 91% at k=10** — dropping to 5 loses more than a third of the relevant material), replacing the arithmetic guess the row count was originally set by. **Three method failures worth more than the results:** the label-free way of choosing k *failed outright* (similarity is still 0.404 at rank 20 against a 0.280 control ceiling — in a corpus all about one company there is no cutoff to find); the first judging pass **truncated the evidence while measuring a truncation bug** (22 of 84 passages marked irrelevant had the query term past the 340-char excerpt shown, moving precision@12 from 32% to 42% once corrected); and **the metric itself counted distinct documents where production returns chunks**, crediting results Donny never receives — with a unit test pinning the error in as many words (*"ranks documents, not chunks"*), which moved recall@10 from 100% to 91% once fixed. **A judge sees what you show it**, and measure what the consumer actually receives. **Now runs unattended (2026-08-23):** a per-PR test pins `k` and `TARGET_CHARS` to the values this measured — `k` is the named constant `INTERNAL_RETRIEVAL_K` and the test asserts the call site uses it, since a pin holding a value nothing reads is worse than no pin because it looks green — and a monthly workflow re-runs the evaluation against a committed `baseline.json`, filing an AIOS finding only when a metric moves past its tolerance. **Comparability is checked before anything is compared, per metric, and by IDENTITY rather than count**: a changed query set makes nothing comparable while a changed *label* set moves only the recall denominators, so the control check still runs — and because counting lets one query be swapped for another with every count intact, the baseline records order-independent hashes of the query and label sets (Codex, round 1). **Two kinds of silence are themselves findings** — *not comparable*, and a configured threshold that did not run; either reads exactly like a clean month, which is this pipeline's own defect one level up. The job never re-records its own baseline (a guard that follows the observed value is a thermometer reporting room temperature), every finding carries the 7-of-53 coverage line, and benchmark drift against live `donny_tool_executions` queries is reported and never acted on (2026-08-23)
- [[Reading Agent Traces]](concepts/reading-agent-traces.md) — the 4th loop-stack layer: audit the agent layer from Claude Code's own session JSONL (`~/.claude/projects/<slug>/`) instead of inferring health from outputs, since every scheduled routine files nothing on a clean run ("last output ≠ last run"). The global `read-the-traces` skill (zero-dep streamed scanner + 4 deterministic gates + the [[Validator Skills]] verdict block); extraction gotchas (correlate `tool_use`→`tool_result` by `tool_use_id` or every error attributes to "?"; carry skill attribution across the `--days` boundary; mtime-selects-files ≠ honouring the window; a worktree has its own trace dir; redact BOTH `ghp_` and `github_pat_`). Keystone trap: **a supabase-js `.then(ok,fail)` hides Postgrest errors** (the builder resolves, not rejects) — how `donny_tool_executions` stayed empty while reading as "clean". Also records the tool's own retracted false positives: a hook that BLOCKS is not a hook that FAILED (a denial surfaces with an "error" prefix — reading it as a fault inverts a gate failing *closed* into one failing *open*), and attribution must ride the `tool_use_id`, never a last-skill-seen heuristic (which fabricated a 68% error rate for a git-only skill from Chrome timeouts). **An observability tool that misclassifies is worse than none**; PR #292 (2026-07-18)
- [[Schedule Agenda View]](concepts/schedule-agenda-view.md) — Agenda-first scheduling: mobile + desktop default to one scrolling day-list (one ＋Schedule button, always-visible Today, tap-month jump = Sheet on mobile/Popover on desktop via useIsMobile); grids kept as optional toggle (drag intact) + readable Month chips; a pure AgendaItem model normalizes Outstand posts + campaign deadlines + sponsorships into one AgendaView; fixed the standalone /calendar ＋Schedule no-op and the review-panel dead-end; "simplest = the default path, not deleting options"
- [[SECURITY DEFINER Advisor Triage]](concepts/security-definer-advisor-triage.md) — 3-signal method (frontend .rpc? / referenced in an RLS policy? / returns trigger?) to classify Supabase definer-function security advisors into keep-by-design vs safe-to-revoke; 2026-06-24 snapshot 43 keep / 32 revoke-safe; DELIBERATELY DEFERRED pre-launch (too risky), no changes made
- [[Self-Improving App]](concepts/self-improving-app.md) — Autoresearch loop (Karpathy pattern, domain-swapped) that grows the wiki + Donny; 5-phase smart-app roadmap; the 4-Condition Test, knowledge-freshness self-heal, and the monthly Loop Scout
- [[Service-Role Data Exposure]](concepts/service-role-data-exposure.md) — DragonCandy's most expensive recurring Codex P1 class + the `data-exposure-reviewer` subagent built for it. A service-role client **bypasses RLS entirely** (86 of 90 edge fns build one, + 4 `_shared` modules that inherit into every importer), so any read reaching a client or an LLM must **re-assert the filter in the query**. These defects **run perfectly** — which is why `edge-function-reviewer` ("will it deploy and run?") passed a service-role IDOR: **a buried checkbox is not a specialty**. The 3-reviewer boundary: `verify-db-schema` = does RLS *permit* the real caller; `edge-function-reviewer` = does it run; this = does it *exclude* everyone else. 8 checks (visibility on every branch AND both sibling tables; ownership on body/LLM-supplied ids; field-level PII; server-derived tenant ids; `invitation_status='active'`; new-scope→pre-existing-fan-out audit; no `select('*')`; three-role definer revoke). Gotchas: the changed-file list is a **TRIGGER SET, not a READ SET** (opposite of the sibling agent); a new `.claude/agents/*.md` needs a session reload; replaying a squash-merge needs reconstruction not `<sha>^`; **a suite can appear to cover a capability while exercising it only via an unrelated precondition** **Remediated + deployed in PR #308** ([[Service-Role Remediation Session]]): 12 guards + `_shared/campaign-access.ts`. Running the reviewer on its own fix found MORE than the original six (incl. a cross-tenant *write*), and **Codex then caught one all three of its rounds missed** — applications are not collaborations under the `campaigns` SELECT policy. Also records the two **functional regressions** the remediation introduced: a security fix that breaks the feature is its own failure mode. **Its own open finding is now closed** ([[Counter-Offer Authorization Session]], `fix/counter-offer-authz`): the zero-authz, anon-executable `create_counter_offer` definer RPC got identity + participant + role-integrity guards (server-derived role closes the forge-then-self-accept escalation), an anon revoke + explicit authenticated/service_role grant, a pinned `sender_role` INSERT RLS policy, and a pinned `RETURNING` list — verified red→green live; Codex's "authenticated loses EXECUTE" P1 verified empirically false (direct default-privilege grant). **A second instance closed 2026-08-08 by deletion** — `donny-dragonshare-score` validated its caller then never used it, so a body `post_id` reached a service-role read *and write* of any tenant's post with the audit row stamped to the **victim**, and `matchQuality` + a plaintext `creatorPostCount` made the target org's boost count solvable; it was the hole in the DB's own guard (`trg_ds_posts_block_self_verify` blocks authenticated non-admins from those exact columns, then waves through the service role). Deleted not patched — zero callers, webhook never wired, `score_events=0` on prod; **undeployed and verified closed 2026-08-08** (`get_edge_function` → not found, live POST → 404) after Codex raised source-deletion-alone as a `[P1]` and proposed a tombstone — rejected because nothing auto-deploys functions here, so a tombstone needs the same manual deploy while institutionalizing the orphaned-endpoint anti-pattern. Confirming its sibling lead (`landing-clips` "orphaned" — **false**, it has a consumer behind a deliberately-off flag) found a real defect: both its media URLs are creator-writable free text, now origin-pinned in the query *and* in `buildClips`. Ends on the transferable lessons: **deleting source is not undeploying**, so audit the *deployed* function list against the repo; and "mirrored in X too — belt and suspenders" decays into a single point of failure the moment one belt is dropped, because the survivor still produces correct output. **4th column-REVOKE-no-op instance + a new open IDOR, found 2026-08-23** ([[Identity & Address Verification]]): closed the `profiles.email`/`.phone` table-wide-SELECT exposure the same way as the prior three instances (table revoke, then column grant-back); found but left OUT of scope a live, unauthenticated `get_user_conversations` IDOR (verified with controls — anon with no JWT reads 13 rows belonging to other users), needing an owner.
- [[Site Access Lockdown (Private Preview)]](concepts/site-access-lockdown.md) — putting dragoncandy.com behind one password without breaking the flows that have to keep working. **Built, reviewed and merged; NOT live** — none of the four Vercel variables is set. The premise that shapes everything: **a password cannot stop a signup** — the anon key ships in the bundle and `supabase.co` never traverses Vercel, so closing Supabase signup is the load-bearing control and the edge password is the lesser of the two. Answers **401, never a redirect**, because a 401 re-requests the identical URL and a password-reset link's `#access_token` fragment survives. Fails closed, so "delete the variables" is the WRONG rollback and `SITE_GATE_ENABLED` exists instead (and needs a **redeploy** — an env-var change does not reach a running deployment). **The claim that this could not be tested before production was FALSE and it hid a total outage**: the gate's *behaviour* is production-only (a preview passes everyone) but Vercel **imports and runs the middleware on every preview request**, and the first preview 500'd on EVERY request — Vercel transpiles to `middleware.js` and runs it as Node ESM **without bundling**, and Node's ESM resolver does not add extensions, so an extensionless import threw `ERR_MODULE_NOT_FOUND` before any gate logic (so `SITE_GATE_ENABLED` could not have rescued it). Typecheck, 2,757 tests and the build were all green because Vite/Vitest/tsc resolve extensionless specifiers; **the e2e smoke suite caught it** by driving a real browser. Durable rule: **a local toolchain that resolves imports for you cannot tell you whether the deployment target will.** Four more found in review: **only allowlist a path with a real file under `public/`** (`/.well-known/*` had none, and `vercel.json` rewrites unmatched paths to `/index.html`, so it served the whole SPA bundle to anonymous browsers — same reason `/promo/:id` is documented rather than allowlisted); the Lighthouse exemption was **inert where it was written** (a category assertion reads a score computed at collect time — 0.69 vs 1.00 measured — and the obvious fix ALSO fails because an `LHCI_COLLECT__SETTINGS__*` env var replaces the config file's whole `settings` object); bare `undefined` is a **Next.js** continue convention, not the framework-agnostic one (`next()` from `@vercel/functions`); and `VERCEL_ENV !== 'production'` was **fail-open on absence**. Records that "a pass cannot carry a Set-Cookie" was asserted in five places and is false, and that a stale `vite preview` on port 8080 nearly made a local audit grade someone else's build (2026-08-23)
- [[Social Measurement Spine]](concepts/social-measurement-spine.md) — how a published post becomes a *measured* post, and the invariants keeping an absent measurement from looking like a real zero. `social_post_log` is the enumeration surface but only rows carrying **`verified_at`** are measured; `outstand-webhook` is the choke point (it sees `post.published` regardless of publish path, so coverage is structural); `reconcile-social-posts` (hourly) re-drives the same match because every path writes its schedule row *after* the provider returns — a fast webhook finds nothing, returns 200, and the provider never retries. **Ownership is server-established**: both consumers used to decide who owns a post by joining `donny_scheduled_posts` on the **client-writable** `metadata->>'outstand_post_id'` (prod: `authenticated` AND `anon` hold INSERT+UPDATE on *every* column; the policy constrains only `user_id`), so anyone could plant a row claiming any post id, get `verified_at` stamped, and have the capture job spend the **org-wide** key filing another tenant's metrics under their row — and 5-character low-entropy ids (`XDb8e`/`XDbxe`, 9 s apart, 3-char shared prefix) mean guessing beats knowing. **The same root cause surfaced four times** (a deleted Codex P1 fallback, #35, the half-closing `verified_at` gate, the reconciliation review) because every fix worked *around* a client-writable trust anchor. Closed by an `outstand_post_ownership` binding minted by `outstand-proxy`/`social-proxy` from `auth.getUser()` + the provider's own response id — **no client write path**; sweep **strict**, webhook **permissive** (legacy counted, not assumed); disagreement rejected **per row, not per post** (per-post would trade the leak for a denial-of-measurement). Traps: a column-level `REVOKE` is a **no-op** vs Supabase's ambient table GRANT; a missing table is **`PGRST205`**, not `42P01`; **unify diverging implementations to the SAFER one**; **reuse-don't-duplicate assumes the reused thing works** (amplification inherited a broken id parser and shipped a change that did nothing, past four reviews). PRs #365, #366
- [[Stakeholder Scorecard]](concepts/stakeholder-scorecard.md) — `/internal/scorecard`: a plain-language "How DragonCandy is doing" page (4 stories — traction, capital efficiency, scale headroom, revenue readiness — with auto green/amber/info signals under a founder-set headline) + a print "Export snapshot" one-pager, so non-technical stakeholders can speak to investors. Deterministic phrasing (no LLM); real-only user metrics; a small internal-gated `aios_stakeholder_burn()` aggregate RPC (SECURITY DEFINER, aggregate-only, data-exposure PASS) lets non-admin stakeholders see burn without the admin-only line items; headroom is physical/conservative infra capacity. Sub-project 4 of 4 of the AIOS scaling dashboard (`feat/internal-stakeholder-scorecard`)
- [[Strategy Library Management]](concepts/strategy-library-management.md) — Reversible soft-archive (`archived_at`/`by`/`reason`) + `is_core` Core-File protection (seeded + `BEFORE INSERT` trigger, enforced in the RPC body) + a monthly audit routine (service-role `dedup_candidate_pairs` cosine + `internal_doc_exact_dupes` via `source_hash` → dupe/conflict/orphan/bloat findings at `/internal/findings`); keystone = an archive-aware `donny-knowledge-sync` so a re-sync never resurrects an archived doc
- [[Stripe Webhook Delivery]](concepts/stripe-webhook-delivery.md) — Why the prod webhook was dead (unset secret, empty events table); platform vs Connect endpoints are separate with separate secrets → dual-secret verification; Snapshot-not-Thin, Vault≠Edge-Secrets, warm-isolate-stale-env, probe-based verification
- [[Supabase .in() Header Overflow]](concepts/supabase-in-filter-header-overflow.md) — an unbounded `.in(col, ids)` is a latent 16 KB bomb that **lies about what it is**: `.in()` serialises every value into the URL, PostgREST echoes the URI back in the `Content-Location` **response** header, and past ~400 values that blows Node/undici's 16 KB `maxHeaderSize` → `UND_ERR_HEADERS_OVERFLOW`. Being transport-layer it is **not** a `PostgrestError`, so it never reaches the `error` branch and surfaces as an opaque `TypeError: fetch failed` — it cost ~2 h of chasing Supabase network restrictions, IP bans and the status page. Measured cliff (prod REST): 250 ids = 10,475 chars OK, 400 = 16,775 OVERFLOW, 500 = 20,975 OVERFLOW; ~42 chars/email, ~39/UUID; chunk at 100. **The diagnostic that kills the connectivity theory: a successful call earlier in the SAME process** (here `bootGate`'s `readKillSwitch` ran before the failing pre-flight). Latent until `MAX_SHARDS` 10→20 doubled a cohort past the cliff — and latent *also* in `mint.ts`, where it would have broken the daily `tick` cron. Fixed in PR #345; the app's 89 `.in()` call sites are **unaudited** (2026-07-26)
- [[Synthetic Load Proof (Phase A → 200K)]](concepts/synthetic-load-proof.md) — how far the platform actually scales, proven by driving real RLS-enforced traffic at prod: **Phase A** (the ramped knee-not-outage driver, cross-tick session pool, two-lane bulk-seed, economics slice) → the **runner matrix** Slice 1 (multi-IP fan-out; writes are matrix-only so single-runner never touches the live cohort) → Slice 2 (real Range-capped-GET storage egress; the overlap-honest event-sweep `honest_peak_concurrency` + `max_concurrent_shards`) → the live **50K- and 200K-band runs**. **The result across all three: the database has never been the constraint** — 91% idle at ~312 concurrency, 83% at 1,000, **70% idle (27/90 conns, 11.40 ms) at 4,000 ≈ the 200K-DAU band**; every ceiling hit so far is client-side (per-IP egress, then per-shard latency once media became real). Cap discovery confirmed GitHub runs ≥20 shards genuinely concurrently. **Split out of [[Synthetic Weight Engine]] 2026-07-26** when the combined page crossed the RAG embedding ceiling and silently blocked 41 pages from syncing
- [[Synthetic Weight Engine]](concepts/synthetic-weight-engine.md) — Safety spine for minting synthetic ("bot") users on prod without contaminating founder metrics or the moat: registry auto-filled by extending `handle_new_user` (email is the source of truth), service-role `is_synthetic`/`is_synthetic_campaign`/`is_synthetic_org` helpers, denormalized `is_synthetic` on 5 telemetry tables, a two-sided **actor-OR-parent** exclusion in `aios_*` + `platform_weight.*_real`, `SYNTHETIC_BOTS_ENABLED` fail-closed kill switch, a live-mode payout money guard checking BOTH parties, email suppression, `get_simulation_stats`/`/internal/simulation` as the one SHOW surface, and `purge_synthetic_data` leaf-first teardown (incl. the non-cascading org rows). Proven on a rollback-wrapped 5-bot round-trip (founder metrics byte-identical, zero residue). Codex caught 4 real segregation gaps across 3 rounds — `CREATE OR REPLACE` silently reverting later `handle_new_user` migrations (diff the CURRENT prod def, not an old file), single-party predicates on multi-party tables (payout, messages), an unfiltered cost-alert. Phase 0 (spine) + Phase 1 (private-crew behavior engine) are both **built and re-runnable, but INERT since 2026-07-30** — `SYNTHETIC_BOTS_ENABLED` is `false` and every synthetic cohort was purged (the 25-bot `bot0##` crew and the 2,000-profile [[Living Synthetic Marketplace]] alike); verified 2026-08-02: `synthetic_users`=0, 42 users all real. They HAD run live from 2026-07-24 at 2,025 registered synthetic users. **The whole load-proof program — Phase A, the runner matrix Slices 1 & 2, and the live 50K/200K runs — moved to [[Synthetic Load Proof (Phase A → 200K)]] on 2026-07-26** (the combined page had crossed the RAG embedding ceiling). Measured-revenue / capped-Donny is **Phase B** (separate gated plan)
- [[Take-Rate Ladder]](concepts/take-rate-ladder.md) — Tiered platform fees from 10% (Free) to 2% (Enterprise)
- [[Test-Mode Stripe UX]](concepts/test-mode-stripe-ux.md) — One-tap test-mode payout bypass (auto-enabled Custom account) + card-only checkout; live unchanged; gated on sk_test_/pk_test_
- [[Trust-Then-Flag Model]](concepts/trust-then-flag-model.md) — DragonShare moderation: post live immediately, flag post-hoc (no admin queue)
- [[Two-Path Boost Payment]](concepts/boost-payment-two-path.md) — Hosted checkout first, off-session repeat, idempotent fulfillment
- [[TypeScript Patterns]](concepts/typescript-patterns.md) — Strict mode, type safety, export conventions
- [[Updated-At Trigger Drift]](concepts/updated-at-trigger-drift.md) — `handle_updated_at()` was a no-op STUB on prod (`-- Function logic here`) for most of the project's life: 35 triggers / 31 tables fired and changed nothing, so `updated_at` sat frozen equal to `created_at`. Repo was never wrong — same `recorded ≠ actual` drift class as [[Content Delivery State Machine]]. Surfaced only as a contradiction (RAG content fresh, timestamp stale), never as an error. **Restored 2026-08-07, PR #385**, after fixing the two consumers that had adapted to it being broken — `donny-analytics-alerts` (a frozen-column `.gte("updated_at")` is a *"created in 24h"* filter; restoring it would have silently reinterpreted it as *"modified in 24h"* and emitted "Payment released" for untouched rows) and DRE `occurred_at` (false recency clearing `go_live_at` ⇒ retroactive "You earned DC Points"; the awards were safe, the *notifications* were not). Adds `campaigns.completed_at` — the anchor whose absence the DRE migration had named in a comment years earlier. **The rule survives the fix: `updated_at` moves on ANY write, so it is a modification stamp, never a status signal** — use a narrow transition-stamped anchor (`content_submitted_at`/`payout_executed_at`/`completed_at`). Legacy `updated_at` is unreliable **both** ways — `== created_at` means "no explicit writer touched it", not "never modified", and tables with an app-level writer moved anyway (`campaign_collaborations` 10/16). **Post-merge correction:** the audit's "`campaign_collaborations` has zero explicit writers ⇒ exactly equivalent" claim was FALSE (`useProjectComplete.ts` writes it), so the repoint costs ~1-in-16 historical status alerts — still a pure narrowing, but the Codex dissent was stronger than credited. **Closed 2026-08-08 (#391)** by the anchor it pointed at: `campaigns.escrow_status_changed_at` (escrow ONLY) + `campaign_collaborations.status_changed_at` (status/content_status), each stamped transition-only. The asymmetry is deliberate and driven by the CONSUMER — Codex caught a symmetric draft that would have announced "Funds held in escrow" on a plain status change. **The test for an anchor is never "how many columns" but "does every column this stamps on produce an event the reader reports".** No backfill (an UPDATE fires handle_updated_at + can trip enforce_single_slot_campaign); `DEFAULT now()` set AFTER `ADD COLUMN` or it stamps every existing row
- [[Validator Skills]](concepts/validator-skills.md) — skills that emit a machine-readable verdict block to close generate→validate loops
- [[verify_jwt Is Not Authorization]](concepts/anon-key-is-not-authorization.md) - **The Supabase anon key IS a valid JWT and ships in the frontend bundle**, so `verify_jwt:true` (the default for any function with no `config.toml` entry) only rejects a *missing* header - it never establishes a user. A service-role function that skips `auth.getUser()` therefore answers **anyone on the internet**. Proven on prod: `dragonshare-notify` returned 401 with no header and **200 with the public anon key**. Sibling of [[Service-Role Data Exposure]] (that page = a credential bypassing RLS; this = a platform gate that only looks like an auth check). A 100-function sweep found 18 candidates: 4 legitimately public (incl. `verify-package-order-escrow`, correctly guest-reachable and bound to Stripe `metadata.order_id`), 8 authorized by a mechanism the regex missed, **6 genuinely exposed**. **The scan blind spot is the lesson: `fire-promotion-social-hook` was cleared because it calls `getUser` - and never checks ownership. Authenticating is not authorizing.** One guard does not fit: the fix follows who legitimately calls it (`isAuthorizedIngest` for service-role-only; `getUser` + record-level ownership for browser; a per-operation split for both - `dragonshare-notify` is browser-called twice, so a blanket guard would have broken submission and decline). Also records **a read gate is not a write gate** (reusing `evaluateCampaignAccess` let a rejected applicant mint signed URLs over private deliverables), unpaired-ids x2, body-supplied cost attribution poisoning the AI kill-switch ledger, and a body-supplied dollar figure in a `role:assistant` Donny message. Traps: `.typecheck-ignore` hid 4 of 6 changed functions from the CI gate (compare `deno check` counts by hand); probe reachability, never impact; **zero Toast tables exist on prod**, which downgraded two findings. **7 functions DEPLOYED and probe-verified on prod (2026-08-08), of which SIX were closed** - each of those flipped 200/404/400 to **401** with the public anon key, and a real vs. bogus campaign id now return an identical 401 (existence oracle closed). The 7th, `landing-clips`, is a legitimate anonymous endpoint and **deliberately still answers 200** - it was hardened (media URLs origin-pinned to our own bucket), not closed, so counting it among the "now 401" set would misreport the platform status. Two further lessons landed during that deploy. (1) An ambiguous PostgREST embed silently made the sponsor-brand authorization arm DEAD CODE: `campaign_sponsorships` has TWO FKs into `business_profiles` (`brand_id`, `restaurant_id`), so a bare `business_profiles!inner` answers **300 PGRST201**, and **supabase-js surfaces an HTTP error status as `{data:null}` rather than throwing** - so the `?? []` fallback at the call site turned a broken query into a confident empty answer. It failed CLOSED (denied a party it meant to allow), and two independent close reads of that file still missed it: a two-FK table is invisible in query text and visible only in the schema. Fix is the `!brand_id` hint, which is load-bearing, not styling. (2) A PARALLEL Claude session edited the same function within hours (#403), surfaced only by an out-of-date merge rejection - so the deployed artifact was a version no review had seen whole, and the pre-deploy gate had to run again. **A pre-deploy review binds to a commit; if the branch moves, re-run it** (2026-08-08)
- [[Workspace Email Signatures]](concepts/workspace-email-signatures.md) — every employee's Gmail signature, generated from one template and installed nightly by an Apps Script. **Email is not the web, and nearly every decision here is a consequence.** Webfonts do not exist in email — Gmail/Outlook/Apple Mail strip `@font-face`, so DragonCandy's entire type system **cannot appear as text in a signature at all**, only inside an image (mock options in the font they will *really* render in, or the mockup lies about the thing being decided). Outlook for Windows uses the Word engine: no WebP, no CSS layout. Transparency is load-bearing — dark-mode auto-inversion turns an opaque mark into a glowing slab. Images are blocked by default in many corporate inboxes, giving the governing rule: **the image is never load-bearing** — strip every image and the signature is still complete, which is why the degrading "Badge" design won. **Google Workspace has no built-in signature management** (Append footer lands *below* the quoted thread and is not a signature), so it is the Gmail API plus a service account with domain-wide delegation — a real standing grant (`gmail.settings.basic` at first; **widened to include `gmail.settings.sharing` on 2026-08-22**, which also lets it set who may send mail as which address for every user in the domain — a deliberate founder decision; the grant is reversible by removing the scope, but that does **not** un-install signatures already written to a sendAs record — clear those first, since clearing them needs the very scope being revoked). **The sharpest finding, and this page got it wrong first: neither a Group NOR an alias is a send-as identity.** The page originally said the shared addresses appear in the send-as list *because* they are aliases, making the planned alias→Group conversion the hazard. The first real run refuted it — **`0 shared` signatures with the aliases intact and no Group anywhere** (2026-08-21). An alias makes mail *arrive*; it never appears in `settings/sendAs`, which is exactly where the installer looks. So the hazard written up as a future consequence of a decision nobody had taken was the state on the day it was written, and Groups were never a prerequisite. A second pessimistic claim also fell: **"no API can create a send-as identity" is false** — `users.settings.sendAs.create` is available *only* to domain-wide-delegated service accounts, precisely what this runs; the constraint was scope (`gmail.settings.sharing`, ungranted at the time), not capability. **A third claim then fell, and it is the one that cost something (2026-08-22): "the manual send-as route needs no new permissions" is also false.** Executing it returned `403 Missing required scope ".../gmail.settings.sharing" for modifying non-primary SendAs` — Google's reference lists `sendAs.update` as accepting `basic` *or* `sharing`, which holds only for the **primary** identity and is silent on the non-primary case every shared address falls into. **Both routes cost the same scope.** Worse, acting on the wrong claim caused a **live regression**: three identities added by hand made the nightly run abort on the first unwritable one, so it logged `ERROR` and stopped refreshing even the working primary signature. Closed by #456 — per-identity error isolation, a `PARTIAL` status distinct from `ok`/`ERROR`, and a `SHARING_SCOPE_ENABLED` switch gating whether the JWT *requests* the wider scope. **That switch defaults off and the default is load-bearing:** granting the scope in the admin console alone does nothing (Codex P1 — the runbook step was actionable and inert), while requesting one the delegation does not carry fails the *entire* token exchange with `unauthorized_client`, taking down every signature for every user. Hence an order that is not optional: **console first, property second.** **Durable lesson: a claim that something is impossible — or that something is free — is itself a claim, and the only instrument that settles it is execution. The first two claims here were caught by reading and by review; the third was caught only by running it, and it is the one that broke production — reviews catch claims that contradict something written down, never ones that are merely untested and plausible.** Three portable traps: **a 200 is not proof of a resource** (the SPA catch-all served `index.html` at the image URL with HTTP 200 — check `content-type`); **a build step that exists is not one that runs** (the documented `clasp push` had no `.clasp.json`/`appsscript.json` and would have uploaded ES-module source, a V8 syntax error failing the whole project); and **a classifier is not an inventory** — listing an address that does not exist yet is inert, omitting one gets it signed as personal mail, so tidiness argues the wrong way. **LIVE since 2026-08-21** (#453, #454): `4 × ok`, daily 2–3am trigger, proven by writing into mailboxes the script owner cannot otherwise touch. **COMPLETE 2026-08-23: shared-mailbox signatures install.** Scope granted, #456 deployed, `SHARING_SCOPE_ENABLED=true`, final run **`ok / 4 identities / 3 shared`** for `dame@` and `ok / 1 identity / 0 shared` for the other three (correct — only he has shared identities). **The deliberate two-run sequence is the transferable part:** push → confirm `PARTIAL` with a non-zero denied count → flip the property → re-run. The intermediate `PARTIAL` is load-bearing — it is the only observation separating *the scope fixed it* from *the scope masked a still-broken loop*, and a lone success at the end proves strictly less. Two further lessons: **merged is not deployed** (#456 sat fixed-in-repo and broken-in-prod for a day because `clasp push` needed a re-auth, and nothing detected it — Apps Script has no CI joining the repo to the live script); and **this page's own status paragraph has been wrong twice in three days**, which is why it is dated and why a present-tense status line on a RAG-fed page should be distrusted past its date. **A run with a finding now emails `ALERT_EMAIL`** (`alerts@dragoncandy.com`, a new alias — the seven existing ones are each already spoken for), silent on a clean run because a nightly "all fine" trains its reader to filter the thread. **And the last step of building an alarm is hearing it ring:** four rounds went into a message nobody had ever received, so #466 added `sendTestAlert()`, which goes *through* `sendRunAlert_` rather than around it (a test with its own `MailApp` call proves MailApp works, which was never in doubt) and **throws where a real run only warns** — because `sendRunAlert_` swallows delivery errors by design, so unchecked, a broken mail path finishes **green and reads as a pass**. It exposed the more transferable gap: **`sendRunAlert_` had no tests at all**, since every test fed `runAlert_`, the pure composer beside it — **a well-tested pure function next to an untested impure one reads as coverage of both**, the same shape as the `runStatus_` mutation that slipped past all 79 tests the day before, and twice now the untested piece was the one deciding whether anyone is told. **`sendTestAlert()` was then run for the first time (2026-08-23) and the alert did not arrive — the alarm had never worked.** `MailApp.sendEmail` reached **0 of 3** external recipients across two providers, each `Bounced` within 0.16s, while the same sender composing in Gmail reached 3 of 3 that week; `GmailApp` delivered. **`MailApp` cannot report this** — it hands off to Google and returns, and the rejection lands milliseconds later outside the execution, so `sendRunAlert_` returned `true` and logged success for every bounce. That is why four rounds improved *what the alert said* while nobody could receive one. Fixed by `GmailApp.sendEmail` (**positional** args, not MailApp's options object — a symbol-only swap sends to `undefined` with every test still green) plus scope `script.send_mail` → `gmail.send`, pinned by a **text assertion** because the property is unobservable at runtime. **Codex flagged `gmail.send` as a P1** on documentation grounds (the reference lists only `https://mail.google.com/`) and was **refuted by the granted scope list**, which shows "Send email as you" and not "read, compose, send, and permanently delete all your email" — taking that advice would have traded a send-only grant for full mailbox read-and-delete. **DKIM was entirely missing and is now published and verified, and it was NOT the cause** — the bounces did not change when it landed. Three instrument lessons, all one shape: **every sender-side signal (execution log, Sent folder, returned id) is the sender's view and none is evidence of delivery**; **a missing bounce message is not evidence that nothing bounced**; and searching the log by the fixed message's `Message-ID` returned **0 results**, which was only safely read after the *same query* returned 1 for a known-good id — **when a probe returns zero, prove it could have returned non-zero**. Still open: **Outlook for Windows is untested and now untestable**; shared identities exist on no account but `dame@`; and the nightly trigger has not yet fired on the new transport (2026-08-23)
- [[X Analytics Connector]](concepts/x-analytics-connector.md) — the fourth direct platform connector (`tweet.read` + `users.read` + `offline.access`, nothing that can post), and **the only one that charges**. X deleted its free tier in February 2026 and moved to pay-per-use — ~$0.005 a post read, ~$0.010 a user read, **no free path to a user timeline at all** — which is why the 15-minute snapshot cache lives in the schema as a **cost control** a client cannot opt out of, why a forced refresh keeps a 60-second floor under it, why pagination is deliberately absent, and why the organic-metrics retry was narrowed (*a retry is only worth paying for when the thing being removed is the thing that failed*). Almost none of the sibling pattern carried: the access token lasts **two hours** (Instagram's 60 days, a Page token forever), the refresh token **rotates**, and **PKCE is mandatory** — the verifier HMAC-derived from the state nonce, so there is nothing to store or expire. **The callback had to move into the app**, because an HMAC-signed state proves the state is *ours* and never that the browser completing consent started the flow. Thirteen Codex rounds, fifteen real findings, every one a race or a false claim in the grant lifecycle; two worth keeping — **a guard that enumerates the bad cases treats every case it has not met as good** (enumerate the good one), and **a lock only helps while it is held**. Also **HTTP 200 means two opposite things**: RFC 7009 returns it for a successful revoke *and* an invalid token. **CONNECTED ON PROD 2026-08-25 AND MEASURING NOTHING** — the token exchange is HTTP Basic with the client secret, so tokens coming back is what finally proved `X_CLIENT_SECRET` (everything earlier proved only the client *ID*), but the analytics read answered **402 credits-depleted** and `last_synced_at` is still null. *"Connected" is not the same claim as "working"*, and this is the first connector with one and not the other. That 402 was **printing X's raw JSON body on a settings page**, because it fell into the catch-all that appends the response body while the card renders `error.message` directly — now its own case, deliberately **not** `needs_reconnect` (reconnecting cannot buy credits) and worded without naming our developer account, since creators read the same card. The **0 followers** on it is a *genuine* zero, checked because that is exactly the shape of the fabricated zero this codebase has already shipped. Two findings about the knowledge layer came out of verifying it: **`docs/runbooks/` is not in Donny's RAG at all** (0 rows of 472), and **`db:apply` leaves the ledger's `statements` column null**, so for those migrations the repo file is the only copy that exists.
- [[YouTube Analytics Connector]](concepts/youtube-analytics-connector.md) — a per-user, **read-only** link to a YouTube channel supplying the analytics Outstand never shipped (Outstand keeps publishing; a test pins the scope list so a write scope is a decision, not a slip). **The design turns on one correction found in review:** the first build had Google redirect straight to an edge function with `verify_jwt = false`, authorized by an HMAC-signed state — but a signature proves the state is ours, **not** that the browser completing consent is the browser that started it, so an attacker could start a connect, send the authorize URL to a victim, and have the **victim's** YouTube tokens stored under the **attacker's** account. The code even carried a comment asserting the harmless *mirror* case as though it were the whole story — getting an attack's direction backwards reads as analysis. Fixed with the pattern the repo already had for Workspace: Google redirects to a **page inside the app**, which forwards the code with the user's own JWT, and `verifyState` requires the state to name that caller. Also: **HTTP 403 means two opposite things** — persisting `needs_reconnect` on every 403 (itself a fix for a stale UI, from the previous review round) would have let one hour of `quotaExceeded` tell every user on the platform to reauthorize, so quota is classified by `reason` + `RESOURCE_EXHAUSTED`; **a live grant is never abandoned** (the token in hand is the only thing that could revoke it, so every non-storing exit revokes first, and disconnect revokes *before* it deletes); rows are read **by column name** because `columnHeaders` order belongs to the response, and positional reads shift every figure the day a metric is added; and the [[Honest Analytics]] rules hold — empty is zero rows not a row of zeros, every figure states its N (`days_with_data`, since YouTube reports a day or two in arrears), and average view duration is **derived from totals** not averaged from daily averages (330s vs 61s on a lopsided pair). Status values are `active`/`needs_reconnect` only — a disconnected connection is an **absent row**, so no `revoked` value with no writer. Five Codex rounds, six real findings. **Merged, applied and deployed 2026-08-23** (#477) — migration on prod with its ledger row written by hand (`db push` is unsafe here, and the `CREATE TRIGGER` is not idempotent), four functions `v1 ACTIVE` at `verify_jwt=true`, grants read back as exactly `service_role` and RLS enabled with zero policies. **WORKING END TO END 2026-08-23 16:46 UTC** — first channel linked (`UC1DnGrwxLBaQkU4hQG1MsCw`, "DragonCandy") under `dame@dragoncandy.com`, two read scopes and no write scope, `last_synced_at` stamped 51s after connect. The proof the figures are real is a number that looks like a typo: the card says **"25 days of data"** against the **28** requested, because YouTube reports in arrears — a fabricated or fallback response would echo 28, so the zeros are real zeros from 25 real rows. Four-agent `edge-function-reviewer` pass: **PASS on all four, zero issues**; all eight deployed files diffed byte-identical against the repo. **Disconnect, revoke and re-consent exercised the same afternoon.** Disconnect deleted the row, which *is* the proof the revoke succeeded (the DELETE is only reached after Google returns `revoked`/`already_invalid`; a failure keeps the row and its token). A second, "independent" proof was claimed and is **withdrawn**: the post-disconnect consent flow proved nothing, because `prompt=consent` makes that flow appear either way — before calling an observation independent, name what would have to differ for it to come out the other way. **Published to production 2026-08-23 18:20 UTC** (reversible), which stops the 7-day refresh-token expiry going forward but does **not** lift the 100-user cap — only verification does, and Google's console says the cap is lifetime and unresettable. The Testing-era token was **retired and re-minted** rather than assumed to inherit the new policy. **A same-day correction:** the consent flow is **two screens**, not one — screen 2 itemises "View your YouTube account" and "View YouTube Analytics reports", both *View*, which is the user-facing proof it cannot post; a follow-up correction then claimed it is "skipped when the account already holds the scopes", which the Codex second review refuted against the code — `prompt=consent` is sent unconditionally, so screen 2 appears on **every** connect and it went unseen only because nobody looked past screen 1. The rule survives with a better reason: **the consent screen is not the record of what was granted** (a flow can skip a screen) — the token response's scope array is, which is why this build reads it back. No "unverified app" interstitial seen in either status; recorded as not-observed, not absent. The boot probe carries its own control — a bogus slug returns 404 where all four return 401, and the anon-key request returns *our* JSON body rather than the gateway's, which is what proves the modules loaded. **The merge shipped a ~20-minute visible defect:** the frontend went live before the migration, so the Settings card's RPC 404'd and every creator and business saw its red error branch — ship the schema before the UI that reads it. Analytics API enabled and the redirect URI moved to `https://dragoncandy.com/youtube/callback` in the console (2026-08-23)

## Flow Diagrams

- [[Feature Flows]](../flows/README.md) â€” Index + system map for all visual feature-flow docs
- [[Campaign Lifecycle Flow]](../flows/campaign-lifecycle.md) â€” Create â†’ apply â†’ deliver â†’ approve â†’ download â†’ auto-post (Mermaid)
- [[DragonShare Flow]](../flows/dragonshare.md) â€” Upload â†’ boost-or-pass â†’ two-path payment â†’ fulfillment â†’ notify
- [[Promotions / CGC Flow]](../flows/promotions-cgc.md) â€” Promotion â†’ anonymous submission â†’ approve â†’ discount code â†’ cross-post
- [[Social Login]](concepts/social-login.md) — Google/Apple/Facebook sign-in, **shipped dark** behind `SOCIAL_LOGIN_ENABLED`. Two blockers had to fall first: an OAuth user would have been told to verify an email nothing ever sends (`profiles.email_verified` defaults false, the trigger never set it, `AuthPage` gates on it, and `authenticated` has INSERT but no UPDATE), and every social signup would have become a `content_creator` because `signInWithOAuth` cannot carry user metadata. **The one-line fix was wrong and prod proved it**: mirroring `email_confirmed_at` would auto-verify every PASSWORD signup, because Supabase's own confirmation is disabled here — 45 of 45 users confirmed, 44 within ONE SECOND of creation, min 6ms. Verification comes from the **provider**, via an allowlist. Three objects in migration `20260825140000`, each for a case a trigger cannot see: `handle_new_user` (accounts OAuth CREATES), `claim_initial_role` (the chosen role, once) and `sync_oauth_email_verification` (accounts OAuth LINKS TO — the INSERT trigger never sees a password account that later signs in with Google). `claim_initial_role` refuses four ways, and **neither of its two account-identity guards is redundant** — provider catches a password account that linked Google, age catches an account genuinely created by Google months ago; either alone converts an existing account. The age window is **not** a consent-screen deadline (`created_at` is stamped after the callback). Role and guarded destination travel in the redirect URL because `sessionStorage` is origin-scoped and this round trip can change origins. Web only: native ejects the user into Safari and needs a custom-scheme redirect. Seven Codex rounds, seven findings, all real. Proven on prod in rolled-back transactions where **the controls did real work** — removing the guards reproduced "an existing account was converted", and widening the identity allowlist made a password-only account verify itself. (2026-08-24)
- [[Onboarding Wizard & Depth]](concepts/onboarding-wizard-and-depth.md) — slices 3 and 4 of the signup/onboarding redesign, on one branch. The wizard is declarative slides driven by [[Account Completeness Engine]]'s registry (`ROLE_STEPS` / `STEP_PHASE` / `REQUIREMENT_STEP`), with a coverage test that carries a forced control. **The core save moved to the collect/service boundary** — every later slide acts on rows that must exist (`verify-address` reads the stored address back; Stripe needs a profile) — which also closed an abandonment bug and created three of its own, all caught by review: the org query is a separate React Query cache so a new business kept `{org: null}` and could never resolve a location; the save can now beat `useAutoDetect`, so a fast creator saved null city/country and nothing ever asked again; and `goNext` became async, so a double tap advanced TWO slides and skipped phone verification. Dirtiness is a **fingerprint, not a flag** (a missed setter fails silently and looks like working code); the late-detection path writes **only** the three location columns, records its key before the await so a failure is attempted once, and fails silently because nobody asked for it. Slice 4: the Locations page finally shows address status (`required`, unmet for every business on prod — 30 units, 4 with an address, 0 verified) with saving now waiting for verification instead of racing the invalidation; `deriveTeam` returns `pending` while an invite is outstanding instead of telling an owner to invite the team they just invited. **Two brand requirements no brand could satisfy**: `address` removed (spec §4.4 excluded it; slice 2 silently reversed that; 7 brand units, all products, zero addresses) and `stripe` KEPT but its slide removed (no brand Connect path exists — both restaurant functions filter `account_type='restaurant'`; 6 brands, 0 Stripe accounts) — **`publish_campaign` demands `stripe` and lists `brand`, and has no call site today**. Ten Codex rounds, twelve findings, all real, all mine; nearly every one a consequence of an earlier change in the same session. Two forced controls changed a conclusion. (2026-08-24)
- [[Onboarding Flow]](../flows/onboarding.md) â€” Signup â†’ role selection â†’ wizard â†’ first-run missions
- [[Stripe Payments Flow]](../flows/stripe-payments.md) â€” Escrow, payout, sponsorship, boost, Connect, take-rate ladder
- [[Creator Journey]](../flows/creator-journey.md) â€” Creator's cross-feature path
- [[Restaurant Journey]](../flows/restaurant-journey.md) â€” Restaurant's cross-feature path

## Analyses

- [[18-Month Tech Engineering & Donny AI]](analyses/18-month-tech-engineering-donny-ai-1m-users.md) â€” Donny-captured analysis: 18-month tech engineering & Donny AI plan toward 1M users
- [[Claude Skills Framework Audit]](analyses/claude-skills-framework-audit.md) â€” Anthropic's 9-category Skills playbook applied to DragonCandy's dev `.claude/skills/` library + Donny (playbooks/tools/RAG); a rubric scorecard, coverage matrix, and a valueÃ—effort-ranked backlog; the library is strong (gaps are whole missing categories, not weak skills); ships the `careful` safety skill as the #1 quick win
- [[Claude Subagents Audit]](analyses/claude-subagents-audit.md) â€” the "how to build Claude Code subagents" video applied to DragonCandy audit-first; 7-dimension rubric + current-usage assessment (zero custom `.claude/agents/`) + a ranked custom-subagent backlog; ships the read-only `edge-function-reviewer` subagent as the #1 quick win. **Tier 2 resolved 2026-07-19** â€” `rls-migration-reviewer` shipped as `data-exposure-reviewer` ([[Service-Role Data Exposure]]); its `~` partial non-redundancy cleared by drawing the boundary the deferral asked for (`verify-db-schema` = does RLS *permit* the real caller; the subagent = does it *exclude* everyone else), and renamed because the evidence sits in service-role query call sites, not migrations.
- [[Competitive Advantage]](analyses/the-honest-framing-first.md) â€” Donny-captured (2026-06-23): how DragonCandy wins vs Cirqle, Aspire, GRIN, Captiv8, CreatorIQ â€” competitor-by-competitor weaknesses and DragonCandy's edge
- [[Content Engine Data Audit]](analyses/content-engine-data-audit.md) â€” what signal data exists in prod for the Donny content engine; context live, performance dark, Toast/social-cache tables missing from prod; foundation-first plan
- [[Dame AI â€” Part 1: What It Is & The Six Domains]](analyses/dame-ai-part-1-what-it-is.md) â€” Workspace-imported (2026-06-27), split 2026-08-07: the problem statement, what Dame is, its six operating domains, the Dame-vs-Donny separation of concerns, and what Dame is NOT. Source doc behind [[Dezzy Agent (Playbook Suite)]] (the agent was renamed Dame â†’ Dezzy)
- [[Dame AI â€” Part 2: Architecture, Cost & Build Plan]](analyses/dame-ai-part-2-architecture-and-build.md) â€” the build half: technical architecture, cost-vs-replaces, the compounding economy of scale with Donny + DRE, immediate actions and the phased build plan
- [[Dragon Rewards Engine â€” Part 1: The Points Economy & Earning]](analyses/dre-part-1-points-economy.md) â€” Workspace-imported (2026-06-27), split 2026-08-07: the DP economy, the per-role reward trigger matrix, daily boosts/hype events, the welcome pack, the admin config engine and the social sharing engine
- [[Dragon Rewards Engine â€” Part 2: Community, Redemption & Implementation]](analyses/dre-part-2-community-and-implementation.md) â€” leaderboards and community mechanics, what DP redeem for, the Supabase schema, how it compounds, success metrics and build phases
- [[DragonCandy Tech & Infrastructure Cost Breakdown]](analyses/here-s-the-exported-doc-dragoncandy-tech-infrastructure-cost-breakdown-nyc-media.md) â€” Donny-captured analysis: NYC-median tech & infrastructure cost breakdown for the capital raise
- [[Human Marketing Team (Part 1)]](analyses/part-1-the-human-marketing-team.md) â€” Donny-captured analysis: the human marketing team (part 1)
- [[Influencer/Creator Outreach]](analyses/influencer-creator-outreach-w-donny.md) â€” Donny-captured (2026-06-23): can Donny reach out to off-platform creators / build campaigns with them â€” what's reachable vs the hard platform walls to design around
- [[North Star & KPI Scorecard]](analyses/north-star-kpi-scorecard.md) â€” Three-year targets + kill-switches operationalized and validated against 2025 SMB-SaaS benchmarks; flags churn-unit ambiguity and a mis-scoped rev/employee gate
- [[PART 1 â€” Engineering & AIOS Operations]](analyses/part-1-engineering-aios-operations.md) â€” Donny-captured analysis: engineering & AIOS operations (part 1)
- [[Platform API Registration Plan]](analyses/platform-api-registration-plan.md) â€” running checklist to swap Outstand for direct Meta/X/TikTok/YouTube + Toast access; per-platform requirements, lead times, Meta deep-dive; unblocks the dark Content Engine signal
- [[Tech & Infrastructure Cost Breakdown (Updated)]](analyses/tech-infrastructure-cost-breakdown-updated.md) â€” Donny-captured analysis: updated tech & infrastructure cost breakdown for the capital raise
- [[The Core Idea: Two Agents, One Company]](analyses/the-core-idea-two-agents-one-company.md) â€” Donny-captured (2026-06-27): Donny serves users, Dezzy (renamed from "Dame") serves the company; Dezzy's six growth domains + the economy-of-scale loop; domain #3 (Outreach Machine) shipped as v1 â€” see [[Dezzy Agent (Playbook Suite)]]
- [[What We Built]](analyses/what-we-built.md) â€” Donny-captured (2026-06-27): a configurable, projected-activity-based rewards/boost mechanic to drive engagement and grow the app automatically (Dragon Rewards Engine context)
