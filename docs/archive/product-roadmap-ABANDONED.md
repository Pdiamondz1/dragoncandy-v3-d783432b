> **ARCHIVED — do not follow this document.**
>
> Generated by a planning tool in early 2026 and never used. It shows 0 of 72 tasks complete and
> describes a design system (`dragon-teal`, `Inter`, `JetBrains Mono`) that was never built — the
> real one is in `docs/DESIGN_SYSTEM.md`. Kept only as history.
>
> For what is actually being built, see `docs/DragonCandy_Tech_Department_Scope.md` and
> `docs/PROJECT_CONTEXT.md`.

# Product Roadmap — DragonCandy

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 0/72 tasks complete
**Current Phase:** Phase 0 — Foundation & Design System

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** The first phase sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The core value proposition — business creates campaign, creator delivers content, payment flows — works as early as possible.
4. **Test as you go.** Don't save testing for the end. Each task includes verification.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Incremental commits only.** DragonCandy has a history of bulk changes breaking production. Every task is committed independently. No batching.
8. **Protect what works.** When modifying existing pages, explicitly preserve working desktop `lg:` Tailwind classes and only target base-level mobile styles.

---

## Phase 0: Foundation & Design System

> **Goal:** The app's design system is updated with new design tokens, the project structure supports all three user roles, and the foundation for AI-powered features is in place. Running the app shows a polished shell with role-based routing.

**Reference sections — read these before starting this phase:**
- PRD: § Technical Architecture, § Auth Implementation, § Design System
- Vision: § Design Direction > Design Tokens, § Design Direction > Color Palette, § Design Direction > Typography

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-design-system`, commit, push, and open a PR for review."

- [ ] **TASK-001** — Update design tokens and Tailwind configuration with new DragonCandy palette
  Files: `tailwind.config.ts`, `src/styles/globals.css`
  Notes: Add the full color palette from docs/prd.md § Design System: dragon-teal, candy-pink, dragon-bg, dragon-surface, dragon-text, semantic colors. Add font-family config for Inter and JetBrains Mono. Add border-radius and spacing tokens. Import Google Fonts in globals.css. Verify: Run dev server, inspect computed styles to confirm tokens are applied.

- [ ] **TASK-002** — Create/update core UI primitives with new design system
  Files: `src/components/ui/Button.tsx`, `src/components/ui/Card.tsx`, `src/components/ui/Input.tsx`, `src/components/ui/Badge.tsx`, `src/components/ui/Modal.tsx`, `src/components/ui/Toast.tsx`
  Notes: Implement component specs from docs/prd.md § Design System > Component Specifications. Button variants: primary, secondary, ghost, danger. Card with surface bg and hover state. Input with focus glow. Badge with color variants. Modal with backdrop fade and scale animation. Toast with slide-in from bottom-right. All components use design tokens. Min touch targets 44px. Verify: Render each component variant in isolation.

- [ ] **TASK-003** — Create layout components for role-based dashboards
  Files: `src/components/layout/Sidebar.tsx`, `src/components/layout/Header.tsx`, `src/components/layout/PageContainer.tsx`, `src/components/layout/DashboardLayout.tsx`
  Notes: Sidebar with navigation items that vary by user role (creator/business/brand/admin). Header with user avatar, notification bell, and role indicator. PageContainer with max-width 1280px. DashboardLayout wraps Sidebar + Header + main content area. Mobile: sidebar collapses to hamburger menu. Verify: Layout renders for each role with correct nav items.

- [ ] **TASK-004** — Set up role-based routing and protected routes
  Files: `src/components/auth/ProtectedRoute.tsx`, `src/pages/auth/Login.tsx`, `src/pages/auth/Signup.tsx`, routing config
  Notes: ProtectedRoute component that checks auth state and user role. Redirect unauthenticated users to /auth/login. Redirect to role-specific dashboard after login. Signup flow with role selection (creator/business/brand). Use Supabase Auth for signup with role in user metadata. See docs/prd.md § Auth Implementation for trigger function. Verify: Sign up as each role, confirm routing works.

- [ ] **TASK-005** — Create database migration for updated schema
  Files: `supabase/migrations/`, relevant SQL files
  Notes: Create migration with the full data model from docs/prd.md § Data Model. Tables: profiles, creator_profiles, business_profiles, brand_profiles, campaigns, creative_briefs, gig_assignments, content_deliverables, payments, messages, conversations, reviews, campaign_analytics, notifications. Include all indexes. Add the handle_new_user trigger function. Apply RLS policies for each table. Verify: Run migration, check tables exist with correct columns and policies.

- [ ] **TASK-006** — Set up Supabase Edge Functions scaffolding
  Files: `supabase/functions/generate-brief/index.ts`, `supabase/functions/match-creators/index.ts`, `supabase/functions/process-payment/index.ts`, `supabase/functions/stripe-webhook/index.ts`, `supabase/functions/send-notification/index.ts`
  Notes: Create skeleton Edge Functions with auth validation, basic error handling, and CORS headers. Each function validates the auth token and returns a structured response. No business logic yet — just the scaffolding. Verify: Deploy functions, test that each returns 401 for unauthenticated requests and 200 for authenticated requests.

- [ ] **TASK-007** — Create placeholder dashboard pages for each role
  Files: `src/pages/creator/Dashboard.tsx`, `src/pages/business/Dashboard.tsx`, `src/pages/brand/Dashboard.tsx`, `src/pages/admin/Dashboard.tsx`
  Notes: Each dashboard uses DashboardLayout with role-specific sidebar nav. Show a welcome message with the user's name and role. Add empty state cards for the main sections (gigs, campaigns, analytics). Use the new design system components. Verify: Log in as each role, confirm correct dashboard renders with proper nav items and styling.

- [ ] **TASK-008** — Create loading skeleton and error state components
  Files: `src/components/ui/Skeleton.tsx`, `src/components/ui/ErrorState.tsx`, `src/components/ui/EmptyState.tsx`
  Notes: Skeleton with pulsing animation using dragon-surface color. ErrorState with message and retry button. EmptyState with illustration placeholder, message, and CTA. These will be used throughout the app. Verify: Render each component with sample props.

---

## Phase 1: Creator & Business Profiles

> **Goal:** Creators and businesses can sign up, complete onboarding, and manage their profiles. Creator portfolios display correctly. Business locations are geocoded. The foundation for the marketplace is ready.

**Reference sections — read these before starting this phase:**
- PRD: § Data Model (profiles, creator_profiles, business_profiles), § Functional Requirements (FR-002, FR-003), § UI/UX Requirements (Creator Profile, Business Dashboard)
- Vision: § User Research > Primary Persona, § User Research > Secondary Personas

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/creator-and-business-profiles`, commit, push, and open a PR for review."

- [ ] **TASK-009** — Build creator onboarding flow
  Files: `src/pages/auth/onboarding/CreatorOnboarding.tsx`, `src/components/creator/PortfolioUploader.tsx`, `src/components/creator/ContentStyleSelector.tsx`, `src/components/creator/LocationPicker.tsx`
  Notes: Multi-step onboarding: Step 1 — Bio and profile photo. Step 2 — Upload 3–10 portfolio pieces (images/videos to Supabase Storage). Step 3 — Select content styles from predefined list (food-photography, video-reels, lifestyle, product-shots, stories). Step 4 — Set location (city, radius in km). Save to creator_profiles table. Mark onboarding_completed = true in profiles. Verify: Complete full onboarding flow, check all data saves correctly.

- [ ] **TASK-010** — Build business onboarding flow
  Files: `src/pages/auth/onboarding/BusinessOnboarding.tsx`, `src/components/business/BusinessInfoForm.tsx`
  Notes: Single-step onboarding: business name, type (restaurant/cafe/food-truck), address (geocode to lat/lng), phone, social handles, logo upload. Save to business_profiles table. Mark onboarding_completed = true. Use a geocoding service or simple city-based location for MVP. Verify: Complete business onboarding, check geocoded coordinates are saved.

- [ ] **TASK-011** — Build creator profile page with portfolio display
  Files: `src/pages/creator/Profile.tsx`, `src/components/creator/PortfolioGrid.tsx`, `src/components/creator/ProfileStats.tsx`, `src/components/creator/AvailabilityToggle.tsx`
  Notes: Profile page shows: avatar, bio, portfolio grid (masonry layout for mixed photo/video), content styles as badges, location, availability toggle (available/busy/unavailable), rating, gigs completed, total earnings. Portfolio pieces open in a lightbox. All editable. Verify: View profile, edit fields, toggle availability — changes persist.

- [ ] **TASK-012** — Build business profile page
  Files: `src/pages/business/Profile.tsx`, `src/components/business/BusinessCard.tsx`
  Notes: Profile page shows: logo, business name, type, address, social handles, description. All editable. BusinessCard component for reuse in campaign displays. Verify: View and edit business profile.

- [ ] **TASK-013** — Build Stripe Connect onboarding for creators
  Files: `src/components/creator/StripeConnectSetup.tsx`, `src/pages/creator/StripeReturn.tsx`, `src/pages/creator/StripeRefresh.tsx`
  Notes: Show Stripe Connect status on creator profile. "Connect Your Bank" button generates Stripe onboarding link via Edge Function (create Connect Express account, generate accountLink). Handle return URL (mark stripe_onboarding_complete = true if account is verified). Handle refresh URL (regenerate onboarding link). See docs/prd.md § Payment Integration. Verify: Click connect button, verify Stripe onboarding link opens.

- [ ] **TASK-014** — Build brand onboarding and profile page
  Files: `src/pages/auth/onboarding/BrandOnboarding.tsx`, `src/pages/brand/Profile.tsx`
  Notes: Onboarding: brand name, industry, description, logo, website, contact email, monthly budget range. Profile page with all fields editable. Verify: Complete brand onboarding and edit profile.

---

## Phase 2: Campaign Creation & AI Brief Generation

> **Goal:** Businesses can create campaigns using AI-generated briefs. The AI brief generator accepts natural language input and produces structured creative briefs. Campaigns are stored and visible on the business dashboard.

**Reference sections — read these before starting this phase:**
- PRD: § Data Model (campaigns, creative_briefs), § Functional Requirements (FR-004), § UI/UX Requirements (Campaign Creation Flow, Business Dashboard), § Edge Cases (AI Brief Generation)
- Vision: § Product Strategy > Magic Moment Design

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/campaign-creation-ai-briefs`, commit, push, and open a PR for review."

- [ ] **TASK-015** — Build campaign creation form (Step 1: plain language input)
  Files: `src/pages/business/CampaignNew.tsx`, `src/components/campaign/BriefInput.tsx`
  Notes: Large text area with placeholder: "Describe what you need — e.g., 'We just launched a spicy chicken sandwich and want people to know about it'". Minimum 10 characters validation. "Generate Brief" button triggers AI generation. Show generating state with teal pulsing animation and "Writing your brief..." text. Verify: Text input works, validation fires, loading state shows correctly.

- [ ] **TASK-016** — Implement AI brief generation Edge Function
  Files: `supabase/functions/generate-brief/index.ts`
  Notes: Accept campaignId and rawInput. Call OpenAI API (or Anthropic) with a system prompt that generates: content ideas (3-5), suggested hashtags (5-10), suggested captions (2-3), posting schedule by platform, and style direction. Parse response into structured JSON. Save to creative_briefs table. Return structured brief. Timeout: 30 seconds. Error handling: retry once on failure, return error with user-friendly message. See docs/prd.md § Edge Cases > AI Brief Generation. Verify: Call function with test input, verify structured brief is returned and saved.

- [ ] **TASK-017** — Build brief review UI (Step 2: review generated brief)
  Files: `src/components/campaign/BriefReview.tsx`, `src/components/campaign/BriefSection.tsx`
  Notes: Display generated brief in card sections: content ideas (as a list of cards), hashtags (as badges), captions (as editable text), posting schedule (as a timeline), style direction (as a description card). "Approve Brief" and "Edit" buttons. Edit mode allows inline editing of any section. On approve, update creative_briefs.approved_by_business = true. Verify: Brief displays correctly, editing works, approval updates database.

- [ ] **TASK-018** — Build campaign confirmation (Step 3: budget and launch)
  Files: `src/components/campaign/CampaignConfirm.tsx`
  Notes: After brief approval: budget input (in dollars, converted to cents for storage), content type selector (photo/video/mixed), target platforms (Instagram/TikTok multi-select), deadline picker (default: 24 hours from now). "Launch Campaign" button creates campaign with status 'matching' and triggers creator matching. Verify: Campaign is created with correct data, status is 'matching'.

- [ ] **TASK-019** — Build campaign list and detail views on business dashboard
  Files: `src/pages/business/Campaigns.tsx`, `src/pages/business/CampaignDetail.tsx`, `src/components/campaign/CampaignCard.tsx`, `src/components/campaign/StatusBadge.tsx`
  Notes: Campaign list shows all campaigns with status badges (draft/matching/in_progress/completed). CampaignCard shows title, status, brief summary, budget, deadline. CampaignDetail shows full brief, matched creators, content deliverables, and payment status. StatusBadge with color-coded statuses. Verify: Business can see all their campaigns, click through to details.

- [ ] **TASK-020** — Update business dashboard with campaign overview widgets
  Files: `src/pages/business/Dashboard.tsx`
  Notes: Replace placeholder content with: active campaigns count, "Content to Review" section (campaigns with content_submitted status), recent campaigns, "New Campaign" CTA. Data fetched from Supabase. Empty state: "Ready to get some amazing content? Tap 'New Campaign' to get started." Verify: Dashboard shows real data from database.

---

## Phase 3: Creator Matching & Gig Flow

> **Goal:** AI matches creators to campaigns. Creators see available gigs, accept them, upload content, and see their work in a structured workflow. The core marketplace loop works end-to-end (except payments).

**Reference sections — read these before starting this phase:**
- PRD: § Data Model (gig_assignments, content_deliverables), § Functional Requirements (FR-005, FR-006, FR-007), § UI/UX Requirements (Creator Dashboard), § Edge Cases (Creator Matching, Content Upload)
- Vision: § Product Strategy > Core User Flows (Flow 1, Flow 2)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/creator-matching-gig-flow`, commit, push, and open a PR for review."

- [ ] **TASK-021** — Implement creator matching Edge Function
  Files: `supabase/functions/match-creators/index.ts`
  Notes: Accept campaignId. Query available creators in the business's city (or within radius). Score by: location proximity (40% weight), content style overlap (30%), rating (15%), gigs completed (10%), availability (5%). Return top 5 matches with scores and reasons. Create gig_assignment records with status 'pending' for top matches. Send notifications to matched creators. If fewer than 3 matches, widen radius by 10km and re-query. See docs/prd.md § Edge Cases > Creator Matching. Verify: Function returns scored matches, gig_assignments created, notifications sent.

- [ ] **TASK-022** — Build creator gig feed (available gigs)
  Files: `src/pages/creator/Gigs.tsx`, `src/components/creator/GigCard.tsx`, `src/components/creator/GigDetail.tsx`
  Notes: "Available Gigs" tab shows pending gig_assignments for this creator. GigCard shows: campaign title, business name/logo, brief summary, location, pay amount, deadline, match score visualization. GigDetail expands to show full brief, content ideas, hashtags, map with business location. "Accept" button (one tap). "Decline" button. Verify: Creator sees matched gigs, can view details, accept/decline.

- [ ] **TASK-023** — Implement gig acceptance flow
  Files: `src/hooks/useGigActions.ts`
  Notes: On accept: update gig_assignment status to 'accepted', set accepted_at timestamp. Update campaign status to 'in_progress'. Notify business that a creator has accepted. On decline: update gig_assignment status to 'declined'. If all creators decline, trigger re-matching. Verify: Accept updates status correctly, business receives notification.

- [ ] **TASK-024** — Build content upload flow
  Files: `src/pages/creator/GigUpload.tsx`, `src/components/creator/ContentUploader.tsx`, `src/components/creator/CaptionEditor.tsx`
  Notes: For accepted gigs: upload area supporting drag-and-drop and file picker. Multiple file support (up to 5 deliverables per gig). Progress bar per file. Validation: max 100MB video, 10MB image, allowed types jpg/png/mp4/mov. Caption text area and hashtag input for each deliverable. "Submit for Review" button uploads to Supabase Storage, creates content_deliverables records, updates gig status to 'content_uploaded'. Notifies business. See docs/prd.md § Edge Cases > Content Upload. Verify: Upload files, check Storage, verify records created.

- [ ] **TASK-025** — Build active gigs view for creators
  Files: `src/pages/creator/ActiveGigs.tsx`
  Notes: Tab showing accepted gigs in various stages: 'accepted' (shoot content), 'content_uploaded' (awaiting review), 'approved' (completed, payment pending), 'revision_requested' (needs update). Each stage shows relevant actions and status. Timeline visualization showing gig progress. Verify: Active gigs show correct status and available actions.

- [ ] **TASK-026** — Update creator dashboard with gig and earnings overview
  Files: `src/pages/creator/Dashboard.tsx`
  Notes: Replace placeholder with: available gigs count with preview cards, active gigs section, earnings widget (this week / this month / all time), rating display, profile completion indicator. Real-time updates via Supabase subscriptions for new gig matches. Empty state: "No gigs yet — make sure your portfolio has at least 5 pieces so our AI can match you." Verify: Dashboard shows real data, updates in real-time when new gigs are matched.

---

## Phase 4: Content Approval & Payments

> **Goal:** Businesses approve content with one tap. Payments flow correctly through the marketplace: business charged, platform fee retained, creator paid. The core magic moment is fully achievable for all parties.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-008, FR-009), § Payment Integration, § UI/UX Requirements (Content Review Modal), § Edge Cases (Payments)
- Vision: § Product Strategy > Magic Moment Design

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 4. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-4/content-approval-payments`, commit, push, and open a PR for review."

- [ ] **TASK-027** — Build content review modal for businesses
  Files: `src/components/business/ContentReviewModal.tsx`, `src/components/business/ContentPreview.tsx`
  Notes: Full-screen modal. Left side: content preview (photo viewer with zoom, video player with controls). Support swiping between multiple deliverables. Right side: caption, hashtags, creator info (name, avatar, rating). Bottom: "Approve" button (teal, prominent, full-width) and "Request Changes" (ghost button). Approve triggers teal flash animation. Request Changes shows text input for revision notes. See docs/prd.md § UI/UX Requirements > Content Review Modal. Verify: Modal opens, media displays, approve and revision flows work.

- [ ] **TASK-028** — Implement content approval logic
  Files: `src/hooks/useContentApproval.ts`
  Notes: On approve: update content_deliverable status to 'approved', update gig_assignment status to 'approved', set approved_at timestamp. Trigger payment processing (call process-payment Edge Function with action 'payout'). Notify creator. On request changes: update content_deliverable status to 'revision_requested', save revision_notes, notify creator. Update gig_assignment status to 'revision_requested'. Verify: Approve updates all statuses correctly, payment function is called.

- [ ] **TASK-029** — Implement payment processing Edge Function
  Files: `supabase/functions/process-payment/index.ts`
  Notes: For 'charge' action: create Stripe PaymentIntent for campaign budget. For 'payout' action: calculate platform fee (15%), calculate creator payout (85%), create Stripe Transfer to creator's Connect account ID. Create payment records in payments table. Handle errors: if transfer fails, retry once, then notify admin. See docs/prd.md § Payment Integration and § Edge Cases > Payments. Verify: Test in Stripe test mode — charge succeeds, transfer to test Connect account succeeds, payment records created.

- [ ] **TASK-030** — Implement Stripe webhook handler
  Files: `supabase/functions/stripe-webhook/index.ts`
  Notes: Verify Stripe signature. Handle events: payment_intent.succeeded (update campaign payment status, notify business), transfer.created (update creator payout status, notify creator "You just earned $X"), account.updated (update creator stripe_onboarding_complete). Log all events. See docs/prd.md § Payment Integration > Webhook Handling. Verify: Send test webhook events via Stripe CLI, verify database updates and notifications fire.

- [ ] **TASK-031** — Build creator earnings dashboard
  Files: `src/pages/creator/Earnings.tsx`, `src/components/creator/EarningsChart.tsx`, `src/components/creator/PaymentHistory.tsx`
  Notes: Show: total earnings (all time), this month, this week. Chart showing earnings over time (recharts line chart with teal color). Payment history table: date, campaign name, amount, status. Pending payouts section. All amounts in dollars (convert from cents). Verify: Earnings display correctly, chart renders with real data.

- [ ] **TASK-032** — Build business spending overview
  Files: `src/components/business/SpendingOverview.tsx`
  Notes: Widget on business dashboard: total spend, active campaign budgets, completed campaign costs. Breakdown by campaign. Simple bar chart. Verify: Spending data accurate, matches payment records.

- [ ] **TASK-033** — Add payment method collection for businesses
  Files: `src/components/business/PaymentMethodSetup.tsx`
  Notes: Stripe Elements embedded form for businesses to add a payment method. Store the payment method ID on the business profile or use Stripe Customer. Required before launching a campaign. Show payment method status on profile. Verify: Business can add card via Stripe Elements in test mode.

---

## Phase 5: Messaging & Notifications

> **Goal:** Creators and businesses can message each other within campaign context. Notifications keep all users informed of important events in real-time.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-012, FR-013), § UI/UX Requirements (Messaging), § Data Model (messages, conversations, notifications), § Edge Cases (Messaging)
- Vision: § Product Strategy > Product Principles ("One-tap everything for businesses")

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 5. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-5/messaging-and-notifications`, commit, push, and open a PR for review."

- [ ] **TASK-034** — Build messaging UI
  Files: `src/pages/Messages.tsx`, `src/components/messaging/ConversationList.tsx`, `src/components/messaging/MessageThread.tsx`, `src/components/messaging/MessageInput.tsx`
  Notes: Left panel: conversation list sorted by most recent, showing other party's name/avatar and last message preview. Right panel: message thread with sender-aligned bubbles (own messages right-aligned teal, others left-aligned surface). Input field with send button at bottom. Conversations scoped to campaigns. See docs/prd.md § UI/UX Requirements > Messaging. Verify: UI renders correctly, conversations list, messages display.

- [ ] **TASK-035** — Implement real-time messaging with Supabase Realtime
  Files: `src/hooks/useMessages.ts`, `src/hooks/useConversations.ts`
  Notes: Subscribe to messages table filtered by conversation_id. On new message insert, append to thread in real-time. Create conversation on first message (check if conversation exists for this campaign + participants). Mark messages as read when conversation is open (update read_at). Unread count on conversation list. Fallback: if realtime disconnects, poll every 30 seconds. See docs/prd.md § Edge Cases > Messaging. Verify: Send message in one tab, see it appear in real-time in another.

- [ ] **TASK-036** — Auto-create conversations when gig is accepted
  Files: `src/hooks/useGigActions.ts` (update)
  Notes: When a creator accepts a gig, auto-create a conversation between creator and business linked to the campaign. Show a system message: "You're connected! Coordinate the shoot details here." Verify: Accept gig, conversation appears for both parties.

- [ ] **TASK-037** — Build notification system
  Files: `src/components/layout/NotificationBell.tsx`, `src/components/layout/NotificationPanel.tsx`, `src/hooks/useNotifications.ts`
  Notes: Bell icon in header with unread count badge. Click opens dropdown panel with notification list. Each notification: icon, title, body, timestamp, click action (navigate to relevant screen). Real-time subscription for new notifications. Mark as read on click. Notification types: gig_matched, gig_accepted, content_uploaded, content_approved, payment_received, new_message, revision_requested. Verify: Notifications appear in real-time, clicking navigates correctly, badge count updates.

- [ ] **TASK-038** — Implement notification Edge Function for email notifications
  Files: `supabase/functions/send-notification/index.ts`
  Notes: Create notification record in database. For critical events (payment_received, gig_matched, content_approved), also send email via Resend. Email template: simple, clean, DragonCandy branded header, clear message, CTA button linking to app. Respect user email preferences (add email_notifications boolean to profiles). Verify: Notification creates record and sends email for critical events.

---

## Phase 6: Brand Sponsorship & Analytics

> **Goal:** Brands can sponsor campaigns at target restaurants. Basic analytics show campaign performance for all three user types. The three-sided marketplace is fully operational.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-014), § Data Model (brand_profiles, campaign_analytics), § UI/UX Requirements (Brand Dashboard)
- Vision: § Product Strategy > Core User Flows (Flow 3), § User Research > Secondary Personas (James)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 6. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-6/brand-sponsorship-analytics`, commit, push, and open a PR for review."

- [ ] **TASK-039** — Build brand campaign creation flow
  Files: `src/pages/brand/CampaignNew.tsx`, `src/components/brand/RestaurantSelector.tsx`, `src/components/brand/BrandBriefInput.tsx`
  Notes: Brand creates sponsored campaign: select target restaurants (from business_profiles list, multi-select), set total budget, add brand messaging to include in briefs. AI brief generation includes brand messaging and product placement direction. Budget splits: brand_contribution stored on campaign. Verify: Brand creates campaign, target restaurants selected, brief includes brand messaging.

- [ ] **TASK-040** — Build brand dashboard with campaign overview
  Files: `src/pages/brand/Dashboard.tsx`, `src/components/brand/CampaignAnalyticsCard.tsx`
  Notes: Dashboard shows: active sponsored campaigns with status, total spend, number of content pieces produced, aggregate analytics (impressions, engagement). Campaign cards link to detail views. Empty state: "Ready to sponsor your first campaign? Tap 'New Campaign' to reach local restaurants." Verify: Brand sees their campaigns and aggregate data.

- [ ] **TASK-041** — Build campaign analytics dashboard
  Files: `src/pages/campaign/Analytics.tsx`, `src/components/analytics/MetricCard.tsx`, `src/components/analytics/EngagementChart.tsx`, `src/components/analytics/ContentPerformance.tsx`
  Notes: Shared analytics view (data filtered by role). MetricCards: impressions, engagement rate, content pieces, delivery time. EngagementChart: line chart over time. ContentPerformance: per-deliverable metrics. For MVP, analytics data is basic — populate campaign_analytics with mock/seeded data or manual entry. Real social media API integration is out of scope. Verify: Analytics page renders charts with data.

- [ ] **TASK-042** — Build brand analytics summary
  Files: `src/pages/brand/Analytics.tsx`, `src/components/brand/ROIDashboard.tsx`
  Notes: Aggregate view across all sponsored campaigns: total impressions, total engagement, spend vs. reach ratio, content gallery. ROIDashboard shows per-campaign breakdown. Export data as CSV (simple download). Verify: Brand can see aggregate analytics and export data.

- [ ] **TASK-043** — Add brand sponsorship payment flow
  Files: update `supabase/functions/process-payment/index.ts`
  Notes: For sponsored campaigns: brand is charged the brand_contribution amount. Payment flow: brand charged → content produced → platform fee from total (business + brand contributions) → creator paid. Update payment records to track payer_type 'brand'. Verify: Sponsored campaign charges brand correctly, creator payout includes brand contribution minus platform fee.

---

## Phase 7: Polish & Launch Prep

> **Goal:** The app is production-ready with comprehensive error handling, a compelling landing page, responsive design, and all the finishing touches needed for launch week.

**Reference sections — read these before starting this phase:**
- PRD: § Non-Functional Requirements, § Edge Cases & Error Handling, § UI/UX Requirements (Landing Page)
- Vision: § Brand Strategy > Voice & Tone Guide, § Design Direction > Accessibility Commitments
- GTM: § Launch Strategy, § Launch Week Plan

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 7. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-7/polish-and-launch-prep`, commit, push, and open a PR for review."

- [ ] **TASK-044** — Build the landing page
  Files: `src/pages/landing/Landing.tsx`, `src/components/landing/Hero.tsx`, `src/components/landing/RoleCards.tsx`, `src/components/landing/SocialProof.tsx`, `src/components/landing/Footer.tsx`
  Notes: Hero section: "Your restaurant's social media, handled." with animated content examples. Three CTA cards for creator/business/brand with role-specific messaging from docs/product-vision.md § Brand Strategy > Messaging Framework. Social proof section (placeholder for testimonials/stats). Footer with links. Mobile-responsive. Use design tokens throughout. See docs/prd.md § UI/UX Requirements > Landing Page. Verify: Landing page renders beautifully on desktop and mobile, all CTAs link correctly.

- [ ] **TASK-045** — Comprehensive error handling pass
  Files: All page and component files
  Notes: Audit every data fetch for error handling. Add ErrorState component where missing. Add try-catch to all Supabase calls. Toast notifications for transient errors. Inline errors for form validation. See docs/prd.md § Edge Cases for specific scenarios. Priority: AI brief generation failures, content upload failures, payment failures. Verify: Trigger error scenarios (network off, invalid data), confirm graceful handling.

- [ ] **TASK-046** — Empty states pass
  Files: All dashboard and list pages
  Notes: Add meaningful empty states with actionable CTAs using the EmptyState component. Copy should follow the voice and tone guide. Examples: "No gigs yet — make sure your portfolio has at least 5 pieces so our AI can match you." (creator), "Ready to get some amazing content? Tap 'New Campaign' to get started." (business). Verify: Remove data, confirm all empty states display correctly.

- [ ] **TASK-047** — Loading states pass
  Files: All page and component files
  Notes: Add Skeleton loading components to every data-dependent view. Use consistent pulsing animation. Dashboards show skeleton cards. Lists show skeleton rows. Detail views show skeleton content layout. Verify: Throttle network in browser devtools, confirm loading states appear before data.

- [ ] **TASK-048** — Form validation pass
  Files: All form components
  Notes: Ensure all forms use react-hook-form with zod schemas. Inline validation messages below fields. Prevent submission until valid. Specific validations: email format, password min 8 chars, business name required, brief min 10 chars, budget min $50, file type/size on upload. Verify: Submit invalid data to every form, confirm clear error messages.

- [ ] **TASK-049** — Responsive design pass
  Files: All layout and page components
  Notes: Test every page at sm/md/lg/xl breakpoints. Sidebar collapses to hamburger on mobile. Campaign cards stack vertically on mobile. Content review modal goes full-screen on mobile. Forms are single-column on mobile. Images/videos scale properly. CRITICAL: preserve working desktop lg: Tailwind classes — only modify base/sm styles. Verify: Check every page at 375px, 768px, 1024px, 1280px widths.

- [ ] **TASK-050** — Accessibility pass
  Files: All components
  Notes: Add aria-labels to all icon-only buttons. Add alt text to all images. Ensure form inputs have associated labels. Add visible focus indicators (2px teal outline with 2px offset). Check color contrast ratios (4.5:1 body, 3:1 large text). Add keyboard navigation to modal close, dropdown menus. Respect prefers-reduced-motion. Verify: Tab through entire app, verify all elements reachable and focusable.

- [ ] **TASK-051** — Performance optimization
  Files: Various
  Notes: Lazy load route components with React.lazy and Suspense. Optimize images (use webp where possible, proper sizing). Verify initial bundle under 300KB gzipped. Add loading="lazy" to off-screen images. Verify LCP under 2 seconds on throttled 4G connection. Verify: Run Lighthouse audit, check Core Web Vitals.

- [ ] **TASK-052** — SEO basics
  Files: `index.html`, `src/pages/landing/Landing.tsx`
  Notes: Set meta title: "DragonCandy — Professional Social Media Content for Restaurants, Delivered Fast". Meta description incorporating the 30-second elevator pitch. Open Graph tags for social sharing. Favicon and app icons. Sitemap for landing page. Verify: Check meta tags render, preview link on social media.

- [ ] **TASK-053** — Admin moderation basics
  Files: `src/pages/admin/Dashboard.tsx`, `src/pages/admin/Creators.tsx`, `src/pages/admin/Campaigns.tsx`
  Notes: Admin dashboard with: platform metrics (campaigns/week, active creators, active businesses, total revenue), creator list with approve/suspend actions, campaign list with ability to view any campaign. Basic dispute resolution: flag campaigns/gigs for review. Verify: Admin can view all data, approve/suspend creators.

- [ ] **TASK-054** — Creator ratings and reviews
  Files: `src/components/business/ReviewCreatorModal.tsx`, `src/hooks/useReviews.ts`
  Notes: After gig completion (content approved + payment processed), prompt business to rate creator (1-5 stars + optional comment). Save to reviews table. Update creator's rating_avg and rating_count. Show ratings on creator profile and in matching results. Per FR-015. Verify: Complete a gig, rating prompt appears, rating saves and displays.

---

## Phase 8: Post-Launch Iteration

> **Goal:** Based on early usage data, iterate on the most impactful features. Add refinements that weren't critical for launch but improve the experience significantly.

**Reference sections — read these before starting this phase:**
- PRD: § Feature Priority > Could Have (P2), § Open Questions
- Vision: § Product Strategy > Risks
- GTM: § Post-Launch Growth

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 8. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-8/post-launch-iteration`, commit, push, and open a PR for review."

- [ ] **TASK-055** — AI content quality scoring on upload
  Files: `supabase/functions/score-content/index.ts`, update `src/components/creator/ContentUploader.tsx`
  Notes: When creator uploads content, call AI to assess quality (composition, lighting, relevance to brief). Score 0-1 stored on content_deliverable. Show quality indicator to creator before submission. If score < 0.3, suggest improvements. Does not block submission — informational only for MVP. Verify: Upload content, see quality score and suggestions.

- [ ] **TASK-056** — Content template library
  Files: `src/pages/business/Templates.tsx`, `src/components/campaign/TemplateCard.tsx`
  Notes: Library of pre-built brief templates for common restaurant content needs: "New menu item launch", "Weekend special promo", "Behind the scenes", "Customer testimonial request", "Holiday/seasonal content". Business selects template, fills in specifics, AI enhances. Verify: Templates display, selecting one pre-fills the campaign creation form.

- [ ] **TASK-057** — Creator portfolio showcase (public profile)
  Files: `src/pages/creator/PublicProfile.tsx`
  Notes: Public-facing creator profile page (no auth required). Shows: name, avatar, bio, portfolio grid, content styles, rating, gigs completed. URL: /creators/:username. Businesses can share creator profiles. Useful for creator marketing. Verify: Public profile accessible without login, displays correctly.

- [ ] **TASK-058** — Business content library
  Files: `src/pages/business/ContentLibrary.tsx`
  Notes: All approved content from past campaigns in a filterable gallery. Filter by campaign, date, content type. Download individual content files. Reuse content or reference for new campaigns. Verify: Content library shows all past approved deliverables.

- [ ] **TASK-059** — Referral system for creators
  Files: `src/pages/creator/Referrals.tsx`, `src/components/creator/ReferralLink.tsx`
  Notes: Each creator gets a unique referral link. When a new creator signs up via referral link and completes their first gig, both referrer and referee get a $25 bonus. Track referrals in a new referral_codes table. Show referral stats on creator dashboard. Verify: Referral link generates, new signups tracked, bonuses credited.

- [ ] **TASK-060** — Bulk campaign creation for brands
  Files: `src/pages/brand/BulkCampaign.tsx`
  Notes: Brands select multiple restaurants and create campaigns for all of them with shared messaging and budget split. AI generates unique briefs per restaurant while maintaining brand consistency. Budget evenly distributed or custom per restaurant. Verify: Brand creates bulk campaign, individual campaigns created per restaurant.

- [ ] **TASK-061** — AI caption and hashtag optimizer
  Files: `src/components/creator/CaptionOptimizer.tsx`, `supabase/functions/optimize-content/index.ts`
  Notes: When creator writes a caption, offer AI "Optimize" button that improves the caption for engagement and suggests trending hashtags. Non-destructive: shows original and optimized side-by-side. Creator chooses which to submit. Verify: Optimizer generates improved caption and hashtag suggestions.

- [ ] **TASK-062** — Campaign analytics email reports
  Files: `supabase/functions/send-analytics-report/index.ts`
  Notes: Weekly email to brands with campaign performance summary. Include: campaigns active, content produced, aggregate impressions/engagement. Use Resend with HTML email template. Scheduled via Supabase cron or Edge Function scheduled invocation. Verify: Report email generates and sends with correct data.

- [ ] **TASK-063** — Dark mode / light mode toggle
  Files: `src/components/layout/ThemeToggle.tsx`, update `src/styles/globals.css`
  Notes: Toggle in header. Persist preference. Default: dark mode. Apply light mode overrides from docs/product-vision.md § Design Direction > Color Palette > Light mode overrides. Smooth transition between modes. Verify: Toggle switches modes, preference persists, all pages look correct in both modes.

- [ ] **TASK-064** — Donny AI chatbot integration
  Files: `src/components/ai/DonnyChatbot.tsx`
  Notes: Floating chat widget (bottom-right, draggable). AI assistant that helps users navigate the platform. Creators: "How do I improve my portfolio?", "Why haven't I been matched?". Businesses: "How do I create a campaign?", "What should I include in my description?". Uses AI API with DragonCandy-specific context. Verify: Chatbot opens, responds to questions with helpful platform-specific answers.

- [ ] **TASK-065** — Push notification support
  Files: `src/lib/push-notifications.ts`, service worker
  Notes: Browser push notifications for: new gig match, content approved, payment received. Request permission on first login. Use Web Push API with Supabase Edge Function as push server. Fallback to email if push not granted. Verify: Permission prompt appears, push notifications fire for key events.

---

## Phase 9: Growth & Optimization

> **Goal:** Data-driven improvements based on actual usage. Optimize the flows that matter most for growth: onboarding conversion, campaign completion rate, and creator retention.

**Reference sections — read these before starting this phase:**
- GTM: § Post-Launch Growth, § Key Metrics, § Channel Strategy
- Vision: § Product Strategy > Success Metrics

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 9. Then read only the Reference sections listed above from docs/gtm.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap."

- [ ] **TASK-066** — Analytics event tracking
  Files: `src/lib/analytics.ts`, instrument key pages
  Notes: Track key events: signup_started, signup_completed, onboarding_completed, campaign_created, brief_generated, gig_matched, gig_accepted, content_uploaded, content_approved, payment_processed. Use Supabase analytics table or integrate a lightweight analytics service (Plausible, PostHog, or Mixpanel free tier). Verify: Events fire on all key actions, visible in analytics dashboard.

- [ ] **TASK-067** — Onboarding conversion funnel
  Files: `src/pages/admin/Analytics.tsx` (update)
  Notes: Admin dashboard showing: signup → onboarding → first action conversion rates for each role. Identify drop-off points. Display as funnel chart. Verify: Funnel shows real conversion data.

- [ ] **TASK-068** — Creator onboarding optimization
  Files: `src/pages/auth/onboarding/CreatorOnboarding.tsx` (update)
  Notes: Based on drop-off data, optimize creator onboarding. Add progress indicator, reduce required fields for initial signup (allow completing portfolio later), add sample portfolios for inspiration. Goal: increase onboarding completion rate. Verify: Onboarding feels faster, progress is clear.

- [ ] **TASK-069** — Campaign completion rate dashboard
  Files: `src/pages/admin/CampaignHealth.tsx`
  Notes: Admin view showing: campaigns by status (funnel from created → brief → matched → delivered → approved → paid), average time per stage, bottleneck identification. Highlight campaigns stuck in a status for > 48 hours. Verify: Dashboard shows real campaign flow data.

- [ ] **TASK-070** — Creator retention features
  Files: `src/components/creator/StreakTracker.tsx`, `src/components/creator/EarningsMilestone.tsx`
  Notes: Streak tracker: consecutive weeks with completed gigs. Milestone celebrations: first gig, $500 earned, $1000 earned, 10 gigs completed. Subtle gamification without being childish — aligned with brand personality. Verify: Milestones trigger at correct thresholds, streak tracks correctly.

- [ ] **TASK-071** — Social sharing for campaigns
  Files: `src/components/campaign/ShareButton.tsx`
  Notes: After content is approved, generate a shareable link/card showing the content with DragonCandy branding. Business can share to their social media. Creator can share to their portfolio. Open Graph meta tags for rich previews. Verify: Share generates correct preview, links work.

- [ ] **TASK-072** — Performance monitoring and alerting
  Files: `src/lib/monitoring.ts`
  Notes: Track and alert on: AI brief generation time > 30s, payment processing failures, creator matching with 0 results, page load > 3s. Use console logging for MVP, with structured format for future integration with monitoring tools. Admin dashboard shows recent alerts. Verify: Alerts fire for configured thresholds.

---

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD, vision doc, and GTM doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. See the Phase Review section above.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything:** The PRD, vision doc, and GTM doc can be large. Each phase's Reference sections tell the agent exactly what to read. Loading the full documents wastes context.
- **Don't skip tasks:** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase:** Run the app after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged. This catches issues early when they're cheap to fix.
- **Update the status line:** After completing tasks, update the header status: `**Status:** X/72 tasks complete` and `**Current Phase:** Phase N`.
- **Commit incrementally:** Given DragonCandy's history with bulk changes breaking production, commit after each task — not at the end of a phase.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called phase-{N}/{slug}, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."
